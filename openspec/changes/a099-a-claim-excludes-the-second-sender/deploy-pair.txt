# 공동 배포 단위 선언 — tools/gate.sh 3단계가 읽는다.
#
# a099 는 원장에 임차를 넣는다. 임차를 잡은 채 죽은 행을 다시 집을 주체는
# a098 의 배달 루프이고, a099 시점에는 Notifier.Flush 의 프로덕션 호출자가 0이다.
# a099 만 배포하면 살아 있는 임차 동안 재발송이 억제되어 오늘보다 나쁘다.
# 사용자 결정 3 (2026-08-10) · design D9.
#
# 4라운드(2026-08-12)가 더한 배포 제약 둘 — design D14.6.
#
# (a) 롤링 교체 금지. 이미 열려 있는 v30 프로세스는 임차를 통째로 우회한다:
#     마이그레이션이 additive 라 옛 바이너리의 outbox SQL 이 그대로 돌고, 그 빌드는
#     claim 열을 읽지도 쓰지도 않으므로 v31 발송자가 쥔 행을 그냥 발행한다.
#     ErrSchemaTooNew 는 「그 뒤에 여는」 프로세스만 막지 「이미 열려 있는」 엔진을
#     fence 하지 못한다. 옛 컨테이너가 완전히 내려간 뒤 새 것을 올린다.
#
# (b) 스키마 31 은 저널 단위로 단방향이다. 바이너리만 되돌리면 엔진이 안 뜨고,
#     이 제품에서 그것은 손절이 없다는 뜻이다. 복구는 backup.go 의 절차이며
#     v30 사본 경로는 schema_meta.pre_migration_backup_path 에 있다.
a098-nobody-sends-what-the-outbox-keeps
