# 공동 배포 단위 선언 — tools/gate.sh 3단계가 읽는다.
#
# a099 는 원장에 임차를 넣는다. 임차를 잡은 채 죽은 행을 다시 집을 주체는
# a098 의 배달 루프이고, a099 시점에는 Notifier.Flush 의 프로덕션 호출자가 0이다.
# a099 만 배포하면 살아 있는 임차 동안 재발송이 억제되어 오늘보다 나쁘다.
# 사용자 결정 3 (2026-08-10) · design D9.
a098-nobody-sends-what-the-outbox-keeps
