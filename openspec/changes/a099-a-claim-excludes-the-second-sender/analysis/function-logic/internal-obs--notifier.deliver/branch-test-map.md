# Branch Test Map: `Notifier.deliver`

**GREEN 칸은 실측해서 채운다.**
`ast.json`의 열거가 정본이다: 분기 12 · 이탈 3.

| Branch | Scenario | Test | RED observed | GREEN observed |
|---|---|---|---|---|
| B1 | `:343` 시도 횟수 기본값 | 기존 — `obs_test.go:384` `TestPersistentDeliveryFailureBlocksEntries` | no | **yes (기존)** |
| B2 | `:349` **재시도 루프 — 그 전체가 한 임차 아래에 있다** | **a099 R6** | **회귀 핀 — RED 아님.** 배제하는 설계 = 「실패 기록이 임차를 푼다」 | no |
| B3 | `:350` publisher 미배선 | 기존 | no | **yes (기존)** |
| B4 | `:355` publish 성공 | 기존 | no | **yes (기존)** |
| B5 | `:357` 정산 성공 → `true` | 기존 | no | **yes (기존)** |
| B6 | `:373` 정산 실패 시 로그 | 기존 (a096 r2) | no | **yes (기존)** |
| B7 | `:378` 정산 실패 시 게이트 래치 → `false` | 기존 (a096 r2) | no | **yes (기존)** |
| B8 | `:384` **시도 기록이 임차를 풀지 않는다** | **a099 R6** | **회귀 핀 — RED 아님.** 배제하는 설계 = 「실패 기록이 임차를 푼다」 | no |
| B9 | `:387` **예산이 남았다 — 아직 안 끝났다** | **a099 R6** | **회귀 핀 — RED 아님.** 배제하는 설계 = 「실패 기록이 임차를 푼다」 | no |
| B10 | `:388` **대기 — 이 동안 행이 무주공산이면 안 된다** | **a099 R6** | **회귀 핀 — RED 아님.** 배제하는 설계 = 「실패 기록이 임차를 푼다」 | no |
| B11 | `:398` 예산 소진 시 로그 | 기존 | no | **yes (기존)** |
| B12 | `:403` 예산 소진 시 게이트 래치 | 기존 | no | **yes (기존)** |
| 이탈 `:406` | **예산 소진 — 여기 직전에 임차를 푼다** | **a099 R7** | **planned RED — 미관측.** 1라운드 B-P8이 *"오늘도 다음 사이클이 재시도하므로 born-GREEN"*이라고 지적했다. §3이 강화하거나 회귀 핀으로 강등한다 | no |

## ⚠⚠ 이 표가 a099의 초안을 뒤집었다

초안: *"`MarkAlertAttemptFailed`가 임차를 푼다."*

B8 → B9 → B10 → B2의 되돌아감이 그것을 반증한다. **`:384`에서 풀면 `:388`의 대기 동안
두 번째 발송자가 들어오고, 원래 발송자는 임차 없이 `:354`를 다시 실행한다.**

한 임차가 덮어야 하는 것은 **`:349`부터 `:406`까지 전체**다.

## ⚠⚠ 해제 자리 — 1라운드가 내 답을 두 번 고쳤다

| 자리 | 상황 | 1판 | **1라운드 뒤** |
|---|---|---|---|
| 이탈 `:381` 직전 | publish 성공 + **정산 실패** | 푼다 | **⛔ 안 푼다** — B-P3: 풀면 다음 관측이 다시 보내 **a096 폭풍이 돌아온다.** 임차가 억제 표시로 남고 만료가 푼다 |
| 이탈 `:406` 직전 | 예산 소진 | 푼다 | **푼다** — 유지 |
| `Flush`의 행마다 (D7) | 그 행의 일이 끝났다 | 푼다 | **푼다** — 유지 |
| 이탈 `:358` | 성공 | 별도 해제 없음 | 유지 — `MarkAlertDelivered`가 정산과 함께 푼다 |

**즉 두 자리다.** 1판이 「둘」이라 했고 내가 「셋」으로 늘렸는데, 리뷰가
늘린 그 하나를 되돌렸다. **tasks §4.5b가 이 표를 따라야 한다.**

`function-logic-map.md`의 Safety conclusion에 같은 정정이 있다.

## 이 함수에 **테스트를 새로 쓰지 않는 분기**

- **B1 · B3 · B4 · B5 · B6 · B7 · B11 · B12** — 기존 테스트가 덮고 a099가 조건도
  반환도 안 바꾼다. **`not-applicable`: 이 change는 이 여덟을 근거로 아무것도
  주장하지 않는다.** 다만 B7과 이탈 `:381`은 §4.5b의 표에 **「⛔ 안 푼다」로** 들어간다 —
  publish는 나갔는데 원장이 그것을 모르는 자리이므로 **해제 자리가 아니다**
  (design D3 · 1라운드 B-P3). **해제는 이탈 `:406` 직전 하나뿐이다.**

  > **⚠ 3판까지 이 문장은 이탈 `:381`을 「해제 자리」라고 불렀다.** 같은 번들의
  > function-logic-map이 스무 줄에 걸쳐 그 반대를 논증하는데도 그랬다. 4라운드가 잡았다.

## 덮이지 않은 것을 이름으로 적는다

- **`Publish`의 실제 기한이 이 함수 밖이다.** `Ntfy.Timeout` 기본 10초를 근거로
  54초를 유도했지만 다른 Publisher가 배선되면 깨진다. §3.4가 실측한다.
- **`n.mu` 구간** — a099의 대상이 아니다. a092가 줄인다.
- **`wait`(`:410-420`)의 내부** — 별도 함수다. a092의 산출물이 있고 a099는 안 건드린다.
