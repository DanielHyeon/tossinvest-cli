# Branch Test Map: `Journal.MarkAlertAttemptFailed`

**GREEN 칸은 실측해서 채운다.**
`ast.json`의 열거가 정본이다: 분기 1 · 이탈 2.

| Branch | Scenario | Test | RED observed | GREEN observed |
|---|---|---|---|---|
| B1 | `:359` `ExecContext`가 실패한다 | 없음 — DB 오류 주입 없음 | no | **no (기존부터 없다)** |
| 이탈 `:362` | **실패를 기록하되 임차는 유지한다** — 발송자가 아직 재시도한다 | 기존 `outbox_test.go` + **a099 R6 · R13** | **R13은 planned RED(§4.5 전), R6은 회귀 핀.** 1라운드 B-P8: 오늘 `n.mu`가 재시도 루프 전체를 덮으므로 **같은 `Notifier`로 쓰면 born-GREEN이다** — 다른 뮤텍스와 장벽이 필요하다 | no |

## ⚠⚠ 초안이 여기서 임차를 푼다고 했고, `deliver`의 AST가 그것을 반증했다

`internal-obs--notifier.deliver` 번들 참조 — 이 함수를 부르는 B8 `notifier.go:384`
다음이 B9 `:387`·B10 `:388`이고 루프가 `:354`로 되돌아간다.
**여기서 풀면 `:388`의 대기 동안 두 번째 발송자가 들어온다.**
해제는 `ReleaseAlertClaim`이 루프 **밖**에서 한다.

## 이 함수에 **테스트를 새로 쓰지 않는 분기**

- **B1** — 드라이버 오류 경로. a099가 조건도 반환도 안 바꾼다.
  **`not-applicable`: 이 change는 B1을 근거로 아무것도 주장하지 않는다.**

## 덮이지 않은 것을 이름으로 적는다

- **`Flush:452`가 이 함수의 반환을 버린다** (`_ =`). a099가 소유자 비교를 더하면
  「임차를 잃었다」가 그 자리에서 **조용히 사라진다.** 정상 경로에서는 안 생기지만
  (D7이 `Flush`를 claim 뒤에만 부르게 한다) **만료가 발송 중에 일어나면 여전히
  조용하다.** a099가 남기는 알려진 구멍이고 contract 단계에서 `Flush`와 함께 사라진다.
  **여기에 적어 두는 이유는 침묵한 생략을 금지하기 때문이다.**
- **`deliver:384`도 이 함수의 오류를 로그로만 다룬다**(B8). 소유자 불일치가
  그 로그에 섞이면 「전송 실패」와 「임차 상실」이 구분 안 된다.
  **§4.5가 그 둘을 다른 이벤트로 적어야 한다.**
- `requireOneRow`의 분기는 그 함수의 것이다 (`outbox.go:465-474`). a099는 안 건드린다.
