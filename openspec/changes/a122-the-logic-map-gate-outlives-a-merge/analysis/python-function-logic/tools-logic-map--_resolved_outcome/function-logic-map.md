# Function Logic Map: `_resolved_outcome` (Python, a122 task 7.5.12 — 새 함수)

## task 7.5.12 — 새 함수 (2026-09-27)

최종 `check_analysis.py` `463982f42151`. `os.path.realpath` 의 대답을 지문과 같이 — 풀이는 실패하지 않는다(고리는 풀리지 않은 채 돌려준다). 재확인(`_PROBE_NOW["resolved"]`)과 판정이 **같은** 함수를
부른다. 분기 0.
