# Function Logic Map: `_resolved` (Python, a122 task 7.5.12 — 새 함수)

## task 7.5.12 — 새 함수 (2026-09-27)

최종 `check_analysis.py` `463982f42151`. 경로를 **푸는 유일한 자리** — `_resolved_outcome` 을 부르고 원장에 `("resolved", 주어진 경로) → "resolved:" + 풀린 경로` 를 적는다. 분기 0.
