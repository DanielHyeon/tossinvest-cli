# Function Logic Map: `_kind` (Python, a122 task 7.5.2.3)

## task 7.5.2.3 — 재확인의 입력 집합은 판정의 입력 집합이다 (7.5.2.2 재리뷰, 2026-09-20)

`tools/logic-map/check_analysis.py:848-852` · 분기 0 · 반환 1 · raise 0 (새 함수).

새 함수. 읽지 않고 **고르는** 판정도 입력이다(어느 change 디렉터리를 판정하는가 · 인용한 시험 파일이 있는가). `is_dir()` · `is_file()` 은 `OSError` 를 삼켜 "못 물었다" 를 "없다" 로 만들고, 그 갈림은 아무 데도 안 남았다.

| id | 줄 | 종류 | 소스 |
|---|---|---|---|
