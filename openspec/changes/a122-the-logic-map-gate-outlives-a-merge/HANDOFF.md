# a122 인계 — 다음 세션이 먼저 읽는 문서

작성 2026-09-18. 대상은 이 change 를 이어받는 다음 세션과, 아래 **사람 결정 두 개**를
정해야 하는 사람이다.

**한 줄(2026-09-25 오후): 7.5.34 를 재리뷰 둘로 돌렸고 사람이 선택지 1 을 골라 7.5.35 로 닫았다 — 해시가 맞는 트리도 git 이 쓰는 모양이어야 받고(`040000` · 같은 이름 · `/` 든 이름이 요구를 비웠다, 7.5.34 의 회귀), 판정 diff 의 형식(접두사 · 색 · 훅 합치기 · `GIT_DIFF_OPTS`)을 게이트가 정한다.** 남은 것: 7.5.26 의 `"` · `\` 든 이름 · 7.5.32(사람 결정) · 7.5.33 남은 절반 · 7.5.3~7.5.7 사람 결정. **이 로트는 tossos-42 세션의 것이다** — 병행 세션이 HANDOFF 의 "다음 세션은 여기부터" 를 보고 고아 로트로 오판해 시험 파일을 만진 일이 있었다(7.5.35). 이어받기 전에 `ListAgents` 로 살아 있는 소유 세션이 있는지 볼 것.

**한 줄(2026-09-25): 7.5.31 을 재리뷰 둘로 돌렸고 사람이 선택지 1 을 골라 7.5.34 로 닫았다 — 판정 blob 을 실제 저장소에서 oid 로 찾지 않는다.** 7.5.31 의 git 밖 대조는 있음만 봐서 내용 위조 · rename 뒤 위조가 `[]` 였다. 이제 판정 바이트는 디스크(게이트가 해시)와 해시를 검증한 객체뿐이고, 두 diff 는 대체 저장소 없는 임시 저장소의 두 트리를 견준다 — base · 커밋 대상 · 하위 트리 위조는 이름 대고 멈추고, 워킹트리 쪽 위조는 판정에 안 닿으며, pathspec 변수 · 빈 새 파일 헛거절 · 연결 워크트리 경로 오류가 같이 없어졌다. 시험 372 · 변이 189 전부 CAUGHT · A/B 126/126 SAME (git 20,825 → 2,431). 남은 것: 7.5.33 의 부분 클론 fetch(P2) · 7.5.26 형식 · 7.5.32 파서(사람 결정). 주장정확성 여덟째 재발(틀린 문장 넷) 정정.

**그전 한 줄(2026-09-24 세 번째): 7.5.25 를 재리뷰 둘로 돌렸고 7.5.31 로 닫았다 — 내 회귀 셋 · 헛거절 둘 · 거짓 일곱.** 스냅숏은 디스크 바이트를 바르게 읽지만 두 diff 가 그 blob 을 oid 로 다시 찾을 때 git 이 저장소를 믿었다 — 교체 참조 하나(F1), 위조 loose · pack 객체(F2 · F3)로 요구가 조용히 비었다(부모는 잡았다). 수리: 모든 git 에 `GIT_NO_REPLACE_OBJECTS=1`, 그리고 **git 밖의** 대조(`_snapshot_disagreement` — Python 이 해시한 oid 가 base 와 다르면 diff 에 나와야 한다). 헛거절 둘(`:`·`"` 경로 · 비 UTF-8 이름)을 없앴고, 같은 실수 일곱 번째(뒤에 더한 시험이 옮겨 적은 수를 낡게 함 + 재지 않고 옮긴 리뷰 문장)를 정정했다. 새 task: **7.5.32**(파서를 판정받는 워킹트리에서 빌드 — P0, 사람 결정) · 7.5.33(base 쪽 저장소 신뢰). 시험 362 · 변이 170 전부 CAUGHT.

**그전 한 줄(2026-09-24 두 번째): 사람이 7.5.25 에 대해 선택지 1(class 수리)을 골랐고 7.5.25 로 닫았다 — 게이트가 git 의 워킹트리 투영을 안 쓴다.** 로트마다 재리뷰가 git 이 워킹트리를 비교 전에 다시 쓰거나 안 읽는 새 방법을 찾았다(마지막: `working-tree-encoding=UTF-7` 의 이중 표현, 그리고 **설정 없는** stat 캐시 10/10). 이제 `_worktree_snapshot` 이 추적 `*.go` 의 디스크 바이트를 깔때기로 읽어 임시 객체 저장소 · 임시 인덱스에 싣고, 두 diff 는 `--cached` 로 그것을 base 와 견준다 — 판정의 현재 쪽도 같은 바이트다. 지운 것: `_git_view_pins` · `_ident_go_paths` · `_hidden_by_index_flags`. 실제 저장소에는 안 쓴다(`splitIndex` · `hooksPath` · fsmonitor 고정, 시험이 `.git` 전 바이트를 견준다). 시험 348 → 357 · 변이 160 전부 CAUGHT (생존 셋은 우연이 지키던 것이라 시험으로 못 박았다). 남은 문: 출력 형식 · pathspec(7.5.26), skip-worktree 뒤의 삭제(잔여). **새 task 7.5.30**: 시험 스위트 한 판이 공유 go 캐시를 ~375 MB 불린다 — 변이 하네스를 병렬로 돌리면 디스크가 찬다. 하네스는 이제 `-trimpath` + 전용 캐시로 돈다. 사람 결정 대기: 7.5.3 · 7.5.4 · 7.5.5 · 7.5.6 · 7.5.7.

**그전 한 줄(2026-09-24): 7.5.24·7.5.27 을 재리뷰 둘로 돌렸고 7.5.28 로 닫았다 — 내 회귀 하나 · 내 거짓 하나 · 새 문 하나 · 옛 결함 하나, 그리고 같은 실수 다섯 번째.** **(1) 내 회귀**: 판정 diff 를 `str.splitlines()` 로 잘라 U+2028 · U+2029 · U+0085 에서도 잘렸다 — 경로 `a<구분자>@@ -1 +1 @@.go` 가 `diff --git` 머리를 잘라 뒷조각이 훅으로 읽히고 그 파일의 요구가 **통째로** 사라졌다. 이름 짝짓기(7.5.23)는 거절했으니 **7.5.24 의 순서 짝짓기가 낸 회귀**다. 바이트로 받아 `\n` 에서만 자른다. **(2) 내 거짓**: 7.5.27 의 "`checkStat` 우회 재현 안 됨" 은 픽스처가 mtime 을 과거로 안 돌려 racy 창에 걸린 탓이었다 — `checkStat=default`·`trustctime=true` 고정. **(3) 새 문**: 내장 속성 `ident` 가 `$Id: …$` 안의 논리 변경을 감춘다 — `-c` 로 못 꺼서 이름 대고 거절. **(4) 옛 결함**: git 은 이름에 공백이 있으면 `---`·`+++` 줄 끝에 **탭**을 붙이는데 그것을 이름으로 읽어 공백 든 `*.go` 가 전부 거짓 차단되고 있었다(이 change 이전부터, 노출 0). **흔들린 시험이 새 측정을 낳았다**: inode 재사용 + 같은 초의 ctime + 과거 mtime 은 **설정 없이도** git 의 기본 stat 검사를 속인다(**20 판 중 17 판**) — 명령줄 고정으로 못 닫는 class 라 7.5.25 에 적었다(임시 인덱스 사본에 `--really-refresh` 를 거는 선택지와 함께). **(5) 같은 실수 다섯 번째**: 가드 분기 "그대로"(실제 14→15) · BTM 이 댄 시험이 그 갈래를 **못** 박음 · 변이 창 이름 틀림 · 빈 `process=` 의 기전("시작 실패" 가 아니라 **아무것도 안 띄움**) · 하네스 주석의 낡은 수 — 이번엔 도구를 늘리지 않고 **순서**를 바꿨다: 산문은 마지막 측정 **뒤에** 쓰고, BTM 의 "무엇이 못 박나" 는 변이 로그에서 옮겨 적는다. 새 task: **7.5.29**(`_self_repair_commits` 도 git 출력을 `splitlines()` 로 자른다 — 판정 영향은 안 쟀다). 증거: 시험 339 → **348** · 변이 **152** 전부 CAUGHT(첫 판 38/39 → 경계 시험 → `AG` 9/9) · `lint`·`sdd-test`(logic-map **423**)·`test-seams`(**108**)·a122 게이트 rc 0 · A/B **126/126 SAME**(git 호출 **+1.1%**, `ident` 조회). 사람 결정 **7.5.3 · 7.5.4 · 7.5.5 · 7.5.6 · 7.5.7 · 7.5.25(class — 이제 설정 없는 stat 구멍까지)** 대기.

**그전 한 줄(2026-09-23 세 번째): 사람이 7.5.25 에 대해 선택지 2(싼 막기)를 골랐고 7.5.27 로 닫았다 — 워킹트리를 다시 쓰는 **알려진 문** 다섯을 닫았고 class 는 7.5.25 에 남는다.** 문을 진짜 git 으로 셌다: `filter.<d>.clean` · 진짜 v2 프로토콜의 `filter.<d>.process` · 거짓말하는 `core.fsmonitor`(리뷰어도 안 적은 새 문) 셋은 편집을 **두 시야 모두**에서 감추고 명령줄 `-c` 고정으로 돌아온다 → 판정의 **두** `git diff` 에 **같은** 고정(`core.fsmonitor=false` + 설정된 모든 드라이버의 `clean=`·`process=`·`required=false` — `clean` 만 비우면 `required=true` 에서 git 이 rc 128 로 죽는다, 드라이버 이름에 점이 들 수 있다). `assume-unchanged`·`skip-worktree` 는 인덱스 플래그라 고정으로 안 닫히므로 플래그 붙은 `*.go` 를 같은 고정으로 해시해 인덱스와 **다를 때만** 이름 대고 거절한다 — sparse-checkout 도 `skip-worktree` 를 쓰므로 플래그만으로는 안 거절한다. `checkStat` 우회는 **재현되지 않아** 안 넣었다(7.5.28 정정: **거짓** — 내 픽스처가 mtime 을 과거로 안 돌렸다). **실물 게이트**(a112, 격리 worktree · 설정은 **환경 변수로만** 주입해 공유 `.git/config` 에 아무것도 안 썼다): 추적 안 된 `.gitattributes` 한 줄 + clean 필터면 편집 전 **판정 줄 36 → 0 · `evidence complete`**, 편집 후 **36 · 64 그대로**(거절이 아니라 올바른 판정). **내 측정 오류 하나를 스스로 찾았다**: 첫 전수에서 process 필터를 "감췄다" 로 읽었는데 프로토콜을 안 지키는 스크립트라 **git 이 rc 128 로 죽은 것**이었다(rc 를 안 봤다) — 진짜 v2 필터로 다시 쟀다. **변이에서 git 의 우연 하나**: `process=` 하나만 비워도 clean 이 꺼져서(빈 `process` 값은 아무것도 안 띄우고 clean 을 끈다 — 7.5.28 정정: "시작에 실패" 가 아니었다) `clean=` 을 빼는 변이가 살아남았다 — 동등이라 적지 않고 **반환값을 직접 재는 계약 시험**으로 못 박았다. 옛 mock 시험 셋이 새 git 호출에서 먼저 실패를 받아 **이름과 다른 이유로** 통과할 뻔한 것도 막았다. 새 거절이 오늘 거절하는 정상 입력 **0**(추적 `*.go` 1,762 전부 `H` · 드라이버 0 · fsmonitor 없음 · sparse 꺼짐), 바뀔 수 있는 **집합**도 적었다(정상적인 clean 필터 — git-lfs · git-crypt). 증거: 시험 329 → **339** · 변이 첫 판 `113:143` **29/30** → `AF` 재실행 **11/11**(총 143, 생존 0; 7.5.28 정정: "`113:130` 19/19" 는 창 이름이 틀렸다) · `lint`·`sdd-test`(logic-map **414**)·`test-seams`(**108**)·`openspec` 58/58·a122 게이트·좌표 게이트 전부 rc 0 · 실물 `main()` A/B **126/126 SAME**(git 호출 20,365 → **20,596, +1.1%** — 설정 읽기와 인덱스 조회가 붙었다). 사람 결정 **7.5.3 · 7.5.4 · 7.5.5 · 7.5.6 · 7.5.7 · 7.5.25(class)** 대기.

**그전 한 줄(2026-09-23 두 번째): 7.5.23 의 수리가 **정상 입력을 거절하는 회귀**였고, 정정은 또 자기 실수를 되풀이했다. 둘 다 7.5.24 로 닫았고, 더 큰 구멍 하나는 **사람 결정(7.5.25)** 으로 넘긴다.** **(1) 내가 게이트를 깨뜨렸다.** 7.5.23 의 교차 검사가 짝을 **이름으로** 지었는데, 가드의 이름은 `-z` 라 **날 바이트**이고 파서의 이름은 `removeprefix` 를 거친 **유도값**이며 git 이 인용하면 `"a/we\"ird.go"` 다 — 두 이름 공간이 안 만난다. 부모와 A/B 로 잰 결과 **정상 입력 셋이 새로 거절**됐다(새 파일 `we"ird.go` · `back\slash.go` · `diff.noprefix` 아래 `b/new.go`), 그것도 `cannot derive modified Go functions` 라 **판정 줄이 하나도 안 나가고** 메시지는 **없는 필터를 탓했다**. 내가 쓴 "새 거절이 거절하는 정상 입력 0" 은 **빈 표본**이었다 — 오늘 저장소의 이름을 센 것이지 거절 가능한 **집합**을 센 것이 아니다. 수리는 **이름을 아예 안 비교하는 것**: 두 호출은 같은 `git diff` 라 파일 순서가 같으므로(여섯 모양 섞은 픽스처로 실측, 레코드 5 · 구역 5 정렬 일치) `zip(records, bodied)` 로 짝짓고 크기가 다르면 그것도 거절한다. textconv 차단은 그대로다. **(2) 경계 셋이 비어 있었다**: 건너뛰기 조건의 `and`→`or` 변이가 **326 시험을 전부 통과**했고(한쪽 칸이 0 인 레코드가 통째로 빠진다) · 크기 검사를 지우는 변이가 **328 시험을 전부 통과**했으며 · git 이 인용하는 이름을 **거절하면 안 된다**는 시험이 없었다. 셋 다 못 박았다. 가드를 mock 하던 옛 시험 셋도 고쳤다(빈 `MagicMock` 이 "바뀐 파일 0" 을 뜻하게 됐다). **(3) 정정이 네 번째로 같은 실수를 했다**: 인용 수를 **105** 라 적었는데 같은 커밋의 HANDOFF·커밋 메시지·하네스는 **106** 이었다(한 커밋 안에서 갈렸다) · BTM 이 **draft** 의 코드를 적었다 · "앵커 셋 재겨눔" 이 실제로는 하나. 셋 다 "하네스가 낸 수를 산문에 베껴 적고 그 뒤 편집이 수를 바꿨다" 이므로, **좌표 게이트가 이제 자기가 내는 수를 산문과 대조**한다(다르면 rc≠0). **(4) 내가 공유 저장소에 남긴 것을 지웠다** — textconv 재현을 격리 worktree 에서 했는데 worktree 는 `.git/config` 를 **공유한다**. 킬 스위치의 절반(`diff.nop.textconv`)이 저장소에 남아 있었다. 격리 worktree 는 파일은 격리하지만 **설정은 격리하지 않는다**. **(5) 사람 결정 7.5.25 — 더 큰 구멍이 남았다.** 교차 검사는 **어긋남**만 본다. `filter.<x>.clean` 은 `git diff` 가 비교하는 **워킹트리 바이트 자체**를 바꾸므로 `--numstat` 이 레코드를 아예 안 낸다 — 어긋남이 없어 거절도 없다. 실측(게이트의 **기본 모드**인 워킹트리 대상): 디스크에 `return 999` 가 있는데 `numstat` 이 빈 글자이고 `required` 가 `[('x.go','Stop')] → []`. `skip-worktree`·`assume-unchanged` 도 같은 계열이다. 닫으려면 게이트가 **git 의 어떤 투영도 안 믿고** 자기가 판정하는 바이트를 직접 대조해야 한다 — 이 change 밖의 판정을 바꿀 수 있는 규칙 변경이라 7.5.4 · 7.5.6 · 7.5.7 과 같은 줄에 뒀다. 증거: 시험 326 → **329** · 변이 **19/19 CAUGHT · 생존 0**(총 132, 대조군 양끝 GREEN `Ran 329`) · `lint` rc 0 · `sdd-test` rc 0(logic-map **404**) · `test-seams` rc 0(**108 패키지**) · `openspec` 58/58 · a122 게이트 rc 0 · 좌표 게이트 rc 0. 사람 결정 **7.5.3 · 7.5.4 · 7.5.5 · 7.5.6 · 7.5.7 · 7.5.25** 대기.

**그전 한 줄(2026-09-23): 7.5.22 를 독립 재리뷰 셋으로 돌렸더니 내가 닫았다고 한 P0 이 **안 닫혀 있었고**, 정정 커밋이 **자기 실수를 되풀이**하고 있었다. 둘 다 7.5.23 으로 닫았다.** **(1) P0 이 안 닫혔다.** `--numstat` 이 가르는 것은 "git 이 본문을 냈는가" 가 아니라 **"이진으로 다루는가"** 다. `diff=<드라이버>` 의 **textconv** 는 원본 blob 을 세는 `--numstat` 에 `1`/`1` 을 내면서 판정 diff 의 본문을 통째로 지운다 — 추적 **안 된** `.gitattributes` 한 줄 + config 키 하나로 실물 게이트(a112)가 다시 **required 0 · `evidence complete` · rc 0** 이었다(재현했다). 기전은 7.5.22 가 **진단한 것 그대로** — 가드와 판정이 다른 투영을 본다. 덤으로 `--no-ext-diff` 도 문을 닫고 있었는데 **지워도 313 시험이 전부 초록**인 공짜 삭제였다. 수리는 둘이고 둘째가 첫째를 못 박는다: 판정 diff 에 `--no-textconv`, 그리고 **두 투영을 맞춰 보기**(numstat 이 내용이 바뀌었다고 한 파일이 훅을 하나도 안 냈으면 거절). 문을 하나씩 세는 대신 **어긋남**을 보므로 깃발이 사라져도 런타임에 잡힌다 — 실측으로 두 깃발을 각각 지우면 거절이 나온다. **(2) 정정이 자기 실수를 되풀이했다** — 열둘(표는 `review.md` `## 정정 — task 7.5.22`): 열거표가 또 **자기 번들 둘이 빠진** 값(12,412, 실제 **12,414**) · 새 좌표가 **전부 +2 만큼 낡음**(같은 커밋의 내 주석이 민 것) · 두 `ast.after` 의 지문이 **역사 어디에도 없는 중간 상태** · RED 기록이 같은 커밋의 다른 두 문서와 어긋남 · 자식 프로세스 열거를 고치며 또 틀림(아홉→**열**) · 살아 있는 좌표 하나(`_verdict:2159`) 누락 · 7.5.13 사유가 앞뒤 안 맞음. **세 로트 연속 같은 기전**이라 값만 고치지 않고 **규칙을 세는 것**으로 만들었다: `analysis/harness/7523_coords.py` 가 열린 task 의 살아 있는 좌표 · 지문 없는 `ast*.json` · 범위 틀린 `ast*.json` 을 세고 하나라도 있으면 **rc≠0** 이다(그 하네스가 덤으로 **7.2.6 이 남긴 뜬 번들 둘**도 찾아 되살렸다). 산문 전체의 절대 인용 **106 건**은 누적이라 세기만 한다 — 다음 로트가 줄인다. **(3) 계측기를 또 고쳤다**: 표식 head 가 유일하지 않으면 `replace` 가 첫 자리에 심는데 문법은 멀쩡해 `compile()` 이 못 본다 — head **20 개**가 유일하지 않고 그중 **다섯**이 다른 문장에(넷은 다른 함수에) 앉았다. 이제 유일할 때만 심고 · 일부만 심겼으면 `"?"` · 심을 자리가 **없으면** `"?"`(앞 판본은 표식 없는 판을 돌려 놓고 "안 닿음" 이라 답했다 — **내 수리가 낸 새 구멍**) · 원복이 `finally` 안 · `control_ran` 기본값 제거. 그리고 **계측기를 처음으로 직접 시험했다**(`7523_reached.py` **6/6**) — `reached()` 는 SURVIVED 인 판에서만 불려 생존 0 인 로트에서는 증거에 안 나타난다. 증거: 시험 313 → **326** · 변이 `AB1~AB5·AC1~AC5·AD1~AD4·AD6` **15/15 CAUGHT · 생존 0**(총 128, 대조군 창 양끝 GREEN `Ran 326`) · `AD5` 는 **동등 변이**라 적지 않고 **중복을 없앴다** · `AD6` 은 행동이 같아 **구조 시험**으로 못 박았다 · 새 거절이 오늘 거절하는 정상 입력 **0**(저장소에 `.gitattributes` **0 개**) · `lint`·`sdd-test`(logic-map **401**)·`test-seams`(**108 패키지** FAIL 0)·`openspec` 58/58·a122 게이트·좌표 게이트 전부 rc 0. 사람 결정 **7.5.3 · 7.5.4 · 7.5.5 · 7.5.6 · 7.5.7** 그대로 대기.

**그전 한 줄(2026-09-22 다섯 번째): 7.5.2.4 수리 트리를 독립 재리뷰 셋으로 돌렸고(적대 · 정확성 · 주장정확성), 사람이 고른 선택지 2 를 7.5.22 로 닫았다 — 정정 + P0.** 리뷰가 확인한 것은 **기전 주장은 참 · 숫자와 좌표는 거짓**이었다. 내가 쓴 거짓 **열넷**을 전부 내 손으로 다시 재서 고쳤고(표는 `review.md` `## 정정 — task 7.5.2.4`), 큰 것 넷은: (1) `make test-seams` "46 패키지" 는 **rtk 가 자른 로그**를 센 것이다 — 다시 재니 **108**(`ok` 100 · `[no test files]` 8 · FAIL 0)이고 `go list ./...` 과 같다, [[missing-tool-reports-clean]] 이 경고한 그 실수다 · (2) **새 task 열다섯의 `check_analysis.py` 줄 번호가 전부 편집 전 파일**을 가리켰다(내 편집이 민 것이다) → 전부 **함수 이름**으로 바꿨다 · (3) STALE 귀속 "a112 16" 은 거짓이고 실제는 **a112 13 · a063 2 · 아카이브 a060 2 · 아카이브 a044 1**(활성 15 · 아카이브 3) — 사람이 **7.5.7** 을 결정할 근거가 바로 이 내역이다 · (4) 열거표 12,411/1,633 은 **편집 도중** 스냅숏이고 커밋된 하네스가 내는 값은 **12,412/1,634** 다. 열넷 중 아홉이 **같은 실수**다 — 오염되거나 편집 도중인 측정을 값으로 적었다. **P0 은 `.gitattributes` 한 줄로 게이트가 꺼지던 것**이다: 워킹트리에 `*.go binary`(또는 `-diff`)만 있으면 — **추적 안 해도** — git 이 훅을 0 개 내고 그 change 의 Go 요구가 **판정 줄 없이 0 건**이 된다. 수리는 앞단 가드가 `--name-only` 대신 `--numstat` 을 읽는 것이다(같은 자리, 호출 수 그대로): 본문 억제 `-`/`-` 와 정상인 mode-only `0`/`0` 이 정확히 갈린다. 덤으로 rename 의 **옛 이름**이 가드 범위에 들어와 **7.5.13 의 절반이 닫혔다**. **내가 만든 회귀 둘도 같이 닫았다**: 도달 계측기가 표식으로 문법을 깨면 트레이스백이 표식을 인쇄해 **시험 0개 돈 판**을 "도달함" 으로 적었고(118 중 **30** — 7.5.23 이 갈랐다: 거짓 "도달함" 26 + 거짓 "안 닿음" 4) · 7.5.2.4 가 훅 적재를 `hunk()` 로 **옮기면서** 안 잰 자리가 있어 뒤 쌍(현재 쪽)을 앞 쌍으로 읽는 변이가 시험 305개를 **전부 통과**했다(`current_hash` → `base_hash`, 이 change 가 없애려는 그 내려앉음). 증거: 시험 305 → **313** · 변이 `AC1~AC5` **5/5 CAUGHT**(총 123, 각 변이가 정확히 그 뜻의 시험을 빨갛게 한다) · 새 거절이 오늘 거절하는 정상 입력 **0**(117 change · `*.go` 파일 줄 **97,235** 전수 — **측정이지 유도가 아니다**) · 가드 단독 A/B **234/234 SAME** · 실물 `main()` A/B **126/126 SAME · DIFFERENT 0**(계수 불변 — git 20,365 → 20,365 · `ast.json` 읽기 9,072 → 9,072, **호출이 하나도 안 늘었다**) · 하네스 **다섯** 커밋(`7522_stale` · `7522_numstat` · `7522_guard_ab` · `7522_marker` · `7522_switch`). **새로 연 task 하나: 7.5.22(닫음).** 기록은 `review.md` `## 정정 — task 7.5.2.4` · `## MEASURE · Pre-Edit Gate — task 7.5.22` · `## VERIFY — task 7.5.22`. 사람 결정 **7.5.3 · 7.5.4 · 7.5.5 · 7.5.6 · 7.5.7** 그대로 대기.

**그전 한 줄(2026-09-22 네 번째): 7.5.2.3 수리 트리를 독립 재리뷰 다섯으로 돌렸고(적대 · 문서정확성 · 정확성 · 보안 · 시험품질), 사람이 고른 선택지 1 을 7.5.2.4 로 닫았다.** 가장 큰 것은 **내 기록 셋이 거짓**이었다는 것이다: (1) "디스크를 읽는 자리가 깔때기 넷뿐" 은 **이 모듈의 파이썬 읽기**에만 참이고 자식 프로세스(`git diff` · `git show` · `go run`)가 읽는 판정 입력은 원장 밖이다 — "전수" 는 사실이 아니라 **방법의 한계**였다(→ 7.5.8, P0, 아직 열려 있다) · (2) 도달 계측기는 실제로 **눈이 멀어** 있었다(원복 전에 불렸다 — 정적 재연 79/113) · (3) 변이 113 중 Y13·Y14 는 `NameError` 라 정직한 값은 111+2 였다. 7.5.2.4 가 고친 것은 **통합 diff 의 본문 줄이 파일 헤더로 읽히던 P0**(지워진 `-- x` 가 `--- x` 로 나와 그 파일의 요구가 **0 건**이 되거나 `revision: base` 로 내려앉았다 — 진짜 git 으로 재현, 오늘 저장소에 그 모양 **111 줄/10 파일**)과 문서·VERIFY 정정 열, 하네스 계측기 셋이다. 증거: 시험 298 → **305** · 변이 `AB1~AB5` 5/5 CAUGHT(총 118) · **A/B 는 전수 논증**(change 117 전부에서 문제의 줄이 0 → 두 파서의 `required` 가 같다) + 실물 `main()` A/B. **새로 연 task 열다섯: 7.5.7~7.5.21** — 그중 **7.5.8 이 P0** 이고 **7.5.7 은 사람 결정**(파일 수준 해시를 넣으면 오늘 정상인 번들 18개가 새로 거절된다 — 귀속은 **7.5.22 에서 정정**했다). 기록은 `review.md` `## 정정 — task 7.5.2.3` · `## VERIFY — task 7.5.2.4`. 사람 결정 **7.5.3 · 7.5.4 · 7.5.5 · 7.5.6 · 7.5.7** 대기.

**그전 한 줄(2026-09-20 세 번째): 7.5.2.2 수리 트리를 다시 재리뷰했고(출처 여섯) 그 결과를 7.5.2.3 으로 고쳤다.** 7.5.2.2 는 끝에서 디스크와 대조했지만 그 **집합을 손으로 골랐다**(`HEAD` + `Evidence`) — a112 한 판 실측으로 판정이 읽는 1,739 경로 중 149 만 봤고, 산문 · 워킹트리 소스 · `review.md` · `base-commit.txt` · 시험 색인을 실행 중 건드리면 `[]` PASS 였다. 손으로 고른 목록이 낡아서 깨진 것이 **두 번째**라, 이제 목록을 만들지 않는다: 디스크를 읽는 **깔때기 넷**이 읽은 결과(실패 포함)를 **원장**에 적고 재확인은 그 원장을 그대로 다시 읽는다 — 재확인의 집합이 구조적으로 판정의 집합이다(깔때기 밖의 원시 호출은 AST 구조 시험이 막는다). 곁들여: 기록 명령이 쓰기 직전에 **거절 집합 전체**를 다시 묻고(순회 도중 Go 편집 → 영구 기록이 생겼다) · `HEAD` 를 재확인 **앞뒤로** 묻고 · 조용한 건너뛰기를 없앴다(FIFO 교체로 감사를 끄던 문, 실측 6/14). A/B **126/126 SAME** · 변이 **113/113**(AA19 는 살아남아 시험을 더해 잡았다) · 시험 273 → **298** · a122 rc=0. 비용: 파일 열기 a112 2,407 → 3,854(+60%, 5.94s → 6.69s). **아직 독립 재리뷰 전**이다(커밋 뒤 돌린다) — `review.md` `## VERIFY — task 7.5.2.3`. 사람 결정 **7.5.3 · 7.5.4 · 7.5.5 · 7.5.6**(새로 하나: 감사 스위치의 비교 규칙 — BOM · UTF-16 · 좌표 없는 행으로 쉬고 있을 때도 감사가 꺼진다) 대기.

**그전 한 줄(2026-09-20 두 번째): 7.5.2.1 수리 트리를 gstack 으로 다시 재리뷰했고(출처 아홉) 그 결과를 7.5.2.2 로 고쳤다.** 7.5.2.1 은 증거를 **한 번** 읽어 판정을 내적으로 일관되게 만들었지만, 그 판정이 **지금 거기 있는 것**의 판정인지는 안 봤다 — 이제 판정을 내놓기(기록을 쓰기) 직전에 `HEAD` 와 증거를 다시 보고 달라졌으면 "다시 돌려라" 다. 창 줄 끝에 판정한 sha 가 남는다(`judged at HEAD <sha12>`). 그리고 7.5.2.1 이 `_bundle_text` 를 다시 쓰며 잃은 `is_file()` 거름(FIFO 에 게이트가 멎음)을 `_read_regular` 한 자리로 되찾았고, **그 수리 안에서 내가 만든 회귀(번들 안 폴더 → `TypeError` traceback)를 BTM 을 쓰다 내가 찾아 고쳤다**. A/B **126/126 SAME** · 변이 **95/95** · 시험 254 → **273** · a122 rc=0. 비용: 끝의 재확인이 증거를 한 번 더 읽어 `ast.json` 읽기 5,803 → 9,072. 7.5.2.2 의 수리 트리는 **아직 독립 재리뷰 전**이다(커밋 뒤 돌린다) — `review.md` `## VERIFY — task 7.5.2.2`. 사람 결정 **7.5.3 · 7.5.4 · 7.5.5** 대기.

**그전 한 줄(2026-09-20): 7.5.2 수리 트리를 gstack 으로 다시 재리뷰했고(출처 여덟) 그 결과를 7.5.2.1 로 고쳤다.** 7.5.2 는 `ast.json` 바이트만 묶고 "묶었다" 고 적었는데 재리뷰가 셋을 재현했다(`HEAD` 는 표본뿐이라 가지 전환 한 번에 rc 0 · 수락 뒤 디렉터리 삭제 · 목록에서 빠진 `ast.json`). 이번엔 **입력 목록을 AST 로 먼저 세고**(`7521_inputs.py`), 명령이 `HEAD` 를 sha 로 **한 번** 풀고 증거를 **한 번** 읽어 모두가 그 값을 쓰게 했다. `main()` 출력 A/B 126/126 SAME · 변이 76/76 · 상징 `HEAD` 읽기 10 → 1. 7.5.2.1 의 수리 트리는 **아직 독립 재리뷰 전**이다(커밋 뒤 돌린다) — `review.md` `## VERIFY — task 7.5.2.1`. 사람 결정 **7.5.3** 은 그대로 대기.

**그전 한 줄(2026-09-19 두 번째 갱신): 7.5.1 수리 트리를 gstack 으로 재리뷰했고 그 결과를 7.5.2 로 고쳤다.** 증거를 읽는 자리가 `_read_evidence` 하나가 되어 착지 판정과 그 뒤의 판정이 같은 바이트를 본다(Codex 가 재현한 "지문과 판정 목록이 따로 읽힌다" · 수락 뒤 창 · ABA 가 닫혔다). `check()` A/B 126/126 SAME · 변이 52/52. **사람 결정 새로 하나: 7.5.3** — 가드 7 이 고정 소스를 경로로만 비교해서 순수 `git mv` 가 V1 을 다시 연다(7.2.2 부터, 두 출처 재현). 7.5.2 의 수리 트리는 **아직 독립 재리뷰 전**이다 — `review.md` `## VERIFY — task 7.5.2`.

**그전 한 줄(2026-09-19): 1.4 독립 리뷰와 gstack 리뷰가 둘 다 끝났고, gstack 리뷰가 찾은 permissive
결함을 7.5.1 로 고쳤다.** 7.5 의 blob 읽기가 "git 이 못 돌았다" 를 "없다" 와 같게 다뤄서, 가드 하나가
**편집 전 커밋을 착지로 기록했다**(두 출처가 독립적으로 찾았고 둘 다 재현했다). 이제 결함이다.
`check()` 전체 A/B 126/126 SAME · 변이 31/31. 자세한 것은 `review.md` 의 `## VERIFY — task 7.5.1`.

**그전 한 줄(2026-09-18): 섹션 7 이 전부 닫혔다.** 남은 일은 **6.4 한 묶음**(그 안에 사람 결정 하나)과
**종결 3종**(1.4 · 3.2.3 · 4.5)이다. 6.1 은 자식 여덟이 다 닫혔고 부모 체크박스만 남았다.

---

## 0. 상태 (사실만)

- 브랜치 `feat/a112-four-family-runtime`.
- **푸시 상태(2026-09-20)**: 원격 브랜치 tip 은 `fc35eb2d` 의 **부모** `e9f905bd` 까지 올라가 있고, 그 위에 이 change 의 미푸시 커밋이 쌓인다(`fc35eb2d` 7.5.2 · `908a8a36` 7.5.2.1 · 그 다음 7.5.2.2). 푸시는 사람이 요청할 때만 한다 — `git rev-list --left-right --count origin/<브랜치>...HEAD` 로 확인할 것.
- **이 워크트리는 병행 세션이 쓴다.** 작업 전에 `git log --oneline -3` 으로 tip 이 움직였는지
  확인하고, 내 것이 아닌 변경은 커밋에 담지 말 것
  ([[tossos-parallel-session-gate-contention]]). 2026-09-18 한 시간 안에 실제로 두 번 겹쳤다.
- 마지막 검증(7.5.2.3, 커밋 전 실행 — 수치는 review.md `## VERIFY — task 7.5.2.3`): `check_analysis --change a122` **rc=0** ·
  `openspec validate --all --strict --no-interactive` **58/58** · `make sdd-test`(373) · `make lint` · `make test-seams` ·
  `test_check_analysis` **298**. `openspec` 는 **저장소에 깔린 1.4.1** 로 돌릴 것 — `npx @fission-ai/openspec@latest`
  는 더 엄해서 이 저장소에서 18 건이 빨갛다(내용 문제가 아니라 판본 차이다, 2026-09-20 실측).
- **긴 하네스는 전경에서 창 단위로 돌릴 것(2026-09-20 실측).** 이 환경은 배경으로 넘긴 프로세스를 오래 살려 주지
  않고(`nohup` 도 마찬가지), 그렇다고 조용히 죽지도 않는다 — 변이 하네스를 배경에 걸어 두고 전경 창을 같이
  돌렸다가 **두 판이 한 사본을 공유**해 서로의 변이를 기준으로 삼았다. 지금은 사본이 프로세스별이고 원본과 같은지
  단언하지만(`75_mut.py`), 규칙은 **한 번에 한 판**이다. 스위트 한 판 ≈ 66s 라 변이 6 개가 한 창(10분)에 들어간다.
  A/B 는 `7521_main_ab.py <before-sha> 480` 로 이어 달리면 창 9 번에 126 건이 끝난다 — 이어 달리기 기록은
  **워킹트리 상태**에 묶이므로 A/B 가 도는 동안 추적 파일을 고치지 말 것(특히 `review.md` — 게이트가 읽는다).
- **기계 상태 주의(2026-09-20)**: 루트 파일시스템이 100% 찼던 적이 있다 — `~/.cache/go-build` 가 203GB 였다.
  게이트는 change 마다 `go run` 으로 Go 함수를 뽑으므로 **A/B 전수 한 판이 그 캐시를 크게 키운다**. 비우면 되고
  (`go clean -cache`) 잃는 것은 다음 빌드 시간뿐이다. 전수 하네스를 돌리기 전에 `df -h /` 를 볼 것.
  `TMPDIR` 을 `/mnt/D` 로 돌리지 말 것 — fuseblk 마운트라 권한 · FIFO 픽스처가 깨져 시험 22개가 실패한다.

## 1. 무엇을 읽나 (순서대로)

1. 이 문서.
2. `tasks.md` — 열린 줄만 본다. 닫힌 줄의 본문은 **그 task 가 실제로 한 일**이 적혀 있다.
3. `review.md` 의 **뒤에서부터**. 절이 앞 절을 정정하므로 diff 해서 상태를 재구성하지 말 것.
   가장 최근 셋: `## MEASURE · Pre-Edit Gate — task 7.5` · `## VERIFY — task 7.5` ·
   `## VERIFY — task 7.9`.
4. `analysis/harness/README.md` — 측정·변이를 재현하는 법.
5. `analysis/python-function-logic/` — 편집한 함수마다 `ast.before-<task>.json` /
   `ast.after-<task>.json` 과 map 둘.

## 2. 남은 일

### 6.1 — 부모 체크박스만 (작다)

자식 여덟(6.1.1 · 6.1.2 · 6.1.2.1~6.1.2.6)이 전부 `[x]` 다. 부모가 적은 P0(저자가 만든 값으로
저자가 고른 값을 검증하는 순환)은 6.1.2 가 값을 도구로 옮기고 7.2.2 · 7.3.1 이 더 조였다.
**7.3 부모를 2026-09-18 에 닫은 것과 같은 모양이다.** 닫기 전에 부모 본문의 위조 표 셋이
지금도 빨간지 `6.1.2.4` 의 기록과 대조할 것.

### 6.4 — P2 묶음 (a)~(f), 각각 작고 독립

**(a) 가 사람 결정이다.** 착지를 선언하면 커밋 안 된 Go 편집이 5단계에 안 보인다. 이관
경로가 이미 쓰는 `git diff --quiet` 를 착지 경로에도 둘지. 넣기 전에 **거부할 정상 입력을
먼저 열거**할 것 — 게이트를 더러운 트리에서 돌리는 것은 개발 중 정상이다
([[fail-closed-must-name-what-it-rejects]]; 7.7 이 같은 자리에서 dirty 를 **거절 순서 맨 뒤**로
옮긴 이유도 이것이다).

(b)~(f) 는 결정이 필요 없다: `base-commit.txt` 를 워킹트리에서 읽고 40-hex 검사가 없다 ·
`ARCHIVED_CHANGE` 의 `\d` 가 유니코드 숫자를 먹는데 `gate.sh` 의 `[0-9]` 는 안 먹는다(`re.ASCII`
한 글자) · `check` 의 `except ValueError` 가 `archive holds N copies` 를 삼킨다 · `gate.sh:110`
주석과 `test_check_analysis.py` docstring 이 지워진 코드를 현재형으로 인용한다 · 규칙이 두 집에 산다.

### 종결 3종

- **1.4** proposal-freeze 적대 리뷰 + gstack 리뷰 기록. (§6 로트 리뷰는 `## VERIFY — §6 로트
  독립 리뷰` 에 있고, 그 뒤 7 절 전체가 아직 독립 리뷰를 안 받았다.)
- **3.2.3** 인용된 테스트 이름의 탐색 범위 결정.
- **4.5** PM 동기화 후 `make gate CHANGE=a122-…`. **순서는 체크 → tracker 재생성 → sdd-sync →
  gate → archive → Story 경로**다.

### 5 절은 작업이 아니다

5.1~5.7 은 이 change 가 **열어 두는 한계 선언**이다. 구현할 것이 없고, 아카이브할 때 같이 닫는다.

## 3. 사람이 정해야 하는 것

1. **6.4(a)** — 착지 경로에 더러운 트리 거절을 둘지. (위 참조)
2. **푸시** — `e34e9e2c`(이 change)와 `1764c833`(다른 세션)이 아직 안 올라갔다.
   브랜치를 밀면 둘 다 간다.

## 4. 이어받는 세션이 밟기 쉬운 지뢰

- **`make lint` 은 Python 을 안 본다** (`go vet` 둘뿐). 이 change 가 만지는 것은 거의 전부
  Python 이다 — 판정은 `test_check_analysis`(182)와 `analysis/harness/75_mut.py` 뿐이다
  ([[missing-tool-reports-clean]]).
- **`make gate` 는 change **완료** 게이트라 진행 중 로트엔 못 쓴다.** 로트 검증은
  `lint` · `test` · `test-seams` · `sdd-test` · `check_analysis --change a122` ·
  `openspec validate --all --strict`.
- **Python 내부를 고치면 FLM 이 먼저다.** 게이트가 Go 만 요구해서 7.2.1 · 7.3.1 · 7.4 가
  **세 task 연속 조용히 빠졌고** 7.6 이 소급해 메웠다. `analysis/python-function-logic/enumerate.py`
  로 편집 **전** AST 를 먼저 뽑을 것 ([[flm-before-claiming-not-before-editing]]).
- **`_landing_refusal` 은 순서가 못이다.** 가드를 옮기면 뒤의 가드들이 못에서 빠진다
  ([[a-new-guard-unpins-the-guards-behind-it]]). 편집 전후 AST 의 분기·반환 수가 같은지 반드시 대조할 것.
- **변이가 살아남으면 그것이 발견이다.** 7.5 에서 첫 판 생존 넷은 전부 "시험이 그 갈래에 안
  닿았다"였다. "동등 변이"로 넘기기 전에 **무엇이 덮고 있는지를 변이로 증명**할 것 — 7.5 의
  T6 은 T18(사전 채움 삭제 → 59개 빨강)로 증명했다 ([[surviving-mutant-may-mean-accidental-safety]]).
- **`75_ab.py` 의 비교 기준을 `HEAD` 로 바꾸지 말 것.** 수리가 랜딩한 뒤엔 대조군이 오염된다.

## 5. 7.5 가 남긴 성능 사실 (다시 재지 말 것, 필요하면 하네스로 재현)

전수 A/B **SAME 116 · DIFFERENT 0**, 합계 **2023.0s → 162.1s (12.5×)**, 최대 36.9×.
비용은 **아무 후보도 안 받는 walk** 에만 있었고 spawn 의 97% 가 blob fetch 였다.
남은 병목은 없다 — 프로파일에서 `compute_landing` 이 a071 3.69s · spawn 697 이다.
`--ancestry-path`(후보 **집합**을 바꾼다)와 조기 종료(거절 문장이 고칠 자리를 **전부** 대야 한다)는
**의도적으로 안 했고** 사유는 `## VERIFY — task 7.5` 의 "안 한 것과 사유" 에 있다.
