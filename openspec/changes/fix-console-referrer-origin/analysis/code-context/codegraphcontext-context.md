# CodeGraphContext Supporting Context

- Index refreshed with `make sdd-sync` on 2026-07-31.
- Global report generated to `/tmp/tossos-referrer-cgc.md`.
- The report identified `internal/console` as a high-connectivity area through
  shared console helpers but did not surface `remoteRuntime.security` or the
  response-header literal as a focused node.
- This broad supporting result does not authorize a wider edit. CodeGraph and
  the current HEAD provide the decisive symbol and branch evidence.
- GBrain was advisory-busy because the existing `gbrain serve` process owns the
  project data home; file memory returned no prior matching episode.
