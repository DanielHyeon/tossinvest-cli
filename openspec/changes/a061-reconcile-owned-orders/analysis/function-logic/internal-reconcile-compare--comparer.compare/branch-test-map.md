# Branch Test Map: `Comparer.Compare`

Source: `internal/reconcile/compare.go`

| Branch | Condition | Verification |
| --- | --- | --- |
| B1 | if at `internal/reconcile/compare.go:362` | Focused regressions and the full/race suites verify this condition and its error path. |
| B2 | range at `internal/reconcile/compare.go:368` | Focused regressions and the full/race suites verify this condition and its error path. |
| B3 | range at `internal/reconcile/compare.go:374` | Focused regressions and the full/race suites verify this condition and its error path. |
| B4 | if at `internal/reconcile/compare.go:375` | Focused regressions and the full/race suites verify this condition and its error path. |
| B5 | range at `internal/reconcile/compare.go:380` | Focused regressions and the full/race suites verify this condition and its error path. |
| B6 | if at `internal/reconcile/compare.go:382` | Focused regressions and the full/race suites verify this condition and its error path. |
| B7 | range at `internal/reconcile/compare.go:389` | Focused regressions and the full/race suites verify this condition and its error path. |
| B8 | if at `internal/reconcile/compare.go:394` | Focused regressions and the full/race suites verify this condition and its error path. |
| B9 | if at `internal/reconcile/compare.go:397` | Focused regressions and the full/race suites verify this condition and its error path. |
| B10 | switch at `internal/reconcile/compare.go:403` | Focused regressions and the full/race suites verify this condition and its error path. |
| B11 | case at `internal/reconcile/compare.go:404` | Focused regressions and the full/race suites verify this condition and its error path. |
| B12 | case at `internal/reconcile/compare.go:406` | Focused regressions and the full/race suites verify this condition and its error path. |
| B13 | case at `internal/reconcile/compare.go:412` | Focused regressions and the full/race suites verify this condition and its error path. |
| B14 | case at `internal/reconcile/compare.go:416` | Focused regressions and the full/race suites verify this condition and its error path. |
| B15 | range at `internal/reconcile/compare.go:426` | Focused regressions and the full/race suites verify this condition and its error path. |
| B16 | range at `internal/reconcile/compare.go:427` | Focused regressions and the full/race suites verify this condition and its error path. |
| B17 | if at `internal/reconcile/compare.go:429` | Focused regressions and the full/race suites verify this condition and its error path. |
| B18 | range at `internal/reconcile/compare.go:442` | Focused regressions and the full/race suites verify this condition and its error path. |
| B19 | if at `internal/reconcile/compare.go:443` | Focused regressions and the full/race suites verify this condition and its error path. |
| B20 | if at `internal/reconcile/compare.go:447` | Focused regressions and the full/race suites verify this condition and its error path. |
| B21 | range at `internal/reconcile/compare.go:454` | Focused regressions and the full/race suites verify this condition and its error path. |
| B22 | if at `internal/reconcile/compare.go:455` | Focused regressions and the full/race suites verify this condition and its error path. |
| B23 | range at `internal/reconcile/compare.go:463` | Focused regressions and the full/race suites verify this condition and its error path. |
| B24 | if at `internal/reconcile/compare.go:464` | Focused regressions and the full/race suites verify this condition and its error path. |
| B25 | if at `internal/reconcile/compare.go:469` | Focused regressions and the full/race suites verify this condition and its error path. |

Coverage includes external-order exclusion, scoped snapshot coexistence, detector reuse, incomplete broker evidence, scoped lineage, durable reconcile authority, recovery refusal/release, reservation decision binding, and nonce retention.
