# Branch Test Map: `Detector.collect`

Source: `internal/filldetect/detect.go`

| Branch | Condition | Verification |
| --- | --- | --- |
| B1 | if at `internal/filldetect/detect.go:365` | Focused regressions and the full/race suites verify this condition and its error path. |
| B2 | if at `internal/filldetect/detect.go:371` | Focused regressions and the full/race suites verify this condition and its error path. |
| B3 | if at `internal/filldetect/detect.go:375` | Focused regressions and the full/race suites verify this condition and its error path. |
| B4 | range at `internal/filldetect/detect.go:382` | Focused regressions and the full/race suites verify this condition and its error path. |
| B5 | if at `internal/filldetect/detect.go:384` | Focused regressions and the full/race suites verify this condition and its error path. |
| B6 | if at `internal/filldetect/detect.go:387` | Focused regressions and the full/race suites verify this condition and its error path. |
| B7 | range at `internal/filldetect/detect.go:399` | Focused regressions and the full/race suites verify this condition and its error path. |
| B8 | if at `internal/filldetect/detect.go:401` | Focused regressions and the full/race suites verify this condition and its error path. |
| B9 | if at `internal/filldetect/detect.go:409` | Focused regressions and the full/race suites verify this condition and its error path. |
| B10 | else at `internal/filldetect/detect.go:414` | Focused regressions and the full/race suites verify this condition and its error path. |
| B11 | if at `internal/filldetect/detect.go:410` | Focused regressions and the full/race suites verify this condition and its error path. |
| B12 | if at `internal/filldetect/detect.go:414` | Focused regressions and the full/race suites verify this condition and its error path. |
| B13 | range at `internal/filldetect/detect.go:423` | Focused regressions and the full/race suites verify this condition and its error path. |
| B14 | if at `internal/filldetect/detect.go:424` | Focused regressions and the full/race suites verify this condition and its error path. |
| B15 | if at `internal/filldetect/detect.go:429` | Focused regressions and the full/race suites verify this condition and its error path. |
| B16 | if at `internal/filldetect/detect.go:434` | Focused regressions and the full/race suites verify this condition and its error path. |
| B17 | if at `internal/filldetect/detect.go:440` | Focused regressions and the full/race suites verify this condition and its error path. |
| B18 | if at `internal/filldetect/detect.go:445` | Focused regressions and the full/race suites verify this condition and its error path. |
| B19 | if at `internal/filldetect/detect.go:449` | Focused regressions and the full/race suites verify this condition and its error path. |

Coverage includes external-order exclusion, scoped snapshot coexistence, detector reuse, incomplete broker evidence, scoped lineage, durable reconcile authority, recovery refusal/release, reservation decision binding, and nonce retention.
