# Branch Test Map: `Journal.RecordFill`

Source: `internal/journal/fills.go`

| Branch | Condition | Verification |
| --- | --- | --- |
| B1 | if at `internal/journal/fills.go:299` | Focused regressions and the full/race suites verify this condition and its error path. |
| B2 | if at `internal/journal/fills.go:303` | Focused regressions and the full/race suites verify this condition and its error path. |
| B3 | if at `internal/journal/fills.go:307` | Focused regressions and the full/race suites verify this condition and its error path. |
| B4 | if at `internal/journal/fills.go:319` | Focused regressions and the full/race suites verify this condition and its error path. |
| B5 | switch at `internal/journal/fills.go:326` | Focused regressions and the full/race suites verify this condition and its error path. |
| B6 | case at `internal/journal/fills.go:327` | Focused regressions and the full/race suites verify this condition and its error path. |
| B7 | case at `internal/journal/fills.go:328` | Focused regressions and the full/race suites verify this condition and its error path. |
| B8 | case at `internal/journal/fills.go:330` | Focused regressions and the full/race suites verify this condition and its error path. |
| B9 | if at `internal/journal/fills.go:336` | Focused regressions and the full/race suites verify this condition and its error path. |
| B10 | if at `internal/journal/fills.go:341` | Focused regressions and the full/race suites verify this condition and its error path. |
| B11 | if at `internal/journal/fills.go:348` | Focused regressions and the full/race suites verify this condition and its error path. |
| B12 | if at `internal/journal/fills.go:357` | Focused regressions and the full/race suites verify this condition and its error path. |
| B13 | if at `internal/journal/fills.go:361` | Focused regressions and the full/race suites verify this condition and its error path. |
| B14 | if at `internal/journal/fills.go:369` | Focused regressions and the full/race suites verify this condition and its error path. |
| B15 | if at `internal/journal/fills.go:376` | Focused regressions and the full/race suites verify this condition and its error path. |
| B16 | if at `internal/journal/fills.go:377` | Focused regressions and the full/race suites verify this condition and its error path. |
| B17 | if at `internal/journal/fills.go:393` | Focused regressions and the full/race suites verify this condition and its error path. |
| B18 | if at `internal/journal/fills.go:397` | Focused regressions and the full/race suites verify this condition and its error path. |
| B19 | if at `internal/journal/fills.go:398` | Focused regressions and the full/race suites verify this condition and its error path. |
| B20 | if at `internal/journal/fills.go:413` | Focused regressions and the full/race suites verify this condition and its error path. |
| B21 | if at `internal/journal/fills.go:414` | Focused regressions and the full/race suites verify this condition and its error path. |
| B22 | if at `internal/journal/fills.go:452` | Focused regressions and the full/race suites verify this condition and its error path. |
| B23 | if at `internal/journal/fills.go:460` | Focused regressions and the full/race suites verify this condition and its error path. |
| B24 | if at `internal/journal/fills.go:464` | Focused regressions and the full/race suites verify this condition and its error path. |
| B25 | if at `internal/journal/fills.go:476` | Focused regressions and the full/race suites verify this condition and its error path. |
| B26 | if at `internal/journal/fills.go:477` | Focused regressions and the full/race suites verify this condition and its error path. |
| B27 | if at `internal/journal/fills.go:482` | Focused regressions and the full/race suites verify this condition and its error path. |

Coverage includes external-order exclusion, scoped snapshot coexistence, detector reuse, incomplete broker evidence, scoped lineage, durable reconcile authority, recovery refusal/release, reservation decision binding, and nonce retention.
