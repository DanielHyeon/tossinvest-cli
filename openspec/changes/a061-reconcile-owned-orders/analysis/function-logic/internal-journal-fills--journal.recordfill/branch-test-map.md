# Branch Test Map: `Journal.RecordFill`

Source: `internal/journal/fills.go`

| Branch | Condition | Verification |
| --- | --- | --- |
| B1 | if at `internal/journal/fills.go:314` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B2 | if at `internal/journal/fills.go:318` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B3 | if at `internal/journal/fills.go:324` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B4 | if at `internal/journal/fills.go:328` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B5 | if at `internal/journal/fills.go:340` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B6 | switch at `internal/journal/fills.go:346` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B7 | case at `internal/journal/fills.go:347` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B8 | case at `internal/journal/fills.go:348` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B9 | case at `internal/journal/fills.go:350` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B10 | if at `internal/journal/fills.go:356` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B11 | if at `internal/journal/fills.go:364` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B12 | if at `internal/journal/fills.go:368` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B13 | if at `internal/journal/fills.go:374` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B14 | if at `internal/journal/fills.go:380` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B15 | if at `internal/journal/fills.go:385` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B16 | if at `internal/journal/fills.go:392` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B17 | if at `internal/journal/fills.go:401` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B18 | if at `internal/journal/fills.go:405` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B19 | if at `internal/journal/fills.go:413` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B20 | if at `internal/journal/fills.go:420` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B21 | if at `internal/journal/fills.go:421` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B22 | if at `internal/journal/fills.go:437` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B23 | if at `internal/journal/fills.go:441` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B24 | if at `internal/journal/fills.go:442` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B25 | if at `internal/journal/fills.go:464` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B26 | if at `internal/journal/fills.go:465` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B27 | if at `internal/journal/fills.go:504` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B28 | if at `internal/journal/fills.go:512` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B29 | if at `internal/journal/fills.go:516` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B30 | if at `internal/journal/fills.go:528` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B31 | if at `internal/journal/fills.go:529` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B32 | if at `internal/journal/fills.go:534` | Focused canonical/temporal ownership regressions plus full and race suites. |

Coverage includes external exclusion, identifier reuse, partial-scope rejection, v19 migration, confirmed temporal ownership, reservation recovery, and operator-release refusal/success.
