# Branch Test Map: `typeCheckPureApprovedCandidateBoundary`

| Branch | Scenario | Test | RED observed | GREEN observed |
|---|---|---|---|---|
| B1 | AST branch B1: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B2 | AST branch B2: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B3 | AST branch B3: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B4 | AST branch B4: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B5 | AST branch B5: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B6 | AST branch B6: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B7 | AST branch B7: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B8 | AST branch B8: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B9 | AST branch B9: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B10 | AST branch B10: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B11 | AST branch B11: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B12 | AST branch B12: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B13 | AST branch B13: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B14 | AST branch B14: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B15 | AST branch B15: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B16 | AST branch B16: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B17 | AST branch B17: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B18 | AST branch B18: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B19 | AST branch B19: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B20 | AST branch B20: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B21 | AST branch B21: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B22 | AST branch B22: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B23 | AST branch B23: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B24 | AST branch B24: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B25 | AST branch B25: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B26 | AST branch B26: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B27 | AST branch B27: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B28 | AST branch B28: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B29 | AST branch B29: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B30 | AST branch B30: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B31 | AST branch B31: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B32 | AST branch B32: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B33 | AST branch B33: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B34 | AST branch B34: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B35 | AST branch B35: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B36 | AST branch B36: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B37 | AST branch B37: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B38 | AST branch B38: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B39 | AST branch B39: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B40 | AST branch B40: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B41 | AST branch B41: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B42 | AST branch B42: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B43 | AST branch B43: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B44 | AST branch B44: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B45 | AST branch B45: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B46 | AST branch B46: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B47 | AST branch B47: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B48 | AST branch B48: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
| B49 | AST branch B49: walks imports, declarations, types, assignments, calls, and control-flow capabilities at the pure approval boundary | `TestApprovedCandidateConsumersStayInsidePureBoundaries` | existing guard baseline / a047 RED where current-life accessors were absent | yes |
