# Branch Test Map: `recordConfirmedFillOrderScope`

| Branch | Scenario | Test | RED observed | GREEN observed |
|---|---|---|---|---|
| B1 | branchless happy path — delegates with the caller's scope and quantity `"10"` | callers in `internal/journal` (per-test coverprofile run of all 875 tests at `648df8ef`: 0 failures) | n/a — test helper | PASS at `648df8ef` |
