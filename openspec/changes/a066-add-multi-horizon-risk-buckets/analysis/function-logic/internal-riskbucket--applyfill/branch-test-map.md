# Branch Test Map: `ApplyFill`

Measured at HEAD `648df8ef` with `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` (statement coverage; `covered` = the branch body ran at least once in the package suite, it does not say which test). Named tests are attributed per test with a single-test `-coverprofile` run. Harness: `analysis/harness/branch_coverage_rows.py`.

| Branch | Scenario | Test | RED observed | GREEN observed |
|---|---|---|---|---|
| B1 | if at 100:2 — `if event.FillID == "" \|\| event.OrderID == "" \|\| event.OrderQuantity == 0 \|\| event.NewCumulativeFill == 0 \|\| event.NewCumulativeFill > event.OrderQuantity \|\| event.ReservationPolicyDigest == "" \|\|`; then `return unchanged, result, refusal(RefusalFillEvidenceInconsistent, "fill_identity_or_quantity", nil)` (line last changed by `8b9821de`) | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | a066 commit `8b9821de` — no RED recorded for this row | NOT covered at `648df8ef` |
| B2 | if at 104:2 — `if err := validateFillBuckets(next.Buckets, event.ReservedMinor); err != nil {`; then `return unchanged, result, err` (line last changed by `8b9821de`) | `TestApplyFillCorruptPersistedUsageIsCrashPure`, `TestApplyFillErrorIsCrashPure` — per-test coverprofile | a066 commit `8b9821de` — no RED recorded for this row | covered at `648df8ef` |
| B3 | if at 107:2 — owner-wide multi-decision fill carries per-decision target HELD (Wave 1D) | `TestDispatchStrategyVerifiedClosesACKFillRaceInOneCompositeTxPairedKRUS`, `TestDispatchStrategyVerifiedReleasesTerminalACKWindowRemaindersPairedKRUS`, `TestRiskBucketAuthoritativePartialFillCommitsWithPositionAndCompletesActualEvidence` (+6 more) — per-test coverprofile | a066 commit `4a364caf` — no RED recorded for this row | covered at `648df8ef` |
| B4 | if at 108:3 — target HELD bucket count differs from reserved buckets: refuse, state unchanged | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | a066 commit `4a364caf` — no RED recorded for this row | NOT covered at `648df8ef` |
| B5 | range at 111:3 — iterate reserved buckets to validate every target HELD value | `TestDispatchStrategyVerifiedClosesACKFillRaceInOneCompositeTxPairedKRUS`, `TestDispatchStrategyVerifiedReleasesTerminalACKWindowRemaindersPairedKRUS`, `TestRiskBucketAuthoritativePartialFillCommitsWithPositionAndCompletesActualEvidence` (+6 more) — per-test coverprofile | a066 commit `4a364caf` — no RED recorded for this row | covered at `648df8ef` |
| B6 | if at 112:4 — unparseable target HELD: refuse, state unchanged | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | a066 commit `4a364caf` — no RED recorded for this row | NOT covered at `648df8ef` |
| B7 | if at 118:2 — immutable order_key (not the broker order id) keys the order watermark (Wave 1D) | `TestApplyFillUsesOrderKeyToKeepDecisionOrdersIndependent`, `TestDispatchStrategyVerifiedClosesACKFillRaceInOneCompositeTxPairedKRUS`, `TestDispatchStrategyVerifiedReleasesTerminalACKWindowRemaindersPairedKRUS` (+7 more) — per-test coverprofile | a066 commit `4a364caf` — no RED recorded for this row | covered at `648df8ef` |
| B8 | if at 122:2 — `if !exists {`; then `order = OrderFillState{` (line last changed by `8b9821de`) | `TestApplyFillLatchesEveryBucketOnOverageWithoutDroppingFill`, `TestApplyFillMalformedActualBecomesUnknownWithoutDroppingFill`, `TestApplyFillPartialThenReplacementCumulativeUsesProportionalDelta` (+7 more) — per-test coverprofile | a066 commit `8b9821de` — no RED recorded for this row | covered at `648df8ef` |
| B9 | else at 135:9 — `} else if order.OrderQuantity != event.OrderQuantity \|\| order.QuoteCurrency != event.QuoteCurrency \|\| order.BaseCurrency != event.BaseCurrency \|\| order.ReservationPolicyDigest != event.ReservationPolicyDigest \|\| !equalMinorMaps(order.ReservedMinor, event.ReservedMinor) {`; then `return unchanged, result, refusal(RefusalFillEvidenceInconsistent, "order_reservation", nil)` (line last changed by `8b9821de`) | `TestApplyFillPartialThenReplacementCumulativeUsesProportionalDelta`, `TestApplyFillRetryIsIdempotent`, `TestApplyFillUnknownActualLatchesThenCompletesMonotonically` (+9 more) — per-test coverprofile | a066 commit `8b9821de` — no RED recorded for this row | covered at `648df8ef` |
| B10 | range at 132:3 — `for key := range event.ReservedMinor {`; then `order.TransferredMinor[key] = "0"` (line last changed by `8b9821de`) | `TestApplyFillLatchesEveryBucketOnOverageWithoutDroppingFill`, `TestApplyFillMalformedActualBecomesUnknownWithoutDroppingFill`, `TestApplyFillPartialThenReplacementCumulativeUsesProportionalDelta` (+7 more) — per-test coverprofile | a066 commit `8b9821de` — no RED recorded for this row | covered at `648df8ef` |
| B11 | if at 135:9 — `} else if order.OrderQuantity != event.OrderQuantity \|\| order.QuoteCurrency != event.QuoteCurrency \|\| order.BaseCurrency != event.BaseCurrency \|\| order.ReservationPolicyDigest != event.ReservationPolicyDigest \|\| !equalMinorMaps(order.ReservedMinor, event.ReservedMinor) {`; then `return unchanged, result, refusal(RefusalFillEvidenceInconsistent, "order_reservation", nil)` (line last changed by `8b9821de`) | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | a066 commit `8b9821de` — no RED recorded for this row | NOT covered at `648df8ef` |
| B12 | if at 139:2 — `if record, seen := order.Fills[event.FillID]; seen {`; then `if record.CumulativeFill != event.NewCumulativeFill {` (line last changed by `8b9821de`) | `TestApplyFillRetryIsIdempotent`, `TestApplyFillUnknownActualLatchesThenCompletesMonotonically`, `TestRiskBucketAuthoritativePartialFillCommitsWithPositionAndCompletesActualEvidence` (+1 more) — per-test coverprofile | a066 commit `8b9821de` — no RED recorded for this row | covered at `648df8ef` |
| B13 | if at 140:3 — `if record.CumulativeFill != event.NewCumulativeFill {`; then `return unchanged, result, refusal(RefusalFillEvidenceInconsistent, "fill_identity_watermark", nil)` (line last changed by `8b9821de`) | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | a066 commit `8b9821de` — no RED recorded for this row | NOT covered at `648df8ef` |
| B14 | if at 143:3 — `if record.ActualKnown {`; then `result.Duplicate = true` (line last changed by `8b9821de`) | `TestApplyFillRetryIsIdempotent` — per-test coverprofile | a066 commit `8b9821de` — no RED recorded for this row | covered at `648df8ef` |
| B15 | if at 148:3 — `if !known {`; then `result.Duplicate = true` (line last changed by `8b9821de`) | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | a066 commit `8b9821de` — no RED recorded for this row | NOT covered at `648df8ef` |
| B16 | range at 152:3 — `for key, transferRaw := range record.TransferMinor {`; then `transfer, err := parseMinor(transferRaw, 0)` (line last changed by `8b9821de`) | `TestApplyFillUnknownActualLatchesThenCompletesMonotonically`, `TestRiskBucketAuthoritativePartialFillCommitsWithPositionAndCompletesActualEvidence`, `TestRiskBucketOwnerAggregateAccountsTwoDecisionOrdersAcrossRestart` — per-test coverprofile | a066 commit `8b9821de` — no RED recorded for this row | covered at `648df8ef` |
| B17 | if at 154:4 — `if err != nil {`; then `return unchanged, FillResult{}, refusal(RefusalFillEvidenceInconsistent, "record_transfer", err)` (line last changed by `8b9821de`) | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | a066 commit `8b9821de` — no RED recorded for this row | NOT covered at `648df8ef` |
| B18 | if at 158:4 — `if err != nil {`; then `return unchanged, FillResult{}, refusal(RefusalFillEvidenceInconsistent, "record_filled", err)` (line last changed by `8b9821de`) | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | a066 commit `8b9821de` — no RED recorded for this row | NOT covered at `648df8ef` |
| B19 | if at 162:4 — `if actualMinor.Cmp(target) > 0 {`; then `target.Set(actualMinor)` (line last changed by `8b9821de`) | `TestApplyFillUnknownActualLatchesThenCompletesMonotonically`, `TestRiskBucketAuthoritativePartialFillCommitsWithPositionAndCompletesActualEvidence` — per-test coverprofile | a066 commit `8b9821de` — no RED recorded for this row | covered at `648df8ef` |
| B20 | if at 165:4 — `if target.Cmp(previousFilled) > 0 {`; then `delta := new(big.Int).Sub(target, previousFilled)` (line last changed by `8b9821de`) | `TestApplyFillUnknownActualLatchesThenCompletesMonotonically`, `TestRiskBucketAuthoritativePartialFillCommitsWithPositionAndCompletesActualEvidence` — per-test coverprofile | a066 commit `8b9821de` — no RED recorded for this row | covered at `648df8ef` |
| B21 | if at 169:5 — `if err != nil {`; then `return unchanged, FillResult{}, refusal(RefusalFillEvidenceInconsistent, "filled_usage", err)` (line last changed by `8b9821de`) | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | a066 commit `8b9821de` — no RED recorded for this row | NOT covered at `648df8ef` |
| B22 | if at 173:5 — `if err != nil {`; then `return unchanged, FillResult{}, refusal(RefusalFillEvidenceInconsistent, "filled_usage_overflow", err)` (line last changed by `8b9821de`) | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | a066 commit `8b9821de` — no RED recorded for this row | NOT covered at `648df8ef` |
| B23 | if at 185:3 — `if err := recomputeOverageLatches(&next); err != nil {`; then `return unchanged, FillResult{}, err` (line last changed by `8b9821de`) | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | a066 commit `8b9821de` — no RED recorded for this row | NOT covered at `648df8ef` |
| B24 | if at 193:2 — `if event.NewCumulativeFill <= order.CumulativeFill {`; then `return unchanged, result, refusal(RefusalFillEvidenceInconsistent, "cumulative_fill", nil)` (line last changed by `8b9821de`) | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | a066 commit `8b9821de` — no RED recorded for this row | NOT covered at `648df8ef` |
| B25 | if at 206:2 — `if actualKnown {`; then `record.ActualEvidence = cloneActualFillEvidence(event.Actual)` (line last changed by `8b9821de`) | `TestApplyFillLatchesEveryBucketOnOverageWithoutDroppingFill`, `TestApplyFillPartialThenReplacementCumulativeUsesProportionalDelta`, `TestApplyFillRetryIsIdempotent` (+2 more) — per-test coverprofile | a066 commit `8b9821de` — no RED recorded for this row | covered at `648df8ef` |
| B26 | range at 209:2 — `for key, reservedRaw := range event.ReservedMinor {`; then `reserved, err := parseMinor(reservedRaw, 0)` (line last changed by `8b9821de`) | `TestApplyFillLatchesEveryBucketOnOverageWithoutDroppingFill`, `TestApplyFillMalformedActualBecomesUnknownWithoutDroppingFill`, `TestApplyFillPartialThenReplacementCumulativeUsesProportionalDelta` (+16 more) — per-test coverprofile | a066 commit `8b9821de` — no RED recorded for this row | covered at `648df8ef` |
| B27 | if at 211:3 — `if err != nil {`; then `return unchanged, FillResult{}, refusal(RefusalFillEvidenceInconsistent, "reserved_minor", err)` (line last changed by `8b9821de`) | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | a066 commit `8b9821de` — no RED recorded for this row | NOT covered at `648df8ef` |
| B28 | if at 216:3 — `if err != nil \|\| allocated.Cmp(previousTransferred) < 0 {`; then `return unchanged, FillResult{}, refusal(RefusalFillEvidenceInconsistent, "transferred_minor", err)` (line last changed by `8b9821de`) | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | a066 commit `8b9821de` — no RED recorded for this row | NOT covered at `648df8ef` |
| B29 | if at 222:3 — `if err != nil {`; then `return unchanged, FillResult{}, refusal(RefusalFillEvidenceInconsistent, "held_usage", err)` (line last changed by `8b9821de`) | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | a066 commit `8b9821de` — no RED recorded for this row | NOT covered at `648df8ef` |
| B30 | if at 226:3 — `if err != nil {`; then `return unchanged, FillResult{}, refusal(RefusalFillEvidenceInconsistent, "filled_usage", err)` (line last changed by `8b9821de`) | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | a066 commit `8b9821de` — no RED recorded for this row | NOT covered at `648df8ef` |
| B31 | if at 230:3 — target HELD present for this bucket | `TestDispatchStrategyVerifiedClosesACKFillRaceInOneCompositeTxPairedKRUS`, `TestDispatchStrategyVerifiedReleasesTerminalACKWindowRemaindersPairedKRUS`, `TestRiskBucketAuthoritativePartialFillCommitsWithPositionAndCompletesActualEvidence` (+6 more) — per-test coverprofile | a066 commit `4a364caf` — no RED recorded for this row | covered at `648df8ef` |
| B32 | if at 232:4 — unparseable target HELD at transfer time: refuse, state unchanged | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | a066 commit `4a364caf` — no RED recorded for this row | NOT covered at `648df8ef` |
| B33 | if at 235:4 — transfer exceeds the decision's own HELD: cap the deduction and latch RISK_OVERAGE instead of consuming another decision's HELD | `TestRiskBucketFillReplacementPredecessorLateFillCancelAndLateSuccessorAreConservative`, `TestRiskBucketLateFillCannotConsumeSiblingDecisionHeld` — per-test coverprofile | a066 commit `4a364caf` — no RED recorded for this row | covered at `648df8ef` |
| B34 | if at 241:3 — `if heldDeduction.Cmp(held) > 0 {`; then `heldDeduction.Set(held)` (line last changed by `8b9821de`) | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | a066 commit `8b9821de` — no RED recorded for this row | NOT covered at `648df8ef` |
| B35 | if at 248:3 — `if actualKnown && actualMinor.Cmp(filledDelta) > 0 {`; then `filledDelta.Set(actualMinor)` (line last changed by `8b9821de`) | `TestApplyFillLatchesEveryBucketOnOverageWithoutDroppingFill`, `TestApplyFillPartialThenReplacementCumulativeUsesProportionalDelta`, `TestApplyFillRetryIsIdempotent` (+2 more) — per-test coverprofile | a066 commit `8b9821de` — no RED recorded for this row | covered at `648df8ef` |
| B36 | if at 252:3 — `if err != nil {`; then `return unchanged, FillResult{}, refusal(RefusalFillEvidenceInconsistent, "filled_usage_overflow", err)` (line last changed by `8b9821de`) | `TestApplyFillRejectsStoredMinorOverflowWithoutPartialMutation` — per-test coverprofile | a066 commit `8b9821de` — no RED recorded for this row | covered at `648df8ef` |
| B37 | if at 256:3 — `if !actualKnown {`; then `latchUsage(&usage, LatchUnknownActualRisk)` (line last changed by `8b9821de`) | `TestApplyFillMalformedActualBecomesUnknownWithoutDroppingFill`, `TestApplyFillRejectsMismatchedActualFXCurrencyPairAsUnknown`, `TestApplyFillUnknownActualLatchesThenCompletesMonotonically` (+10 more) — per-test coverprofile | a066 commit `8b9821de` — no RED recorded for this row | covered at `648df8ef` |
| B38 | if at 268:2 — `if err := recomputeOverageLatches(&next); err != nil {`; then `return unchanged, FillResult{}, err` (line last changed by `8b9821de`) | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | a066 commit `8b9821de` — no RED recorded for this row | NOT covered at `648df8ef` |
