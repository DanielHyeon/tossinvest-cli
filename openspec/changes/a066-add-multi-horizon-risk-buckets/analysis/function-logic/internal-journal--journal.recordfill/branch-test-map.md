# Branch Test Map: `Journal.RecordFill`

Measured at HEAD `648df8ef` with `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` (statement coverage; `covered` = the branch body ran at least once in the package suite, it does not say which test). Named tests are attributed per test with a single-test `-coverprofile` run. Harness: `analysis/harness/branch_coverage_rows.py`.

| Branch | Scenario | Test | RED observed | GREEN observed |
|---|---|---|---|---|
| B1 | if at 314:2 — `if strings.TrimSpace(obs.OrderID) == "" {`; then `return FillResult{}, fmt.Errorf("%w: a fill snapshot needs an order id", ErrInvalidRequest)` (line last changed by `15b25b40`) | `TestBlankOrderIdIsStillRefused` — per-test coverprofile | n/a — branch line last changed by `15b25b40`, not an a066 commit | covered at `648df8ef` |
| B2 | if at 318:2 — `if !scope.complete() && !scope.legacyUnscoped() {`; then `return FillResult{}, fmt.Errorf(` (line last changed by `e3f8b391`) | `TestRecordFillRejectsPartialScopeWithoutOverwritingOrRepeatingDelta` — per-test coverprofile | n/a — branch line last changed by `e3f8b391`, not an a066 commit | covered at `648df8ef` |
| B3 | if at 324:2 — `if err != nil {`; then `return FillResult{}, fmt.Errorf("%w: filled quantity %q is not a decimal",` (line last changed by `15b25b40`) | `TestRecordFillRejectsAnUnreadableQuantity` — per-test coverprofile | n/a — branch line last changed by `15b25b40`, not an a066 commit | covered at `648df8ef` |
| B4 | if at 328:2 — `if math.IsNaN(filled) \|\| math.IsInf(filled, 0) {`; then `return FillResult{}, fmt.Errorf("%w: filled quantity %q is not finite",` (line last changed by `15b25b40`) | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | n/a — branch line last changed by `15b25b40`, not an a066 commit | NOT covered at `648df8ef` |
| B5 | if at 340:2 — `if err != nil {`; then `return res, fmt.Errorf("journal: starting the fill transaction for %s: %w", orderID, err)` (line last changed by `15b25b40`) | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | n/a — branch line last changed by `15b25b40`, not an a066 commit | NOT covered at `648df8ef` |
| B6 | switch at 346:2 — `switch {`; then `case errors.Is(err, ErrFillNotFound):` (line last changed by `15b25b40`) | `TestACorrectionMovesTheBasisAndNotTheQuantity`, `TestARefusedSnapshotLeavesTheProjectionAlone`, `TestAmountOnlyCorrectionIsDetected` (+30 more) — per-test coverprofile | n/a — branch line last changed by `15b25b40`, not an a066 commit | covered at `648df8ef` |
| B7 | case at 347:2 — `case err == nil:`; then `case errors.Is(err, ErrFillNotFound):` (line last changed by `15b25b40`) | `TestACorrectionMovesTheBasisAndNotTheQuantity`, `TestARefusedSnapshotLeavesTheProjectionAlone`, `TestAmountOnlyCorrectionIsDetected` (+30 more) — per-test coverprofile | n/a — branch line last changed by `15b25b40`, not an a066 commit | covered at `648df8ef` |
| B8 | case at 348:2 — `case errors.Is(err, ErrFillNotFound):`; then `prev = FillSnapshotRecord{}` (line last changed by `15b25b40`) | `TestA111CycleSequenceOrdersFrozenClockRefreshAndSurvivesRestart`, `TestA111EqualTimeOfficialEvidenceOutranksCycleButNotTheReverse`, `TestA111OfficialRefreshTemporalCASIsNoWriteForOlderOrAmbiguousEvidence` (+213 more) — per-test coverprofile | n/a — branch line last changed by `15b25b40`, not an a066 commit | covered at `648df8ef` |
| B9 | case at 350:2 — `default:`; then `return res, err` (line last changed by `15b25b40`) | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | n/a — branch line last changed by `15b25b40`, not an a066 commit | NOT covered at `648df8ef` |
| B10 | if at 356:2 — `if ownerErr != nil {`; then `return res, ownerErr` (line last changed by `c93f5f4a`) | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | n/a — branch line last changed by `c93f5f4a`, not an a066 commit | NOT covered at `648df8ef` |
| B11 | if at 364:2 — `if hadPrev && scope.complete() {`; then `if owners == 1 && prev.CommittedAt <= ownershipAt {` (line last changed by `c93f5f4a`) | `TestACorrectionMovesTheBasisAndNotTheQuantity`, `TestARefusedSnapshotLeavesTheProjectionAlone`, `TestAmountOnlyCorrectionIsDetected` (+30 more) — per-test coverprofile | n/a — branch line last changed by `c93f5f4a`, not an a066 commit | covered at `648df8ef` |
| B12 | if at 368:3 — `if owners == 1 && prev.CommittedAt <= ownershipAt {`; then `prev = FillSnapshotRecord{}` (line last changed by `c93f5f4a`) | `TestLateParentFillStartsANewSequenceAfterExternalEvidenceAndReplacement`, `TestPostOwnerFillStartsANewCumulativeSequenceAfterExternalEvidence` — per-test coverprofile | n/a — branch line last changed by `c93f5f4a`, not an a066 commit | covered at `648df8ef` |
| B13 | if at 374:2 — `if err != nil {`; then `return res, fmt.Errorf("journal: ordering fill evidence for %s: %w", orderID, err)` (line last changed by `c93f5f4a`) | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | n/a — branch line last changed by `c93f5f4a`, not an a066 commit | NOT covered at `648df8ef` |
| B14 | if at 380:2 — `if hadPrev {`; then `prevFilled, _ = strconv.ParseFloat(strings.TrimSpace(orZero(prev.FilledQuantity)), 64)` (line last changed by `15b25b40`) | `TestACorrectionMovesTheBasisAndNotTheQuantity`, `TestARefusedSnapshotLeavesTheProjectionAlone`, `TestAmountOnlyCorrectionIsDetected` (+28 more) — per-test coverprofile | n/a — branch line last changed by `15b25b40`, not an a066 commit | covered at `648df8ef` |
| B15 | if at 385:2 — `if refusal := classifyFillRefusal(obs, prev, hadPrev, filled, prevFilled); refusal != nil {`; then `res.FailClosed = true` (line last changed by `15b25b40`) | `TestARefusedSnapshotLeavesTheProjectionAlone`, `TestApplyHooksSkipRefusedAndNoOpSnapshots`, `TestCallerFailClosedIsRecordedWithoutAdvancing` (+7 more) — per-test coverprofile | n/a — branch line last changed by `15b25b40`, not an a066 commit | covered at `648df8ef` |
| B16 | if at 392:3 — `if err := markFillRefused(ctx, tx, scope, obs, refusal, now, hadPrev); err != nil {`; then `return res, err` (line last changed by `77d6dc60`) | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | n/a — branch line last changed by `77d6dc60`, not an a066 commit | NOT covered at `648df8ef` |
| B17 | if at 401:3 — `if err != nil {`; then `return res, err` (line last changed by `8c173ede`) | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | n/a — branch line last changed by `8c173ede`, not an a066 commit | NOT covered at `648df8ef` |
| B18 | if at 405:3 — `if err := tx.Commit(); err != nil {`; then `return res, fmt.Errorf("journal: committing the refused snapshot of %s: %w", orderID, err)` (line last changed by `15b25b40`) | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | n/a — branch line last changed by `15b25b40`, not an a066 commit | NOT covered at `648df8ef` |
| B19 | if at 413:2 — `if nearlyZero(delta, filled) {`; then `delta = 0` (line last changed by `15b25b40`) | `TestACorrectionMovesTheBasisAndNotTheQuantity`, `TestAmountOnlyCorrectionIsDetected`, `TestAnAmendmentKeepsOneInstance` (+22 more) — per-test coverprofile | n/a — branch line last changed by `15b25b40`, not an a066 commit | covered at `648df8ef` |
| B20 | if at 420:2 — `if hadPrev && delta == 0 && sameSnapshot(obs, prev) {`; then `if err := tx.Commit(); err != nil {` (line last changed by `15b25b40`) | `TestApplyHooksSkipRefusedAndNoOpSnapshots`, `TestDispatchStrategyVerifiedClosesACKFillRaceInOneCompositeTxPairedKRUS`, `TestDuplicateSurvivesRestart` (+4 more) — per-test coverprofile | n/a — branch line last changed by `15b25b40`, not an a066 commit | covered at `648df8ef` |
| B21 | if at 421:3 — `if err := tx.Commit(); err != nil {`; then `return res, fmt.Errorf("journal: closing the no-op snapshot of %s: %w", orderID, err)` (line last changed by `15b25b40`) | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | n/a — branch line last changed by `15b25b40`, not an a066 commit | NOT covered at `648df8ef` |
| B22 | if at 437:2 — `if err := upsertFillSnapshot(ctx, tx, scope, obs, now); err != nil {`; then `return res, fmt.Errorf("journal: recording the fill snapshot of %s: %w", orderID, err)` (line last changed by `77d6dc60`) | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | n/a — branch line last changed by `77d6dc60`, not an a066 commit | NOT covered at `648df8ef` |
| B23 | if at 441:2 — `if correction {`; then `if err := recordExecutionCorrection(ctx, tx, ExecutionCorrection{` (line last changed by `298c9443`) | `TestACorrectionMovesTheBasisAndNotTheQuantity`, `TestAmountOnlyCorrectionIsDetected`, `TestAveragePriceCorrectionIsRecordedWithoutAQuantityDelta` (+7 more) — per-test coverprofile | n/a — branch line last changed by `298c9443`, not an a066 commit | covered at `648df8ef` |
| B24 | if at 442:3 — `if err := recordExecutionCorrection(ctx, tx, ExecutionCorrection{`; then `return res, err` (line last changed by `298c9443`) | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | n/a — branch line last changed by `298c9443`, not an a066 commit | NOT covered at `648df8ef` |
| B25 | if at 464:2 — `if delta > 0 {`; then `if _, err := tx.ExecContext(ctx,` (line last changed by `15b25b40`) | `TestA111CycleSequenceOrdersFrozenClockRefreshAndSurvivesRestart`, `TestA111EqualTimeOfficialEvidenceOutranksCycleButNotTheReverse`, `TestA111OfficialRefreshTemporalCASIsNoWriteForOlderOrAmbiguousEvidence` (+204 more) — per-test coverprofile | n/a — branch line last changed by `15b25b40`, not an a066 commit | covered at `648df8ef` |
| B26 | if at 465:3 — `if _, err := tx.ExecContext(ctx,`; then `return res, fmt.Errorf("journal: appending the fill of %s: %w", orderID, err)` (line last changed by `15b25b40`) | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | n/a — branch line last changed by `15b25b40`, not an a066 commit | NOT covered at `648df8ef` |
| B27 | if at 504:2 — `if err != nil {`; then `return res, err` (line last changed by `f7bb9e03`) | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | n/a — branch line last changed by `f7bb9e03`, not an a066 commit | NOT covered at `648df8ef` |
| B28 | if at 512:2 — `if obs.Terminal && locallyOwned {`; then `released, err := releaseReservationsForOrder(ctx, tx, orderID, ownedOrigin.IntentID,` (line last changed by `f7bb9e03`) | `TestA111CycleSequenceOrdersFrozenClockRefreshAndSurvivesRestart`, `TestA111EqualTimeOfficialEvidenceOutranksCycleButNotTheReverse`, `TestA111OfficialRefreshTemporalCASIsNoWriteForOlderOrAmbiguousEvidence` (+140 more) — per-test coverprofile | n/a — branch line last changed by `f7bb9e03`, not an a066 commit | covered at `648df8ef` |
| B29 | if at 516:3 — `if err != nil {`; then `return res, err` (line last changed by `8c173ede`) | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | n/a — branch line last changed by `8c173ede`, not an a066 commit | NOT covered at `648df8ef` |
| B30 | if at 526:2 — a066 risk-bucket sidecar runs only for a locally owned fill, inside the same BEGIN IMMEDIATE fill transaction as the snapshot, Position and hooks (Wave 1C) | `TestA111CycleSequenceOrdersFrozenClockRefreshAndSurvivesRestart`, `TestA111EqualTimeOfficialEvidenceOutranksCycleButNotTheReverse`, `TestA111OfficialRefreshTemporalCASIsNoWriteForOlderOrAmbiguousEvidence` (+175 more) — per-test coverprofile | a066 commit `c60fee07` — no RED recorded for this row | covered at `648df8ef` |
| B31 | else at 536:9 — ownership is ambiguous: every registered, unreleased risk owner in scope gets a REPLAY_MISMATCH scope latch, unknown-actual flags and a FILL_UNACCOUNTED event; the broker fill itself is kept (Wave 1C/1D) | `TestAmountOnlyCorrectionIsDetected`, `TestAnExternalOrderIsIgnoredByTheExitApplier`, `TestAnOrderWithNoLocalIntentIsNotProjected` (+36 more) — per-test coverprofile | a066 commit `4a364caf` — no RED recorded for this row | covered at `648df8ef` |
| B32 | if at 527:3 — sidecar storage error aborts the whole fill transaction; semantic gaps latch entry inside the callee and return nil, so they never take this branch | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | a066 commit `c60fee07` — no RED recorded for this row | NOT covered at `648df8ef` |
| B33 | if at 530:3 — `if err := j.releaseTerminalRiskBucketOrderInTx(ctx, tx, applied); err != nil {`; then `return res, fmt.Errorf("journal: releasing terminal risk buckets for fill %s: %w", orderID, err)` (line last changed by `8022f578`) | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | n/a — branch line last changed by `8022f578`, not an a066 commit | NOT covered at `648df8ef` |
| B34 | if at 533:3 — `if err := applyWeeklyReservationLifecycleInTx(ctx, tx, applied); err != nil {`; then `return res, fmt.Errorf("journal: applying weekly reservation lifecycle for fill %s: %w", orderID, err)` (line last changed by `8022f578`) | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | n/a — branch line last changed by `8022f578`, not an a066 commit | NOT covered at `648df8ef` |
| B35 | if at 536:9 — latch write error aborts the whole fill transaction | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | a066 commit `4a364caf` — no RED recorded for this row | NOT covered at `648df8ef` |
| B36 | if at 547:2 — `if locallyOwned {`; then `if err := j.runApplyHooks(ctx, tx, applied); err != nil {` (line last changed by `f7bb9e03`) | `TestA111CycleSequenceOrdersFrozenClockRefreshAndSurvivesRestart`, `TestA111EqualTimeOfficialEvidenceOutranksCycleButNotTheReverse`, `TestA111OfficialRefreshTemporalCASIsNoWriteForOlderOrAmbiguousEvidence` (+175 more) — per-test coverprofile | n/a — branch line last changed by `f7bb9e03`, not an a066 commit | covered at `648df8ef` |
| B37 | if at 548:3 — `if err := j.runApplyHooks(ctx, tx, applied); err != nil {`; then `return res, err` (line last changed by `f7bb9e03`) | `TestAFailingApplyHookRollsBackTheFill`, `TestAFailingExitHookRollsBackTheProjectionToo`, `TestCampaignHookRunsBetweenProjectionAndExitAndRollsBackAtomically` (+2 more) — per-test coverprofile | n/a — branch line last changed by `f7bb9e03`, not an a066 commit | covered at `648df8ef` |
| B38 | if at 553:2 — `if err := tx.Commit(); err != nil {`; then `return res, fmt.Errorf("journal: committing the fill snapshot of %s: %w", orderID, err)` (line last changed by `15b25b40`) | package suite `per-test -coverpkg=./internal/journal,./internal/riskbucket runs of all 875 ./internal/journal tests + 42 ./internal/riskbucket tests` | n/a — branch line last changed by `15b25b40`, not an a066 commit | NOT covered at `648df8ef` |
