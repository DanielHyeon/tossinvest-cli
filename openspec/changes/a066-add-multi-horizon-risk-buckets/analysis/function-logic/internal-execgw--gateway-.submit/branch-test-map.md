# Branch Test Map: `Gateway.submit`

Measured at HEAD `648df8ef` with `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` (statement coverage; `covered` = the branch body ran at least once in the package suite, it does not say which test). Named tests are attributed per test with a single-test `-coverprofile` run. Harness: `analysis/harness/branch_coverage_rows.py`.

| Branch | Scenario | Test | RED observed | GREEN observed |
|---|---|---|---|---|
| B1 | if at 475:3 — `if strategyCrossedTransport \|\| returnedErr == nil \|\| preTransportClaimFinalizationAttempted {`; then `return` (line last changed by `8022f578`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `8022f578`, not an a066 commit | covered at `648df8ef` |
| B2 | if at 478:3 — `if preTransportClaim == nil && plan.strategy != nil && !preTransportClaimAttempted &&`; then `preTransportClaimAttempted = true` (line last changed by `8022f578`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `8022f578`, not an a066 commit | NOT covered at `648df8ef` |
| B3 | if at 482:4 — `if claimRejected != nil {`; then `returnedErr = errors.Join(returnedErr, claimRejected)` (line last changed by `8022f578`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `8022f578`, not an a066 commit | NOT covered at `648df8ef` |
| B4 | if at 488:3 — `if preTransportClaim == nil {`; then `return` (line last changed by `8022f578`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `8022f578`, not an a066 commit | covered at `648df8ef` |
| B5 | if at 492:3 — `if !errors.As(returnedErr, &rejected) {`; then `rejected = reject(ReasonStrategyDispatchFenced, "%v", returnedErr)` (line last changed by `8022f578`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `8022f578`, not an a066 commit | NOT covered at `648df8ef` |
| B6 | if at 506:2 — `if err != nil {`; then `return Outcome{Reason: ReasonInvalidRequest, Detail: err.Error()}, err` (line last changed by `d295555a`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `d295555a`, not an a066 commit | NOT covered at `648df8ef` |
| B7 | if at 519:2 — `if !g.claimSymbol(symbolKey) {`; then `return Outcome{Reason: ReasonSymbolInFlight, Detail: "another mutation on " + plan.symbol + " is in flight"},` (line last changed by `9dfbfd03`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `9dfbfd03`, not an a066 commit | covered at `648df8ef` |
| B8 | if at 525:2 — `if rejected, err := g.checkSymbolFree(ctx, plan); err != nil {`; then `return Outcome{Reason: ReasonInvalidRequest, Detail: err.Error()}, err` (line last changed by `9dfbfd03`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `9dfbfd03`, not an a066 commit | covered at `648df8ef` |
| B9 | else at 527:9 — `} else if rejected != nil {`; then `return Outcome{Reason: rejected.Reason, Detail: rejected.Detail}, rejected` (line last changed by `9dfbfd03`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `9dfbfd03`, not an a066 commit | covered at `648df8ef` |
| B10 | if at 527:9 — `} else if rejected != nil {`; then `return Outcome{Reason: rejected.Reason, Detail: rejected.Detail}, rejected` (line last changed by `9dfbfd03`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `9dfbfd03`, not an a066 commit | covered at `648df8ef` |
| B11 | if at 535:2 — `if rejected, err := g.checkDecisionUnspent(ctx, prep, decision); err != nil {`; then `return Outcome{Reason: ReasonInvalidRequest, Detail: err.Error()}, err` (line last changed by `122985d9`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `122985d9`, not an a066 commit | NOT covered at `648df8ef` |
| B12 | else at 537:9 — `} else if rejected != nil {`; then `return Outcome{Reason: rejected.Reason, Detail: rejected.Detail}, rejected` (line last changed by `658d4b0a`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `658d4b0a`, not an a066 commit | covered at `648df8ef` |
| B13 | if at 537:9 — `} else if rejected != nil {`; then `return Outcome{Reason: rejected.Reason, Detail: rejected.Detail}, rejected` (line last changed by `658d4b0a`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `658d4b0a`, not an a066 commit | covered at `648df8ef` |
| B14 | if at 545:2 — `if err != nil {`; then `return Outcome{IntentID: prep.Intent.ID, Reason: ReasonInvalidRequest, Detail: err.Error()},` (line last changed by `d295555a`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `d295555a`, not an a066 commit | covered at `648df8ef` |
| B15 | if at 552:3 — `if plan.strategy != nil && preTransportClaim == nil && !preTransportClaimAttempted {`; then `preTransportClaimAttempted = true` (line last changed by `8022f578`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `8022f578`, not an a066 commit | covered at `648df8ef` |
| B16 | if at 555:4 — `if claimRejected != nil {`; then `refused, refusalErr := g.refuseWithStrategySettlement(ctx, attempt, out, rejected, true)` (line last changed by `8022f578`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `8022f578`, not an a066 commit | NOT covered at `648df8ef` |
| B17 | if at 561:3 — `if plan.strategy != nil && preTransportClaim != nil {`; then `preTransportClaimFinalizationAttempted = true` (line last changed by `8022f578`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `8022f578`, not an a066 commit | covered at `648df8ef` |
| B18 | if at 564:4 — `if err := g.refusePreparedClaimedStrategyPreTransport(ctx, attempt, *preTransportClaim, rejected); err != nil {`; then `return out, errors.Join(rejected,` (line last changed by `8022f578`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `8022f578`, not an a066 commit | covered at `648df8ef` |
| B19 | if at 583:2 — `if rejected != nil {`; then `return refusePrepared(rejected)` (line last changed by `171739a4`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `171739a4`, not an a066 commit | covered at `648df8ef` |
| B20 | if at 588:2 — `if rejected := g.checkEntry(plan); rejected != nil {`; then `return refusePrepared(rejected)` (line last changed by `03a226bb`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `03a226bb`, not an a066 commit | covered at `648df8ef` |
| B21 | if at 594:2 — `if plan.preflight != nil {`; then `if rejected := plan.preflight(ctx); rejected != nil {` (line last changed by `0c332b80`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `0c332b80`, not an a066 commit | covered at `648df8ef` |
| B22 | if at 595:3 — `if rejected := plan.preflight(ctx); rejected != nil {`; then `return refusePrepared(rejected)` (line last changed by `0c332b80`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `0c332b80`, not an a066 commit | covered at `648df8ef` |
| B23 | if at 604:2 — `if loadRejected != nil {`; then `return refusePrepared(loadRejected)` (line last changed by `658d4b0a`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `658d4b0a`, not an a066 commit | covered at `648df8ef` |
| B24 | if at 607:2 — `if rejected := g.checkDecision(decision, ref, plan, g.clk.Now()); rejected != nil {`; then `return refusePrepared(rejected)` (line last changed by `658d4b0a`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `658d4b0a`, not an a066 commit | covered at `648df8ef` |
| B25 | if at 610:2 — `if plan.strategy != nil {`; then `preTransportClaimAttempted = true` (line last changed by `8022f578`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `8022f578`, not an a066 commit | covered at `648df8ef` |
| B26 | if at 613:3 — `if rejected != nil {`; then `return refusePrepared(rejected)` (line last changed by `8022f578`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `8022f578`, not an a066 commit | covered at `648df8ef` |
| B27 | if at 617:3 — `if rejected := g.checkClaimedStrategyLease(ctx, decision, prep.ClientOrderID, plan); rejected != nil {`; then `return refusePrepared(rejected)` (line last changed by `8022f578`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `8022f578`, not an a066 commit | NOT covered at `648df8ef` |
| B28 | if at 625:2 — a066 initial q_final/aggregate-hold revalidation refuses before any broker call (true branch entered by both named tests, per-test coverprofile) | `TestGatewayRefusesQFinalMarkedDecisionWithoutExactAdmissionBeforeBroker`, `TestRevokedDecisionIsRefusedAtTheLastMoment` | q_final checkpoint 2026-08-04: missing admission reached the broker path | covered at `648df8ef` |
| B29 | if at 636:3 — `if plan.strategy != nil {`; then `strategyCrossedTransport = true` (line last changed by `8022f578`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `8022f578`, not an a066 commit | covered at `648df8ef` |
| B30 | if at 648:3 — `if rejected != nil {`; then `return notSent(rejected)` (line last changed by `658d4b0a`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `658d4b0a`, not an a066 commit | NOT covered at `648df8ef` |
| B31 | if at 651:3 — `if rejected := g.checkDecision(fresh, ref, plan, now); rejected != nil {`; then `return notSent(rejected)` (line last changed by `658d4b0a`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `658d4b0a`, not an a066 commit | covered at `648df8ef` |
| B32 | if at 654:3 — `if fresh.ClientOrderID != prep.ClientOrderID {`; then `return notSent(reject(ReasonGuardianKeyMismatch,` (line last changed by `658d4b0a`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `658d4b0a`, not an a066 commit | NOT covered at `648df8ef` |
| B33 | if at 658:3 — `if _, rejected := g.checkProtection(dctx, plan, protectionCheckpoint); rejected != nil {`; then `return notSent(rejected)` (line last changed by `171739a4`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `171739a4`, not an a066 commit | covered at `648df8ef` |
| B34 | if at 665:3 — `if rejected := g.checkEntry(plan); rejected != nil {`; then `return notSent(rejected)` (line last changed by `8022f578`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `8022f578`, not an a066 commit | covered at `648df8ef` |
| B35 | if at 672:3 — `if plan.strategy == nil {`; then `if rejected := g.checkReservation(dctx, fresh); rejected != nil {` (line last changed by `8022f578`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `8022f578`, not an a066 commit | covered at `648df8ef` |
| B36 | if at 673:4 — a066 last-moment q_final barrier (ordinary plan): hold released after the initial check is refused before send tracking (true branch entered, per-test coverprofile) | `TestGatewayLastMomentQFinalBarrierRefusesHoldReleaseAfterInitialAdmissionCheck` | q_final checkpoint 2026-08-04: last-moment check missing | covered at `648df8ef` |
| B37 | if at 685:4 — `if err := dctx.Err(); err != nil {`; then `return err` (line last changed by `8022f578`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `8022f578`, not an a066 commit | NOT covered at `648df8ef` |
| B38 | if at 688:4 — `if plan.strategy != nil {`; then `if plan.strategy.finalAuthorityCheck == nil {` (line last changed by `8022f578`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `8022f578`, not an a066 commit | covered at `648df8ef` |
| B39 | if at 689:5 — `if plan.strategy.finalAuthorityCheck == nil {`; then `return errors.New("strategy scheduler authority has no final source-backed revalidator")` (line last changed by `8022f578`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `8022f578`, not an a066 commit | NOT covered at `648df8ef` |
| B40 | if at 692:5 — `if err := plan.strategy.finalAuthorityCheck(dctx); err != nil {`; then `return fmt.Errorf("strategy scheduler authority changed before transport: %w", err)` (line last changed by `8022f578`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `8022f578`, not an a066 commit | covered at `648df8ef` |
| B41 | if at 695:5 — `if rejected := g.checkReservation(dctx, fresh); rejected != nil {`; then `return rejected` (line last changed by `8022f578`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `8022f578`, not an a066 commit | NOT covered at `648df8ef` |
| B42 | if at 699:5 — `if requireTransport == nil {`; then `requireTransport = g.journal.RequireCurrentStrategyDispatchTransportAuthority` (line last changed by `8022f578`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `8022f578`, not an a066 commit | NOT covered at `648df8ef` |
| B43 | if at 702:5 — `if err := requireTransport(dctx, plan.strategy.lease); err != nil {`; then `return fmt.Errorf("strategy owner or submitting lease changed before transport: %w", err)` (line last changed by `8022f578`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `8022f578`, not an a066 commit | covered at `648df8ef` |
| B44 | if at 711:3 — `if plan.strategy != nil && !g.skipStrategyEntryABAForTest {`; then `if err := g.withStrategyEntryGateAuthority(dctx, plan.strategy.entryGateAuthority,` (line last changed by `8022f578`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `8022f578`, not an a066 commit | covered at `648df8ef` |
| B45 | else at 717:10 — `} else {`; then `if err := call(); err != nil {` (line last changed by `8022f578`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `8022f578`, not an a066 commit | covered at `648df8ef` |
| B46 | if at 712:4 — `if err := g.withStrategyEntryGateAuthority(dctx, plan.strategy.entryGateAuthority,`; then `return notSent(reject(ReasonStrategyDispatchFenced,` (line last changed by `8022f578`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `8022f578`, not an a066 commit | covered at `648df8ef` |
| B47 | if at 718:4 — `if err := call(); err != nil {`; then `return notSent(reject(ReasonStrategyDispatchFenced,` (line last changed by `8022f578`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `8022f578`, not an a066 commit | covered at `648df8ef` |
| B48 | if at 723:3 — `if tracker == nil {`; then `return notSent(reject(ReasonStrategyDispatchFenced, "strategy send tracker was not created"))` (line last changed by `8022f578`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `8022f578`, not an a066 commit | NOT covered at `648df8ef` |
| B49 | if at 729:2 — `if plan.strategy == nil {`; then `res, err = attempt.DispatchVerified(ctx, dispatch, g.roundTripFor(plan))` (line last changed by `8022f578`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `8022f578`, not an a066 commit | covered at `648df8ef` |
| B50 | else at 731:9 — `} else if g.dispatchStrategyVerified != nil {`; then `res, err = g.dispatchStrategyVerified(ctx, attempt, plan.strategy.lease, dispatch, g.roundTripFor(plan))` (line last changed by `8022f578`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `8022f578`, not an a066 commit | covered at `648df8ef` |
| B51 | if at 731:9 — `} else if g.dispatchStrategyVerified != nil {`; then `res, err = g.dispatchStrategyVerified(ctx, attempt, plan.strategy.lease, dispatch, g.roundTripFor(plan))` (line last changed by `8022f578`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `8022f578`, not an a066 commit | covered at `648df8ef` |
| B52 | else at 733:9 — `} else {`; then `res, err = attempt.DispatchStrategyVerified(ctx, plan.strategy.lease, dispatch, g.roundTripFor(plan))` (line last changed by `8022f578`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `8022f578`, not an a066 commit | NOT covered at `648df8ef` |
| B53 | if at 736:2 — `if err != nil {`; then `if errors.Is(err, journal.ErrNonceSpent) {` (line last changed by `d295555a`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `d295555a`, not an a066 commit | covered at `648df8ef` |
| B54 | if at 740:3 — `if errors.Is(err, journal.ErrNonceSpent) {`; then `return refusePrepared(reject(ReasonGuardianNonceReused,` (line last changed by `122985d9`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `122985d9`, not an a066 commit | NOT covered at `648df8ef` |
| B55 | if at 744:3 — `if plan.strategy != nil && !strategyCrossedTransport {`; then `return refusePrepared(strategySubmittingRefusal(err))` (line last changed by `8022f578`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `8022f578`, not an a066 commit | covered at `648df8ef` |
| B56 | if at 756:2 — `if res.Final == journal.StateConfirmed {`; then `return out, nil` (line last changed by `d295555a`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `d295555a`, not an a066 commit | covered at `648df8ef` |
| B57 | if at 759:2 — `if res.Err != nil {`; then `return out, res.Err` (line last changed by `d295555a`) | package suite `go test -count=1 -coverprofile ./internal/execgw (untagged + -tags tossos_testseams)` | n/a — branch line last changed by `d295555a`, not an a066 commit | covered at `648df8ef` |
