# Branch Test Map: `Journal.applyMigration`

| Branch | Scenario | Test | RED observed | GREEN observed |
|---|---|---|---|---|
| B1 | `if` at line 275: `if err != nil {`; invariant: missing/corrupt/alternate path is explicit | `TestMigrationV15TransactionBoundariesSurviveSIGKILL`, `TestFailedV15MigrationRollsBackColumnTriggerAndVersion`, migration package regressions | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B2 | `if` at line 280: `if _, err := tx.ExecContext(ctx, m.SQL); err != nil {`; invariant: missing/corrupt/alternate path is explicit | `TestMigrationV15TransactionBoundariesSurviveSIGKILL`, `TestFailedV15MigrationRollsBackColumnTriggerAndVersion`, migration package regressions | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B3 | `if` at line 284: `if _, err := tx.ExecContext(ctx,`; invariant: missing/corrupt/alternate path is explicit | `TestMigrationV15TransactionBoundariesSurviveSIGKILL`, `TestFailedV15MigrationRollsBackColumnTriggerAndVersion`, migration package regressions | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B4 | `if` at line 290: `if _, err := tx.ExecContext(ctx,`; invariant: missing/corrupt/alternate path is explicit | `TestMigrationV15TransactionBoundariesSurviveSIGKILL`, `TestFailedV15MigrationRollsBackColumnTriggerAndVersion`, migration package regressions | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B5 | `if` at line 295: `if _, err := tx.ExecContext(ctx,`; invariant: missing/corrupt/alternate path is explicit | `TestMigrationV15TransactionBoundariesSurviveSIGKILL`, `TestFailedV15MigrationRollsBackColumnTriggerAndVersion`, migration package regressions | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B6 | `if` at line 300: `if j.migrationHook != nil {`; invariant: missing/corrupt/alternate path is explicit | `TestMigrationV15TransactionBoundariesSurviveSIGKILL`, `TestFailedV15MigrationRollsBackColumnTriggerAndVersion`, migration package regressions | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B7 | `if` at line 304: `if _, err := tx.ExecContext(ctx, "PRAGMA user_version = "+strconv.Itoa(m.Version)); err != nil {`; invariant: missing/corrupt/alternate path is explicit | `TestMigrationV15TransactionBoundariesSurviveSIGKILL`, `TestFailedV15MigrationRollsBackColumnTriggerAndVersion`, migration package regressions | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B8 | `if` at line 307: `if j.migrationHook != nil {`; invariant: missing/corrupt/alternate path is explicit | `TestMigrationV15TransactionBoundariesSurviveSIGKILL`, `TestFailedV15MigrationRollsBackColumnTriggerAndVersion`, migration package regressions | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B9 | `if` at line 310: `if err := tx.Commit(); err != nil {`; invariant: missing/corrupt/alternate path is explicit | `TestMigrationV15TransactionBoundariesSurviveSIGKILL`, `TestFailedV15MigrationRollsBackColumnTriggerAndVersion`, migration package regressions | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
