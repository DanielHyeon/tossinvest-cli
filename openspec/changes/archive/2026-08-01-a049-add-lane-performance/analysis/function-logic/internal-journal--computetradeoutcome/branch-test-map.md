# Branch Test Map: `computeTradeOutcome`

| Branch | Scenario | Test | RED observed | GREEN observed |
|---|---|---|---|---|
| B1 | `if` at line 165: `if !model.Configured() {`; invariant: missing/corrupt/alternate path is explicit | `TestTheOutcomeIsFrozenInTheClosingTransaction`, `TestABackfillRecoversTheGapAndRefusesToRewriteIt`, `TestAnAnalyticsFailureDoesNotRollBackTheClose` | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B2 | `if` at line 180: `if err != nil {`; invariant: missing/corrupt/alternate path is explicit | `TestTheOutcomeIsFrozenInTheClosingTransaction`, `TestABackfillRecoversTheGapAndRefusesToRewriteIt`, `TestAnAnalyticsFailureDoesNotRollBackTheClose` | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B3 | `if` at line 183: `if !rows.Next() {`; invariant: missing/corrupt/alternate path is explicit | `TestTheOutcomeIsFrozenInTheClosingTransaction`, `TestABackfillRecoversTheGapAndRefusesToRewriteIt`, `TestAnAnalyticsFailureDoesNotRollBackTheClose` | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B4 | `if` at line 187: `if err := rows.Scan(&account, &market, &symbol, &decisionID, &adoptionID,`; invariant: missing/corrupt/alternate path is explicit | `TestTheOutcomeIsFrozenInTheClosingTransaction`, `TestABackfillRecoversTheGapAndRefusesToRewriteIt`, `TestAnAnalyticsFailureDoesNotRollBackTheClose` | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B5 | `if` at line 193: `if decisionID == "" && adoptionID == "" {`; invariant: missing/corrupt/alternate path is explicit | `TestTheOutcomeIsFrozenInTheClosingTransaction`, `TestABackfillRecoversTheGapAndRefusesToRewriteIt`, `TestAnAnalyticsFailureDoesNotRollBackTheClose` | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B6 | `if` at line 201: `if !ok {`; invariant: missing/corrupt/alternate path is explicit | `TestTheOutcomeIsFrozenInTheClosingTransaction`, `TestABackfillRecoversTheGapAndRefusesToRewriteIt`, `TestAnAnalyticsFailureDoesNotRollBackTheClose` | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B7 | `if` at line 206: `if adoptionID != "" {`; invariant: missing/corrupt/alternate path is explicit | `TestTheOutcomeIsFrozenInTheClosingTransaction`, `TestABackfillRecoversTheGapAndRefusesToRewriteIt`, `TestAnAnalyticsFailureDoesNotRollBackTheClose` | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B8 | `else` at line 208: `} else {`; invariant: missing/corrupt/alternate path is explicit | `TestTheOutcomeIsFrozenInTheClosingTransaction`, `TestABackfillRecoversTheGapAndRefusesToRewriteIt`, `TestAnAnalyticsFailureDoesNotRollBackTheClose` | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B9 | `if` at line 211: `if !ok \|\| buy.quantity.Sign() <= 0 {`; invariant: missing/corrupt/alternate path is explicit | `TestTheOutcomeIsFrozenInTheClosingTransaction`, `TestABackfillRecoversTheGapAndRefusesToRewriteIt`, `TestAnAnalyticsFailureDoesNotRollBackTheClose` | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B10 | `if` at line 217: `if err != nil {`; invariant: missing/corrupt/alternate path is explicit | `TestTheOutcomeIsFrozenInTheClosingTransaction`, `TestABackfillRecoversTheGapAndRefusesToRewriteIt`, `TestAnAnalyticsFailureDoesNotRollBackTheClose` | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B11 | `if` at line 221: `if err != nil {`; invariant: missing/corrupt/alternate path is explicit | `TestTheOutcomeIsFrozenInTheClosingTransaction`, `TestABackfillRecoversTheGapAndRefusesToRewriteIt`, `TestAnAnalyticsFailureDoesNotRollBackTheClose` | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B12 | `if` at line 234: `if denominator.Sign() != 0 {`; invariant: missing/corrupt/alternate path is explicit | `TestTheOutcomeIsFrozenInTheClosingTransaction`, `TestABackfillRecoversTheGapAndRefusesToRewriteIt`, `TestAnAnalyticsFailureDoesNotRollBackTheClose` | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
