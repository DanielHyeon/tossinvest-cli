# Branch Test Map: `TestCollectPersistsUnknownCostAsSQLNullWithoutInventingZero`

| Branch | Scenario | Test | RED observed | GREEN observed |
|---|---|---|---|---|
| B1 | `if` at line 82: `if _, err := store.Collect(context.Background(), trade, []Observation{{`; invariant: missing/corrupt/alternate path is explicit | `TestCollectPersistsUnknownCostAsSQLNullWithoutInventingZero` (this regression test) | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B2 | `if` at line 89: `if err := store.db.QueryRow(\`SELECT cost_total FROM performance_trades WHERE id=?\`, trade.ID).Scan(&cost); err != nil {`; invariant: missing/corrupt/alternate path is explicit | `TestCollectPersistsUnknownCostAsSQLNullWithoutInventingZero` (this regression test) | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B3 | `if` at line 92: `if cost.Valid {`; invariant: missing/corrupt/alternate path is explicit | `TestCollectPersistsUnknownCostAsSQLNullWithoutInventingZero` (this regression test) | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B4 | `if` at line 97: `if err := store.db.QueryRow(\`SELECT status,gross_value,cost_adjusted_value`; invariant: missing/corrupt/alternate path is explicit | `TestCollectPersistsUnknownCostAsSQLNullWithoutInventingZero` (this regression test) | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B5 | `if` at line 101: `if status != string(StatusNotMeasured) \|\| gross.String != "5" \|\| adjusted.Valid {`; invariant: missing/corrupt/alternate path is explicit | `TestCollectPersistsUnknownCostAsSQLNullWithoutInventingZero` (this regression test) | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B6 | `if` at line 107: `if err != nil {`; invariant: missing/corrupt/alternate path is explicit | `TestCollectPersistsUnknownCostAsSQLNullWithoutInventingZero` (this regression test) | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B7 | `if` at line 110: `if view.States.NotMeasured == 0 \|\| len(view.Aggregates) != 1 {`; invariant: missing/corrupt/alternate path is explicit | `TestCollectPersistsUnknownCostAsSQLNullWithoutInventingZero` (this regression test) | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B8 | `range` at line 113: `for _, metric := range view.Aggregates[0].Metrics {`; invariant: missing/corrupt/alternate path is explicit | `TestCollectPersistsUnknownCostAsSQLNullWithoutInventingZero` (this regression test) | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B9 | `if` at line 114: `if metric.Key == "markout_5" && (metric.Status != StatusNotMeasured \|\| metric.Value != "") {`; invariant: missing/corrupt/alternate path is explicit | `TestCollectPersistsUnknownCostAsSQLNullWithoutInventingZero` (this regression test) | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
