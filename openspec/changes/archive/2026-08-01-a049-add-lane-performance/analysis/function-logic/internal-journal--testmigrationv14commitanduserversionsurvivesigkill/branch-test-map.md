# Branch Test Map: `TestMigrationV14CommitAndUserVersionSurviveSIGKILL`

| Branch | Scenario | Test | RED observed | GREEN observed |
|---|---|---|---|---|
| B1 | `if` at line 16: `if os.Getenv(crashEnvMode) == crashModeMigrationV14AfterCommit {`; invariant: missing/corrupt/alternate path is explicit | `TestMigrationV14CommitAndUserVersionSurviveSIGKILL` (this regression test) | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B2 | `if` at line 18: `if version, err := j.SchemaVersion(context.Background()); err != nil \|\| version != 14 {`; invariant: missing/corrupt/alternate path is explicit | `TestMigrationV14CommitAndUserVersionSurviveSIGKILL` (this regression test) | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B3 | `if` at line 28: `if err := old.Close(); err != nil {`; invariant: missing/corrupt/alternate path is explicit | `TestMigrationV14CommitAndUserVersionSurviveSIGKILL` (this regression test) | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B4 | `if` at line 34: `if err != nil {`; invariant: missing/corrupt/alternate path is explicit | `TestMigrationV14CommitAndUserVersionSurviveSIGKILL` (this regression test) | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B5 | `if` at line 39: `if err := raw.QueryRow("PRAGMA user_version").Scan(&version); err != nil \|\| version != 14 {`; invariant: missing/corrupt/alternate path is explicit | `TestMigrationV14CommitAndUserVersionSurviveSIGKILL` (this regression test) | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B6 | `range` at line 42: `for _, name := range []string{"strategy_decision_lineage", "strategy_attempt_lineage", "strategy_execution_lineage", "strategy_attempt_refusals", "idx_strategy_execution_reverse"} `; invariant: missing/corrupt/alternate path is explicit | `TestMigrationV14CommitAndUserVersionSurviveSIGKILL` (this regression test) | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B7 | `if` at line 44: `if err := raw.QueryRow(\`SELECT count(*) FROM sqlite_master WHERE name=?\`, name).Scan(&count); err != nil \|\| count != 1 {`; invariant: missing/corrupt/alternate path is explicit | `TestMigrationV14CommitAndUserVersionSurviveSIGKILL` (this regression test) | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B8 | `if` at line 50: `if after := countRows(t, reopened.db, v8AllTables); !sameCounts(before, after) {`; invariant: missing/corrupt/alternate path is explicit | `TestMigrationV14CommitAndUserVersionSurviveSIGKILL` (this regression test) | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
