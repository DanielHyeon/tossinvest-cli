# Branch Test Map: `TestTheApplyAnswerNamesPairedAccountBaseFXRequirements`

> base 시점 이름은 TestTheApplyAnswerNamesTheMarketTheCurrencyCloses 였다. a073 커밋 `8022f578` 이 지금 이름으로
> 바꿨고, `ast.json` 은 `revision: base` 라 옛 이름을 그대로 둔다. 이 표가
> 가리키는 **덮는 테스트**는 현재 트리에 실제로 있는 이름이어야 한다.

| Branch | Scenario | Test | RED observed | GREEN observed |
|---|---|---|---|---|
| B1 | range branch at `internal/console/settings_limits_test.go:256` preserves its exact condition and failure path | named `TestTheApplyAnswerNamesPairedAccountBaseFXRequirements` regression plus affected-package suite | source/base behavior recorded | verification required |
| B2 | if branch at `internal/console/settings_limits_test.go:265` preserves its exact condition and failure path | named `TestTheApplyAnswerNamesPairedAccountBaseFXRequirements` regression plus affected-package suite | source/base behavior recorded | verification required |
| B3 | if branch at `internal/console/settings_limits_test.go:268` preserves its exact condition and failure path | named `TestTheApplyAnswerNamesPairedAccountBaseFXRequirements` regression plus affected-package suite | source/base behavior recorded | verification required |
