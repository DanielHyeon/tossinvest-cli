# Branch Test Map: `Journal.ReleaseReconciles`

Source: `internal/journal/reconcile_states.go`

| Branch | Condition | Verification |
| --- | --- | --- |
| B1 | if at `internal/journal/reconcile_states.go:309` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B2 | range at `internal/journal/reconcile_states.go:321` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B3 | switch at `internal/journal/reconcile_states.go:329` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B4 | case at `internal/journal/reconcile_states.go:330` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B5 | case at `internal/journal/reconcile_states.go:332` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B6 | case at `internal/journal/reconcile_states.go:334` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B7 | case at `internal/journal/reconcile_states.go:336` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B8 | if at `internal/journal/reconcile_states.go:340` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B9 | if at `internal/journal/reconcile_states.go:348` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B10 | range at `internal/journal/reconcile_states.go:354` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B11 | if at `internal/journal/reconcile_states.go:357` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B12 | if at `internal/journal/reconcile_states.go:361` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B13 | if at `internal/journal/reconcile_states.go:364` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B14 | range at `internal/journal/reconcile_states.go:374` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B15 | if at `internal/journal/reconcile_states.go:380` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B16 | if at `internal/journal/reconcile_states.go:383` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B17 | if at `internal/journal/reconcile_states.go:384` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B18 | if at `internal/journal/reconcile_states.go:393` | Focused canonical/temporal ownership regressions plus full and race suites. |

Coverage includes external exclusion, identifier reuse, partial-scope rejection, v19 migration, confirmed temporal ownership, reservation recovery, and operator-release refusal/success.
