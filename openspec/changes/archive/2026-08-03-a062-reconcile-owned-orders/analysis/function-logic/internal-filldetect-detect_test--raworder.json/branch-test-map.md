# Branch Test Map: `rawOrder.json`

Source: `internal/filldetect/detect_test.go`

| Branch | Condition | Verification |
| --- | --- | --- |
| B1 | if at `internal/filldetect/detect_test.go:255` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B2 | if at `internal/filldetect/detect_test.go:258` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B3 | if at `internal/filldetect/detect_test.go:261` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B4 | if at `internal/filldetect/detect_test.go:264` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B5 | if at `internal/filldetect/detect_test.go:267` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B6 | if at `internal/filldetect/detect_test.go:270` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B7 | if at `internal/filldetect/detect_test.go:282` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B8 | if at `internal/filldetect/detect_test.go:286` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B9 | switch at `internal/filldetect/detect_test.go:289` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B10 | case at `internal/filldetect/detect_test.go:290` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B11 | case at `internal/filldetect/detect_test.go:292` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B12 | if at `internal/filldetect/detect_test.go:295` | Focused canonical/temporal ownership regressions plus full and race suites. |

Coverage includes external exclusion, identifier reuse, partial-scope rejection, v19 migration, confirmed temporal ownership, reservation recovery, and operator-release refusal/success.
