# Branch Test Map: `Tracker.Observe`

Source: `internal/reconcile/mismatch.go`

| Branch | Condition | Verification |
| --- | --- | --- |
| B1 | if at `internal/reconcile/mismatch.go:366` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B2 | if at `internal/reconcile/mismatch.go:373` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B3 | else at `internal/reconcile/mismatch.go:396` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B4 | range at `internal/reconcile/mismatch.go:380` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B5 | if at `internal/reconcile/mismatch.go:381` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B6 | if at `internal/reconcile/mismatch.go:388` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B7 | range at `internal/reconcile/mismatch.go:398` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B8 | if at `internal/reconcile/mismatch.go:399` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B9 | if at `internal/reconcile/mismatch.go:405` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B10 | if at `internal/reconcile/mismatch.go:418` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B11 | range at `internal/reconcile/mismatch.go:440` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B12 | range at `internal/reconcile/mismatch.go:443` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B13 | if at `internal/reconcile/mismatch.go:444` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B14 | range at `internal/reconcile/mismatch.go:449` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B15 | range at `internal/reconcile/mismatch.go:453` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B16 | range at `internal/reconcile/mismatch.go:456` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B17 | if at `internal/reconcile/mismatch.go:462` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B18 | else at `internal/reconcile/mismatch.go:475` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B19 | range at `internal/reconcile/mismatch.go:467` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B20 | if at `internal/reconcile/mismatch.go:468` | Focused canonical/temporal ownership regressions plus full and race suites. |

Coverage includes external exclusion, identifier reuse, partial-scope rejection, v19 migration, confirmed temporal ownership, reservation recovery, and operator-release refusal/success.
