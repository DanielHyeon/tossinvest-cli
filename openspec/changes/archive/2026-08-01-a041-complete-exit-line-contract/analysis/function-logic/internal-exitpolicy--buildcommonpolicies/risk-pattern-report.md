# Risk Pattern Report: `internal/exitpolicy/common_policy.go`

| Rule | Location | Message |
|---|---|---|
| go-panic | `internal/exitpolicy/common_policy.go:74` | panic can bypass normal error and shutdown handling; map the recovery boundary. |
| go-panic | `internal/exitpolicy/common_policy.go:78` | panic can bypass normal error and shutdown handling; map the recovery boundary. |
| go-panic | `internal/exitpolicy/common_policy.go:167` | panic can bypass normal error and shutdown handling; map the recovery boundary. |

> Findings are review candidates, not automatic defect verdicts.
