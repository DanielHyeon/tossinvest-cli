# Branch Test Map: `runConsole`

> **측정 방법**: `go test -covermode=set -coverprofile ./cmd/tossctl`.
> 분기의 *조건*이 아니라 **분기 본문 블록의 실행 여부**를 측정했다(같은 줄에서 시작하는
> 커버리지 블록 중 시작 column이 가장 큰 것이 본문이다).
> 측정일 2026-08-11(편집 전·후), 2026-08-12(리뷰 수정 후). **세 번 다 측정했다.**
>
> | | 패키지 | `runConsole` | 분기 수 | return | source SHA-256 |
> |---|---|---|---|---|---|
> | 편집 전 | 49.6% | **0.0%** | 41 | 17 | `301ddbc20a70208a…` |
> | 편집 후 | 50.0% | **0.0%** | 44 | 18 | `b5f9cd6435d421c2…` |
> | 리뷰 수정 후 | 50.0% | **0.0%** | **44** | 21 | `08562e7541ddf0d8…` |

## 측정 결과: `runConsole`은 세 번 다 **0.0%** — 44개 분기가 전부 미실행이다

한 개도 실행되지 않았다. 이 패키지의 테스트는 `console.ListenAndServe`와 각 seam을 직접
구동하며, **콘솔 조립 함수 자체는 어느 테스트도 부르지 않는다.** 부르려면 실제 포트를 열고
브로커 client와 엔진 control plane을 세워야 하기 때문이다.

**이 사실이 a101의 편집 형태를 결정했다.** `runConsole` 안에 판정을 넣으면 그 판정은 기존
테스트가 닿지 못하는 곳에 놓인다. 그래서 `runConfiguredEngineAutostart`가 이미 쓴 형태를
따랐다 — 그 함수의 주석이 이유를 적어 뒀다: *"It receives functions so tests can prove every
branch without constructing a broker or spawning a process."*

**리뷰 수정이 이 규칙의 시험이었다.** 리뷰는 "부팅에서 이미 돌고 있는 서베이를 죽이면 안
된다"는 판정을 하나 추가하라고 요구했다. 그 판정을 여기 넣었다면 분기는 45가 되고 새 분기의
커버리지는 0.0%였을 것이다. `bootSurvey`에 넣었으므로 **분기 수가 44 그대로다** — 늘어난
것은 closure 안의 return 3개(18 → 21)뿐이고, 판정 자체는 100.0%로 측정된다.

| Branch | Kind | 본문 실행됨 | 블록 | 비고 |
|---|---|---|---|---|
| B1 | `:213` if | **no** | 213.16 |  |
| B2 | `:222` if | **no** | 222.16 |  |
| B3 | `:227` if | **no** | 227.16 |  |
| B4 | `:231` if | **no** | 231.16 |  |
| B5 | `:235` if | **no** | 235.16 |  |
| B6 | `:239` if | **no** | 239.16 |  |
| B7 | `:243` if | **no** | 243.16 |  |
| B8 | `:248` if | **no** | 248.16 |  |
| B9 | `:256` if | **no** | 256.23 |  |
| B10 | `:258` if | **no** | 258.17 |  |
| B11 | `:261` else | **no** | 261.9 |  |
| B12 | `:265` if | **no** | 265.17 |  |
| B13 | `:268` else | **no** | 268.9 |  |
| B14 | `:279` if | **no** | 279.54 |  |
| B15 | `:282` else | **no** | 282.8 |  |
| B16 | `:290` if | **no** | 290.42 |  |
| B17 | `:293` else | **no** | 293.59 | 같은 줄 블록 2개 |
| B18 | `:293` if | **no** | 293.59 | 같은 줄 블록 2개 |
| B19 | `:295` else | **no** | 295.8 |  |
| B20 | `:297` if | **no** | 297.18 |  |
| B21 | `:305` else | **no** | 305.9 |  |
| B22 | `:299` if | **no** | 299.59 |  |
| B23 | `:301` else | **no** | 301.10 |  |
| B24 | `:308` if | **no** | 308.22 |  |
| B25 | `:312` if | **no** | 312.19 | 같은 줄 블록 2개 |
| B26 | `:314` else | **no** | 314.10 |  |
| B27 | `:321` if | **no** | 321.21 |  |
| B28 | `:324` if | **no** | 324.18 |  |
| B29 | `:338` if | **no** | 338.23 |  |
| B30 | `:345` if | **no** | 345.26 | 같은 줄 블록 2개 |
| B31 | `:366` if | **no** | 366.21 |  |
| B32 | `:378` if | **no** | 378.86 | 같은 줄 블록 2개 |
| B33 | `:387` if | **no** | 387.21 |  |
| B34 | `:389` if | **no** | 389.60 |  |
| B35 | `:401` else | **no** | 401.49 | 같은 줄 블록 2개 |
| B36 | `:391` if | **no** | 391.22 |  |
| B37 | `:393` else | **no** | 393.10 |  |
| B38 | `:401` if | **no** | 401.49 | 같은 줄 블록 2개 |
| B39 | `:405` if | **no** | 405.64 |  |
| B40 | `:412` else | **no** | 412.49 | 같은 줄 블록 2개 |
| B41 | `:407` if | **no** | 407.22 |  |
| B42 | `:409` else | **no** | 409.10 |  |
| B43 | `:412` if | **no** | 412.49 | 같은 줄 블록 2개 |
| B44 | `:498` if | **no** | 498.23 |  |

**측정 결과: 44개 중 0개 실행.**

## a101이 추가한 분기 3개와, 추가하지 **않은** 분기 1개

추가한 셋은 `:357`(`soakBoot != nil`), `:378`(autostart `note != ""`), 그리고 `RestartSoak`
closure 안의 `soakBoot != nil`이다. 셋 다 nil seam 검사와 출력 판정이고, **셋 다 측정되지
않는 자리에 있다.** 그 자리가 결정하는 내용은 전부 측정되는 함수 쪽에 있다:

| 판정 | 어디서 측정되나 | 값 |
|---|---|---|
| 승인 없음 / 읽기 실패 / 배선 없음 / 기동 실패 / 빈 note | `runConfiguredSoakAutostart` | **100.0%** |
| **이미 돌고 있으면 두기 / 열거 실패는 "없음"이 아님** | **`bootSurvey`** | **100.0%** |
| 승인 영속과 그 실패 처리 | `rememberSoakApproval` | **100.0%** |
| 승인의 저장·audit·재조회 | `consoleSoakBoot` | Load 100.0%, Save 81.8% |

두 번째 줄이 리뷰가 만든 것이다. 여기(`runConsole`)에 넣었으면 45번째 분기가 되고
**0.0%였을 판정**이 100.0%로 측정된다.

## 측정으로 보장되지 않는 것 (숨기지 않는다)

Pre-Edit 통과 조건 넷 중 **(1) 엔진 autostart보다 뒤**와 **(2) 실패해도 return하지 않음**은
테스트로 고정하지 못했다. `runConsole`이 0.0%이므로 그 안의 순서와 early-return 부재를
관측할 수단이 없다. 둘은 코드 리뷰 조건으로만 존재하며 `pre-edit-gate.md`에 그렇게 적었다.

리뷰 수정이 조건 하나를 더 그 목록에 넣었다: **(5) 부팅 경로는 `PrepareSpawn`을 넘기지
않는다.** 두 closure(`startSurvey`, `bootSurveyIfAbsent`)의 차이는 `runConsole` 안에 있고,
어느 테스트도 그 함수를 부르지 않으므로 이것도 코드 리뷰 조건이다. `bootSurvey` 자체는
`start` seam을 인자로 받으므로 **무엇을 넘겼는지는 모르고 언제 부르는지만 안다.**

(3) 판정을 밖에 두기와 (4) 반환 계약 유지는 측정된다 — 위 표의 100.0% 네 줄이다.
