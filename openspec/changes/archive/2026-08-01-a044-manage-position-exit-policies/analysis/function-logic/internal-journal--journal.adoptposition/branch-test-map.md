# Branch Test Map: `Journal.AdoptPosition`

| Branch | Scenario | Test | RED observed | GREEN observed |
|---|---|---|---|---|
| B1 | AST branch B1 preserves its fail-closed/default outcome | existing adoption tests and a044 generation isolation | yes | yes |
| B2 | AST branch B2 preserves its fail-closed/default outcome | existing adoption tests and a044 generation isolation | yes | yes |
| B3 | AST branch B3 preserves its fail-closed/default outcome | existing adoption tests and a044 generation isolation | yes | yes |
| B4 | AST branch B4 preserves its fail-closed/default outcome | existing adoption tests and a044 generation isolation | yes | yes |
| B5 | AST branch B5 preserves its fail-closed/default outcome | existing adoption tests and a044 generation isolation | yes | yes |
| B6 | AST branch B6 preserves its fail-closed/default outcome | existing adoption tests and a044 generation isolation | yes | yes |
| B7 | AST branch B7 preserves its fail-closed/default outcome | existing adoption tests and a044 generation isolation | yes | yes |
| B8 | AST branch B8 preserves its fail-closed/default outcome | existing adoption tests and a044 generation isolation | yes | yes |
| B9 | AST branch B9 preserves its fail-closed/default outcome | existing adoption tests and a044 generation isolation | yes | yes |
| B10 | AST branch B10 preserves its fail-closed/default outcome | existing adoption tests and a044 generation isolation | yes | yes |
| B11 | AST branch B11 preserves its fail-closed/default outcome | existing adoption tests and a044 generation isolation | yes | yes |
| B12 | AST branch B12 preserves its fail-closed/default outcome | existing adoption tests and a044 generation isolation | yes | yes |
| B13 | AST branch B13 preserves its fail-closed/default outcome | existing adoption tests and a044 generation isolation | yes | yes |
