# Branch Test Map: `Journal.OpenExitState`

| Branch | Scenario | Test | RED observed | GREEN observed |
|---|---|---|---|---|
| B1 | AST branch B1 preserves its fail-closed/default outcome | exit-state identity tests and a044 snapshot immutability | yes | yes |
| B2 | AST branch B2 preserves its fail-closed/default outcome | exit-state identity tests and a044 snapshot immutability | yes | yes |
| B3 | AST branch B3 preserves its fail-closed/default outcome | exit-state identity tests and a044 snapshot immutability | yes | yes |
| B4 | AST branch B4 preserves its fail-closed/default outcome | exit-state identity tests and a044 snapshot immutability | yes | yes |
| B5 | AST branch B5 preserves its fail-closed/default outcome | exit-state identity tests and a044 snapshot immutability | yes | yes |
| B6 | AST branch B6 preserves its fail-closed/default outcome | exit-state identity tests and a044 snapshot immutability | yes | yes |
| B7 | AST branch B7 preserves its fail-closed/default outcome | exit-state identity tests and a044 snapshot immutability | yes | yes |
| B8 | AST branch B8 preserves its fail-closed/default outcome | exit-state identity tests and a044 snapshot immutability | yes | yes |
| B9 | AST branch B9 preserves its fail-closed/default outcome | exit-state identity tests and a044 snapshot immutability | yes | yes |
| B10 | AST branch B10 preserves its fail-closed/default outcome | exit-state identity tests and a044 snapshot immutability | yes | yes |
| B11 | AST branch B11 preserves its fail-closed/default outcome | exit-state identity tests and a044 snapshot immutability | yes | yes |
| B12 | AST branch B12 preserves its fail-closed/default outcome | exit-state identity tests and a044 snapshot immutability | yes | yes |
| B13 | AST branch B13 preserves its fail-closed/default outcome | exit-state identity tests and a044 snapshot immutability | yes | yes |
| B14 | AST branch B14 preserves its fail-closed/default outcome | exit-state identity tests and a044 snapshot immutability | yes | yes |
| B15 | AST branch B15 preserves its fail-closed/default outcome | exit-state identity tests and a044 snapshot immutability | yes | yes |
| B16 | AST branch B16 preserves its fail-closed/default outcome | exit-state identity tests and a044 snapshot immutability | yes | yes |
| B17 | AST branch B17 preserves its fail-closed/default outcome | exit-state identity tests and a044 snapshot immutability | yes | yes |
| B18 | AST branch B18 preserves its fail-closed/default outcome | exit-state identity tests and a044 snapshot immutability | yes | yes |
| B19 | AST branch B19 preserves its fail-closed/default outcome | exit-state identity tests and a044 snapshot immutability | yes | yes |
| B20 | AST branch B20 preserves its fail-closed/default outcome | exit-state identity tests and a044 snapshot immutability | yes | yes |
