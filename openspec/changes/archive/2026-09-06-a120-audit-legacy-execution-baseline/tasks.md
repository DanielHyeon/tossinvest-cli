## 1. Contract and evidence

- [x] 1.1 Reserve a120/Story, capture its unchanged baseline and validate the complete proposal/design/spec/task artifacts.
- [x] 1.2 Record CodeGraph and pre-edit Python AST/function/branch evidence for every existing checker function that will change.
- [x] 1.3 Complete independent adversarial proposal review and gstack autoplan review; Manager freezes the exact exception policy before implementation.

## 2. Implement the bounded adoption contract

- [x] 2.1 Add isolated Git-fixture regressions for wrong/modified/symlinked P, fixed E, strict S-before-H ancestry, source-tree/record substitution, dirty/build-input drift, omitted/duplicate/reverted/merge-parent history, missing E-based maps and unchanged ordinary behavior, with valid positive controls.
- [x] 2.1a Cover untracked/ignored Go/C/assembly/header/embed inputs, source files in allowed metadata roots, harmless generated metadata, both Go enumeration modes and enumeration failures.
- [x] 2.2 Implement strict one-tuple adoption/base-file validation and a non-overwriting draft generator sharing the canonical graph/history/function serializer.
- [x] 2.3 Integrate effective-base selection and immutable-target inventory derivation while preserving ordinary checking, reference conflicts and full E-based map validation.
- [x] 2.4 Document the exception, generator commands, evidence limits, recovery and unchanged ordinary policy in WORKFLOW and tool guidance.
- [x] 2.5 Review/freeze the external-interpreter integration amendment, capture doctor pre-edit evidence, then implement/test explicit external SDD_PYTHON with unchanged default behavior and no adoption-source relaxation; document and prove adopted-worktree compatibility.

## 3. Verification and acceptance

- [x] 3.1 Run focused regression tests, full SDD tests, strict OpenSpec/PM checks and appropriate whole-repository test, seams, race and vet checks; retain actual exits.
- [x] 3.2 Complete a separate adversarial implementation review, resolve findings, then complete post-adversarial gstack review.
- [x] 3.3 Refresh hard evidence and run make sdd-sync and make sdd-check against the final isolated-worktree implementation; record the reviewed source snapshot.
- [x] 3.4 Obtain Manager's independent implementation verification and readiness decision for the final completion sequence, with PM mapping valid and no unresolved implementation-review findings.

## Mandatory completion sequence

After the prerequisite tasks above are complete, run
`make gate CHANGE=a120-audit-legacy-execution-baseline`. Only an actual success
permits Manager final acceptance and the official spec-syncing archive command.
Then synchronize the dated PM path, validate the canonical spec and PM mapping,
and integrate only the reviewed changes. A failed gate reopens the relevant task
and blocks archive. Completed checkboxes alone never establish change completion.
