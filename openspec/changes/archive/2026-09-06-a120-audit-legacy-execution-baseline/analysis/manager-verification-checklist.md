# Manager verification — pending

This checklist records remaining acceptance work, not a completion verdict.
The isolated WIP commit is not approved for integration into the shared checkout.

## Evidence required before readiness

- A valid detached P/E/S/H fixture must select E. Each key negative mutation
  must reject for its intended diagnostic, rather than an earlier fixture error.
- Strict record and ledger schemas must reject boolean schema values, unknown
  fields, malformed field types and altered inherited-history debt disposition.
- Distinct, committed, current-change review files must bind actual adversarial
  and subsequent gstack reviews; symlink parents and substituted bytes must fail.
- Source verification must cover ordinary untracked and ignored files, tracked
  Go modes/blobs, NUL-delimited Git paths and both actual Go enumeration modes.
- Generator outputs must refuse existing files, including broken symlinks, and
  must never create an approval claim or modify the planning base.
- End-to-end checker evidence must show ordinary behavior remains intact and
  adoption still requires complete E-based maps, hashes, revisions, branches,
  calls and test citations, including deleted functions.
- Final code must receive independent adversarial review followed by gstack
  review; provisional findings are not closed by an implementer's statement.
- Final test, SDD, strict OpenSpec and PM validation results must retain actual
  commands and exit statuses. A missing exit status is unknown, not success.

After these prerequisites, Manager may mark readiness tasks and require the
actual a120 gate before final acceptance, canonical archive and integration.
The disclosed implementation-before-RED deviation remains in the evidence.
Nothing in a120 acceptance completes a063 operational acceptance.

## Continuation checkpoint

Manager inspected durable exit files for the valid-adoption/inventory pair,
evidence-binding increment and interim source guard; each records exit 0.
These are implementation-side focused results, not independent final review.
The dedicated filesystem fixture first failed because nested cache `.pyc`
files were accepted, then passed after restricting the allowance to direct
children of `__pycache__`. Ignored assembly and remaining build-input cases
still need explicit coverage.

Implementation ownership was transferred from `a120_impl` to fresh Terra
`a120_finish` without discarding the isolated worktree. `sdd_adversary` remains
the dedicated independent adversary for the implementation. Strict schema,
generator and end-to-end checker work remained open at that checkpoint.

Subsequent independent review confirmed helper tests and closure of the
`core.filemode=false` executable-bit bypass. Real checker fixtures now cover
adoption E selection, environment override rejection, invalid-record failure,
stale evidence, local/reference conflict and deleted-function base revision.
A shared fixture regression was found during full SDD testing and corrected.
The ordinary dirty-Go/no-record positive control still required repair, so
fresh Terra `a120_verify` took implementation/verification ownership from
`a120_finish`; the same independent adversary remains assigned. No final
review, gate, archive or shared-checkout integration has been approved.

## Current verified implementation

The subsequent gstack review closed its two code findings and inspected the
corrected AST evidence. It reports no unresolved implementation findings while
retaining the disclosed historical/process concerns. Manager compared the four
source/test SHA-256 values in that report directly to the current files; all match.

Manager also read durable exit 0 records for full Go tests, seam-tagged tests,
repository race targets, both vet profiles, final full SDD tests and strict
OpenSpec validation. PM generation/check passed. Tasks 2, 3.1 and 3.2 are now
complete. Freshness/check and final gate readiness are still required before
acceptance or archive. This change does not complete a063 operations.

## External interpreter amendment — current acceptance hold

The preceding verification describes the original implementation snapshot.
Manager subsequently found that the doctor's mandatory local virtualenv would
conflict with the adoption source guard. The frozen amendment adds an explicit
external doctor interpreter without relaxing that guard. Task 2.5 and tasks
3.1–3.4 remain open pending final amendment evidence and independent reviews.

The dedicated amendment adversary found real symlink-loop and lexical path
alias/dot-segment failures, plus malformed UTF-8 requirements handling. Terra
is repairing these with regression evidence. A NUL argument is a defensive
helper case only: actual operating-system environment values cannot contain
NUL. No final gate, archive or shared-checkout integration is accepted at that
checkpoint.

## Final readiness decision — 2026-09-06

The amendment's dedicated adversary and subsequent gstack implementation review
are CLEAR, including independent re-execution of the two final test closures.
Manager inspected the production path/pin/error handling and real Git fixture,
read the reviews and actual numeric command exits, and compared these final
SHA-256 values directly with the files:

| File | SHA-256 |
| --- | --- |
| `tools/logic-map/check_analysis.py` | `f7d95a10cee85773189b4ee446a282eed7fb78021e361868029bda4ae5f1ebf2` |
| `tools/logic-map/execution_baseline.py` | `e9d8a55c718fe3568d09facb355e507c06c565f5bed915ef1b0f780d0104e1c3` |
| `tools/logic-map/test_execution_baseline.py` | `95bbd64dd0acc61a69ee1b18a809f3a75e80e867fc1ad7b4a6d526d99b8182ea` |
| `tools/logic-map/test_check_analysis.py` | `85e0de6a38ebbc5e68ea00781c314bc01db416fe44742a30d5df74c312b73430` |
| `tools/sdd/sdd_doctor.py` | `e038dce83e10cec6df49977a20c6bcb8dc5d873edae6bf81a95e08f2f7f04268` |
| `tools/sdd/test_sdd_doctor.py` | `43fca70980b7bd24c8cc9931eff04bea2c2d78788dd301f84dcd22b274c2369a` |

Final focused and full external-mode SDD tests passed, as did a120/all OpenSpec
strict validation, PM check and diff check. Prior full Go/seams/race/vet runs
remain applicable to unchanged Go source. Terra's final `make sdd-sync` and
`make sdd-check`, both with
`SDD_PYTHON=/tmp/tossos-a120-external-sdd-venv/bin/python`, each have actual exit
0 in `/tmp/a120-external-final-sdd-{sync,check}.exit`. GBrain advisory upgrade
and un-extracted-edge messages are not interpreted as hard proof or hidden.

Manager accepts readiness for the mandatory completion sequence. Because this
task-state update changes documentation after synchronization, Terra must
regenerate/check PM, refresh SDD again, and run the actual a120 gate. Final
acceptance, official archive and integration remain conditional on that gate's
actual success. Historical/process exceptions remain disclosed; no a063
operational acceptance is inferred.
