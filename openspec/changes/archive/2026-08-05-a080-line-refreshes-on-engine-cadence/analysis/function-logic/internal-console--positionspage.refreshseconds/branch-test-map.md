# Branch Test Map: `positionsPage.RefreshSeconds`

> **개정 2026-08-05 (review.md F5).** 이 표의 앞 판은 철회된 fragment+스크립트
> 설계를 서술하고 존재하지 않는 task 3.8·3.9·3.10을 참조했다. AST만 재생성하고
> 본문을 갱신하지 않은 결과였다 — gate가 산출물의 **존재**는 보지만 실질은 보지
> 않는다는 것이 그 사이 드러난 사실이다.

AST `branches: null` — 분기가 없다. 따라서 분기별 표가 아니라 **계약별** 표다.
분기 0개인 함수의 위험은 "어느 갈래가 안 덮였나"가 아니라 "이 값을 읽는 곳들이
서로 어긋나나"이므로 소비처를 기준으로 덮는다.

| Branch | Scenario | Test | RED observed | GREEN observed |
|---|---|---|---|---|
| B1 | happy path — 분기 없는 유일 경로. 엔진 관측 주기를 초로 환산해 5를 반환한다 | 3.1 | yes | yes |
| C1 | 렌더된 meta 태그가 `content="5"` | 3.1 | yes | yes |
| C2 | 출처 분리 — `holdingsTTL`을 바꿔도 이 값이 따라 바뀌지 않는다 | 3.1 | yes | yes |
| C3 | 스트립 reload 셀과 meta 태그가 계속 한 사실이다 | 3.3 | no | yes |
| C4 | 두 라인 화면이 같은 주기를 렌더한다 | 3.2 | yes | yes |
| C5 | shadowing 미발생 — 이 메서드를 지우면 `chrome`의 0이 승격되어 화면이 조용히 갱신을 멈춘다 | 3.4 | no | yes |
| C6 | 브로커 예산 — 주기가 TTL 아래여도 홀딩스 호출이 `ceil(창 길이 / TTL)`을 넘지 않는다 | 2.1 | yes | yes |
| C7 | 엔진 부하 — 주기가 6배가 되어도 엔진 프로세스 읽기는 간격당 1벌을 넘지 않는다 | a081 2.2 | yes | yes |

C6은 이 함수의 테스트는 아니지만 이 함수의 **주석이 주장하던 것**이고, 그 주장이
틀렸다는 것이 a080의 근거다(issues.md I1).

C7은 a080이 land하기 위해 반드시 성립해야 하는 성질이며 **a080이 아니라 a081이**
만든다. 독립 리뷰 F1이 찾은 것이 정확히 이 칸의 부재였다 — 초안에는 브로커
예산(C6)만 있었고, 같은 질문을 엔진에 대해 하지 않았다.
