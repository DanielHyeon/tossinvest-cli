# Evidence reconciliation

- OpenSpec and current HEAD agree that automatic external-position adoption is
  already implemented and executed only by the Verified engine reconciliation
  path.
- CodeGraph shows no need for a new engine or policy implementation.
- Current deployed HTML confirms that the controls already exist at `/settings`
  but are labelled only `편입 설정` and share a long page with unrelated
  Guardian, operating, and update controls.
- The user request is therefore resolved as a discoverability/copy revision:
  navigation link, adoption anchor, title, and explicit execution-boundary text.
- CodeGraphContext was unavailable as advisory evidence; this does not conflict
  with current HEAD, strict OpenSpec, rendered HTML, or CodeGraph hard evidence.
- Function Logic Map: not-applicable. No existing Go function body is modified;
  only template constants and tests are changed.
