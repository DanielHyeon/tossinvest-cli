# a098 tasks (2판)

base: `e6c4636adc4358796a6ac6feb3f8ac9a23d73193` (`base-commit.txt`)

> **⛔ base를 재고정했다 — a099가 착지했기 때문이다 (2026-08-12).**
> 옛 base는 `285c7619`(a097 커밋)였고, 그 뒤로 a099·a100·a101이 main에 들어갔다.
> 그대로 두면 `check_analysis`가 **a099의 편집을 a098의 diff로 센다** — 실측:
> `missing evidence for modified function` **서른 건 넘게**(`outbox.go:Journal.EnqueueAlert`,
> `notifier.go:Notifier.claimAndDeliver`, `soak.go:Runner.RunCycle` …).
> 재고정 뒤 그 목록이 **둘**로 줄었고, 그 둘을 §1.5가 다시 만들어 **exit 0**이 됐다.
>
> `capture_change_base.py`는 덮어쓰기를 거절하므로(`change base already captured`)
> 파일을 직접 고쳤다. **그 도구의 「immutable」 주장에서 벗어난 것이고, 숨기지 않는다** —
> 근거는 짝 change가 먼저 착지하는 것이 이 배포 단위의 **설계**(0.1·D6)라는 것이다.
> `e6c4636a`의 tree는 a099 tip `21a315d1`과 **바이트 단위로 같다**(`rev-parse ^{tree}` 대조).

> **이 change의 규율 하나**: a092가 열여덟 라운드를 통과 못 한 이유는 결함이 어려워서가
> 아니라 **한 change가 다섯 가지를 동시에 지려 했기 때문**이다. a098은 하나만 진다 —
> **밀린 critical 알림을 보내는 주체를 만든다.** 범위를 넓히자는 제안이 오면 a092로 간다.

## ⛔⛔ 2판이 바꾼 것 — `Flush`를 안 부른다 (2026-08-10, 사용자 결정 안 1)

1판은 *"`Flush`를 부르는 주체를 만든다"*였다. a099의 1라운드 리뷰(A-P2)가 그것을 깼다:
`Flush`는 `n.mu`를 쥔 채 밀린 행 **전부**를 publish하고(`notifier.go:434-437`·`:441-451`),
**동기 정지 알림 경로가 같은 뮤텍스를 쓴다.** 정지가 `N × publish timeout`만큼 밀리고
**N에 상한이 없다** — a092가 고치려는 결함을 a098이 다시 만드는 형태였다.

| | 1판 | **2판** |
|---|---|---|
| 배달 주체 | `Notifier.Flush`를 부르는 루프 | **자기 배달 경로를 가진 루프** (`internal/app/engine`) |
| 잠금 | `n.mu`를 전송 위에서 쥔다 | **어떤 뮤텍스도 전송 위에서 안 쥔다** |
| 배제 | `n.mu` | **a099의 원장 claim** |
| a099 의존 | 없음 | **선행 필수** |
| a092 의존 | 없음 | 없음 (표면이 안 겹친다) |

정본은 design **D1.1·D1.2**다.

## 0. 선행 조건

- [x] 0.0 **⛔ a099와 한 배포 단위로 나간다** (사용자 결정 3, 2026-08-11 · design D6).
      a099를 혼자 올리면 claim 뒤 죽은 행을 **다시 집을 주체가 없어 오늘보다 나쁘다.**
      각자 자기 gate를 통과하되 **중간 상태로 배포하지 않는다**.

      **이제 기계가 강제한다** (사용자 결정 7-1, 2026-08-11):
      `deploy-pair.txt`가 이 디렉터리와 a099 양쪽에 있고, `tools/gate.sh`의 3단계가
      짝의 미완료 task와 **상호 선언**을 검사한다. 문장 선언만 남기지 않는다 —
      a099 3라운드가 그것을 두 보이스에서 각각 결함으로 지목했다
- [x] 0.1 **a099가 먼저 착지해야 한다.** 이 루프는 `n.mu`를 안 쥐므로
      `claimAndDeliver`와 갈리는 것이 원장 claim뿐이다 (design D1.2).
      a099 없이 이 루프를 켜면 **같은 행을 둘이 보낸다**

      **착지했다 (2026-08-12).** `feat/a097-rearm-is-a-new-episode` 21커밋을
      `--no-ff`로 main에 병합했다 — merge `e6c4636a`, a099 본체는
      `757550f1`·`b8ad147b`·`21a315d1` 셋이다. main에서 `go build ./...` 통과.

      > **「착지」는 「배포」도 「완료」도 아니다.** a099의 `6.4 make gate`는 **열린 채**
      > 착지했다 — gate 3단계 (b)가 짝의 미완료 0건을 요구하는데 그 미완료에 **이 파일의
      > task들**이 들어간다. 면제는 짝의 `make gate` 줄 하나뿐이고(gate.sh:36-39)
      > 0.1은 면제 대상이 아니었다. 그래서 순환이었고, 푸는 방향은 하나뿐이다:
      > **a099가 gate 없이 착지 → a098 구현 → 그때 a099의 gate가 초록 → 합동 배포.**
      > a099를 혼자 배포하지 않는 제약은 그대로다(0.0 · a099 §6.6).
- [ ] 0.2 **a099 4라운드가 넘긴 요건 — 이 루프가 없으면 알림이 무기한 안 나간다.**
      (a099 review §11.3 · design D14.4, 2026-08-12. codex 교차 모델이 지목했다.)

      **만료는 수동이다.** `alert_claim.go`의 만료 술어는 *다음 claim 이 일어날 때만*
      평가된다 — 시간이 지난다고 임차가 저절로 풀리지 않는다. 그리고 a099 시점에
      `ClaimHeldElsewhere`는 발행도 게이트도 에스컬레이션도 하지 않는다
      (`notifier.go`의 `owed=false`, D10이 옳다고 세운 그대로).

      두 사실이 겹치면: 발송자가 임차를 쥔 채 죽고 → 조건이 **한 번** 더 관측되고 →
      그 관측이 held 로 물러나고 → 조건이 다시 안 일어나면, **그 critical 알림을
      집을 주체가 영원히 없다.** a099 이전에는 다음 관측이 그냥 보냈다.

      a099는 이 창을 **좁히고 보이게** 했지 닫지 않았다 — 정상 종료의 반납을 고쳤고
      (취소되지 않는 ctx), held 를 `Warn` + `claim_expires_at` 으로 올렸다. **닫는 것은
      이 루프다.** 그러므로 이 루프는:

      1. PENDING 을 주기적으로 훑는다 — 조건의 재관측에 의존하지 않는다.
      2. **만료된 임차를 실제로 집는다** (`ClaimAlertByID` 가 `Stole=true` 로 준다).
      3. `ClaimHeldElsewhere` 를 건너뛰되 **조용히 넘기지 않는다** — 같은 행이 매 주기
         held 로 남으면 그것은 경합이 아니라 잠긴 것이다.

      §3 이 이것을 RED 로 관측한다: 임차를 쥔 채 죽은 행 하나를 두고, 조건을 다시
      일으키지 않은 채 루프만 돌려서 배달되는 것을 본다.

      > **⛔ 그 문장을 쓴 시점에 §3 에는 대응 행이 없었다 — 이제 있다 (2026-08-12).**
      > 위 셋을 §3 등록부에 하나씩 배정했다. **①만 이미 있었다.**
      >
      > | 0.2 의 요구 | 지는 R | 상태 |
      > |---|---|---|
      > | ① PENDING 을 주기적으로 훑는다 | **R1** (이미 있었다) | — |
      > | ② 만료된 임차를 실제로 집는다 | **R20** | **신설** |
      > | ③ held 를 조용히 안 넘긴다 | **R21** | **신설** |
      >
      > *"§3 이 관측한다"*는 **§3 에 행이 있을 때만 참**이다. 그 문장이 스스로를
      > 참으로 만들지 않는다 — 두 줄이 열흘 가까이 주인 없이 있었다.

## 1. 증거 (완료)

- [x] 1.1 `Gateway.parkAlert` AST — 분기 2 (`replay.go:534-559`)
      → `analysis/function-logic/internal-execgw--gateway.parkalert`
- [x] 1.2 `Notifier.Flush` AST — ~~분기 6 / 이탈 4 (`notifier.go:427-462`)~~
      → **분기 19 / 이탈 4 (`notifier.go:727-826`)**, §1.5가 다시 쟀다
      → `analysis/function-logic/internal-obs--notifier.flush`
- [x] 1.3 `Journal.PendingAlerts` AST — 분기 2 / 이탈 2
      (~~`outbox.go:392-405`~~ → **`:517-530`**, 본문은 안 바뀌었다)
      → `analysis/function-logic/internal-journal--journal.pendingalerts`
- [x] 1.4 **프로덕션 호출자 수 실측** — `Flush` **0** · `Acknowledge` **0**
      (자기 패키지 밖 기준, 테스트 제외)

      > **⛔ 5라운드 B-T1 — 이 줄이 거짓이었다.**
      > 원래는 *"`PendingAlerts` 0 · `UndeliveredCount` 0"*까지 적혀 있었는데
      > **둘 다 호출자가 있다** — `notifier.go:437`·`:491`(`PendingAlerts`),
      > `notifier.go:460`·`:506`(`UndeliveredCount`). `internal/obs`는
      > `internal/journal` 밖의 패키지이므로 「자기 패키지 밖」에 정확히 해당한다.
      >
      > **참인 명제는 도달성이다**: 그 둘을 부르는 상위(`Flush`·`Acknowledge`)의
      > 프로덕션 호출자가 0이라 **런타임에서 도달하지 않는다.**
      > 「호출자 0」과 「도달 불가」는 다른 말이고, 앞엣것으로 적으면
      > **`grep`으로 5초면 반증된다.**
      >
      > **재측정 (2026-08-12, a099 착지 뒤)**: `Notifier.Flush`의 프로덕션 호출자는
      > **여전히 0**이다 — non-test 매치는 전부 `bufio`·`csv`·`http.Flusher`다.
      > 테스트 호출자만 3 → **6**으로 늘었다.

- [x] **1.5 a099가 이 증거를 무효로 만들었고, 다시 쟀다** (2026-08-12).

      `check_analysis`가 `AST source hash is stale`로 두 번들을 막았다. **막은 것이 옳다.**

      | 번들 | 1판 (`285c7619`) | **재측정 (`e6c4636a`)** | 무엇이 바뀌었나 |
      |---|---|---|---|
      | `internal-obs--notifier.flush` | `:427-462` · 분기 **6** · 호출 9 | `:727-826` · 분기 **19** · 호출 **24** | a099 §4.11이 이 함수를 임차 API 위로 다시 썼다 |
      | `internal-journal--journal.pendingalerts` | `:392-405` · 분기 2 | `:517-530` · 분기 **2** | **본문은 그대로.** 밀린 것은 줄뿐 |

      > **✅ 그런데 D1.1의 판정은 안 흔들렸다 — 그것을 확인하려고 다시 잰 것이다.**
      > 안 C를 기각한 근거 두 줄이 오늘 코드에 그대로 있다:
      > `n.mu`를 배수 루프 전체 위에서 쥐고(`:734-735`), `PendingAlerts(ctx, **0**)`으로
      > 전부를 받는다(`:737`). a099는 그 **구간 안**을 임차로 정확하게 만들었지
      > **구간 자체를 줄이지 않았다** — 오히려 행마다 claim·settle·release가 붙어
      > 잠금 아래 원장 왕복이 **늘었다.**

      **커버리지는 주장이 아니라 측정값이다** — `go test ./internal/obs/ -coverprofile`
      (exit 0 · **81건 통과**)의 블록 카운트를 `:727-826`으로 잘라 읽었다.
      **19 분기 중 본문이 실행되는 것은 넷**(B3·B7·B8·B9)이고, 셋(B6·B12·B16)은
      **조건조차 평가되지 않는다** — 한 번도 안 도는 오류 팔 안의 `n.Log != nil` 가드다.

## 2. Function Logic Map / Branch Test Map 완성

- [x] 2.1 세 함수의 `function-logic-map.md`를 AST 열거로 채웠다.
      **손으로 읽은 분기 주장을 넣지 않았다** — 열거는 `ast.json`에서 왔다
- [x] 2.2 세 함수의 `branch-test-map.md`. GREEN 칸의 테스트는 전부 **호출 지점을
      실측해서** 골랐다. **덮이지 않은 것을 덮였다고 적지 않았다**

      > **⛔⛔ 이 줄이 적고 있던 뒷문장은 두 번 거짓이었다 — 지운다 (2026-08-12).**
      > 원문: *"`Flush`는 분기 6 중 **5개가 미덮임**이고 그 다섯이 §3의 R1~R5가 됐다."*
      >
      > | 무엇이 거짓인가 | 왜 |
      > |---|---|
      > | *"분기 6"* | a099 착지 뒤 **19**다 (§1.5) |
      > | *"그 다섯이 §3의 R1~R5"* | §3 등록부가 5판·6판·7판을 거치며 **다른 성질로 다시 세워졌다.** 오늘의 R1은 *"기록된 critical 알림은 발송된다"*, R5는 *"운영자가 밀린 것을 읽고 승인으로 게이트를 푼다"*다 |
      >
      > **번호는 그대로 두고 뜻만 바꾼 참조가 가장 반증하기 어렵다** — 읽는 사람이
      > R1을 찾아가면 문장이 **있기는 하기** 때문이다. 두 산출물의 대응은 이제
      > 분기가 아니라 **규범** 단위로 적혀 있다(`branch-test-map.md`의 마지막 표).

- [x] 2.3 `risk-pattern-report.md` — `ast-grep`을 **실제로 돌렸다**.
      a099 착지 뒤 두 파일에 대해 재생성했고 **둘 다 매치 없음**이다
- [x] 2.4 `check_analysis.py --change a098-…` → **PASS**

      > **이 초록은 두 번 뒤집혔다.** 1판에서 PASS → a099 착지로 **FAIL**(서른 건 넘는
      > `missing evidence` + `AST source hash is stale` 둘) → base 재고정으로 둘만 남고
      > → §1.5의 재생성으로 **exit 0** (`evidence complete or diff-proven exempt`).
      >
      > **그러므로 이 줄은 「한 번 통과했다」가 아니라 「지금 통과한다」로만 읽어야 한다.**
      > 짝 change가 같은 파일을 건드리면 다시 뒤집힌다 — a092가 `Flush` 본문을 편집할
      > 계획이므로(design D4) **그때 또 재생성해야 한다.**
- [x] **2.5 `Runtime.Run` 번들** (19판 4차 신설 — 19라운드 **B-P7**)

      > ## ⚠⚠ §2가 완료를 선언했는데 편집할 High-risk 함수 하나가 빠져 있었다
      >
      > design D2와 `design.md:40`이 *"루프 하나 신설 + **`Runtime` 감독 정책
      > 한 갈래**"*라고 적는다. `Runtime.Run`은 **기존 High-risk 함수**이므로
      > `.claude/CLAUDE.md`상 **면제할 수 없다.** 19판 4차까지 a098의 번들은
      > 셋뿐이었고 `runtime.run`이 없었다.
      >
      > **`check_analysis`는 그것을 잡지 못했고, 못 잡은 것이 맞다** —
      > 프로덕션 Go diff가 아직 비어 있어 검사가 **비교할 대상이 없다.**
      > 초록이 증거가 아니었던 자리이고, 그 사실이 **이 change의 §2 전체에
      > 걸린다**: diff가 생기는 순간 검사가 비로소 말을 시작한다.
      >
      > `ast.json`은 `go run ./tools/logic-map`으로 뽑았고 a092의 같은 함수 번들과
      > **바이트 단위로 같다**(같은 HEAD·같은 함수). 분기 4 · 이탈 3 · 호출 30 ·
      > defer 3. **다른 것은 markdown 둘이다** — a092는 *"편집하지 않는다"*를 적고,
      > a098은 **무엇을 어디에 더하는지**를 적는다(B3의 슬라이스, B4와 `:331` 사이).
      >
      > **`wg.Wait()`(`runtime.go:300`)가 두 제약이 만나는 한 줄**이라는 사실이
      > 이제 a098 안의 산출물에 있다. 19판이 분리할 때 그 산출물은 a092에 남았고
      > 요구도 절반만 건너왔다 — R7이 나머지 절반이다.

      > **검사기가 이 bundle을 쓰는 중에 나를 두 번 잡았다. 적어 둔다.**
      >
      > 1. `replay_test.go:628`이라고 썼는데 그 이름은 **두 패키지에 있다**
      >    (`internal/execgw` 753줄 · `internal/journal` 228줄). 검사기가
      >    가까운 쪽으로 풀어서 *"228줄 파일의 끝을 넘는다"*로 울렸다.
      >    **19판이 B-T1로 만든 바로 그 함정에 19판이 걸렸다.**
      > 2. 그것을 고치려고 경로를 `internal/execgw/replay_test.go`로 한정했더니
      >    **검사기가 그 한정을 버리고 있었다** — `resolve_test_file`이 basename만
      >    썼다. 한정하라고 말해 놓고 한정을 무시하면 그 조언은 빈 말이다.
      >    도구를 고쳤고(`tools/logic-map/check_analysis.py`) 회귀 테스트 둘을 더했다.
      >
      > 두 번째가 더 중요하다. 첫 번째는 문서의 오타이고, 두 번째는 **검사가
      > 스스로 약해지는 방식**이었다.

- [x] **2.6 `NewRuntime` 번들** (4판 신설 — 사용자 결정 9-2).
      배달 실행자를 감독 집합 밖에 두려면 `RuntimeOptions`에 필드가 생기고,
      **그 필드를 조립 시점에 거부하는 자리가 `NewRuntime`**이다. 기존 High-risk 함수다.
      AST **분기 12 · 이탈 7 · 호출 13** → `analysis/function-logic/internal-app-engine--newruntime`

      > 이 번들이 잡은 것 하나: **B10·B11·B12의 참 쪽(0값 → 기본값 보정)을
      > 아무 테스트도 안 덮는다.** 기존 열한 테스트가 전부 명시값을 넘긴다.
      > a098은 그 셋을 안 건드리므로 RED 대상이 아니지만(`not-applicable`),
      > **「덮여 있다」고 적을 뻔한 자리**였다.
- [x] **2.7 `AllReasonCodes` 번들** (4판 신설 — 사용자 결정 8-1).
      여섯 번째 트리거가 **자기 ReasonCode를 가져야 하고**(design D8.2) 그 코드는
      이 함수의 리터럴에 들어간다 — 기존 함수 본문 편집이다.
      AST **분기 0 · 이탈 2 · 호출 1** → `analysis/function-logic/internal-execgw--allreasoncodes`

      > **분기가 0이어도 산출물을 만든 이유**: 「분기가 없다」를 AST로 **확인한 것**과
      > **안 본 것**은 다르다. 그리고 이 번들이 실제로 하나를 잡았다 —
      > 상수를 `reason.go`에만 선언하고 `AllReasonCodes`에 안 넣으면
      > **아무 테스트도 안 깨진다.** `failclosed.go:283-288`이 그 일이 이미 있었다고 적는다.
      > §5의 VERIFY가 열거 **길이**를 직접 재는 이유가 이것이다.
- [x] **2.8 `Runtime.Run` 번들을 결정 9-2로 개정했다** (4판).
      **3판의 결론이 뒤집혔다** — 3판은 *"배달 루프가 `Loops`에 들어간다(B3의 원소가 는다)"*
      였고, 4판은 **안 들어간다.**

      | | 3판 | **4판 (결정 9-2)** |
      |---|---|---|
      | `:331`(엔진 전체 정지)을 피하는 법 | **정지 정책 한 갈래**를 더해 판정으로 피한다 | **`stops`에 안 보내서** 구조적으로 못 온다 |
      | 편집 후 예측 | 분기 5 · **이탈 3 이상** | 분기 5 · **이탈 3 그대로** |

      > 개정이 **새 위험 하나를 드러냈다**: `wg`가 배달 실행자를 안 담으므로
      > `Runtime.Run`의 doc(`runtime.go:258-260`)이 계약으로 적는
      > *"every goroutine it started has returned too … without racing a loop that is
      > still writing"*가 **깨진다.** 배달 실행자는 원장에 쓴다.
      > 답은 보조 goroutine을 **기존 `wg`에 넣는 것**이다 — 새 WaitGroup을 만들면
      > 잊을 수 있는 자리가 둘(`Add`와 `Wait`)이 되고, `wg`를 쓰면 하나(`Add`)다.
      > **위험이 0이 되지는 않고** R14가 그 한 자리를 잰다.

## 3. RED — 실패하는 것을 먼저 관측한다

> ## ⛔⛔ 5판 — 5라운드 B-P3·B-P4·B-T8이 이 등록부를 다시 세우게 했다
>
> 4판까지 이 표는 **종류 칸이 없었고**, 없는 칸은 *"전부 오늘 빨갛다"*로 읽혔다.
> 보이스 B가 하나씩 재서 **일곱이 그렇지 않다**고 판정했다 —
> R2·R3·R4·R4b·R6·R7·R13은 **오늘 빨간 게 아니라 관측 자체가 불가능하거나 공허하게 통과**하고,
> 반대로 **R8은 a099 없이도 오늘 빨갛다.**
>
> **「오늘 빨갛다」와 「만든 직후에만 빨갛다」와 「빨간 적이 없다」는 서로 다른 증거다.**
> 셋을 안 가르면 born-GREEN이 RED로 보고된다 — 그것이 a092가 열여덟 라운드를 못 넘긴 형태다.
>
> 그리고 **시간 계약 넷이 약했다**(B-P4). *"유한 시간"*·*"체류를 잰다"*·*"N을 키워도 안 는다"*·
> *"`≤ alertFlushInterval`"*은 전부 **긴 유한 지연을 통과시킨다.** 아래에서 상한을 못 박았다.
>
> 행 수도 맞췄다 — R4b 때문에 **열여덟**이다(B-T8. 3.1이 *"R1~R17"*이라고 적고 있었다).

**종류 셋을 가른다.**

| 종류 | 뜻 |
|---|---|
| **RED 오늘** | 지금 HEAD에서 그 실패를 **실제로 유도할 수 있다** |
| **RED — 시점** | 지정한 중간 단계에서만 빨갛다. **그 시점을 안 잡으면 born-GREEN이다** |
| **회귀 핀** | 빨간 시점이 없다. 오늘의 성질이 **안 바뀌었음**을 고정한다 |

| # | 종류 | 성질 | 관측 (상한·대조군까지) | 근거 |
|---|---|---|---|---|
| **R1** | **RED 오늘** | **기록된 critical 알림은 발송된다** | `parkAlert`가 행을 쓴 뒤 살아 있는 publisher로 그 행이 `DELIVERED`가 된다. **가짜 시계에서 `주기 × 2` 안에** — *"유한 시간"*으로 쓰면 임의로 긴 `Sleep`도 통과한다(B-P4) | 발송 주체가 없다. `Flush`의 프로덕션 호출자 0이고 2판은 부르지도 않는다 |
| **R2** | **RED — 시점** | **배달 실행자가 죽어도 exit 관측 루프가 산다** | **4.0 뒤 4.1 전.** 먼저 실행자를 `Loops`에 넣어 `TestALoopReturningOnItsOwnStopsEverythingAndIsCritical`(`runtime_test.go:171`)이 **엔진을 내리는 것을 보고**, `Auxiliary`로 옮겨 안 내려가는 것을 본다. **그리고 그 정지가 기록으로 남는 것**까지 본다 — 생존만 보면 **아무 기록도 안 남기는 구현이 통과한다**(6라운드 P1) | 오늘 실행자가 없어 **관측 불가**. 9-2 아래에서는 `stops`에 안 써서 구조적으로 안 내려간다 — **그 중간 단계를 안 거치면 born-GREEN**(B-P3) |
| **R3** | **RED — 시점** | **배달 실행자의 죽음은 진입 게이트를 잠근다** | **4.2 뒤 4.3 전.** `OnStop`이 있고 `Block`이 아직 없을 때 `gate.Blocks()`가 비어 있는 것을 보고, 4.3 뒤 차는 것을 본다 | 오늘 실행자도 `OnStop`도 없다 — **관측 불가**(B-P3) |
| **R4** | **RED — 시점** | **전송 수단이 죽어도 exit 사이클 체류가 안 늘어난다** | 응답 없는 publisher를 세우고 exit 사이클 체류를 잰다. **허용 상한은 §5.3이 실측한 기준선 + 고정 여유**로 못 박는다 — *"잰다"*만으로는 나쁜 유한 지연이 통과한다(B-P4) | 배달 경로 자체가 없어 오늘은 대조군이 없다 |
| **R4b** | **RED — 시점** | **배달이 도는 동안 정지 알림이 안 밀린다** | 밀린 행 N개를 두고 실행자를 돌리며 동기 정지 알림의 체류를 잰다. **절대 상한과 N-독립성을 둘 다** 단언한다 — 최소 두 N(예: 10·1000)에서 체류 차이가 고정 여유 안이어야 한다 | **안 C였다면 깨진다**(`Flush`가 `n.mu`를 쥔 채 N개). D1.1의 관측이고 a099 1라운드 A-P2가 요구한 것 |
| **R5** | **RED 오늘** | **운영자가 밀린 것을 읽고 승인으로 게이트를 푼다** | 읽기 표면이 PENDING 행을 돌려주고, 승인 후 `gate.Blocks()`가 빈다. **그리고 그 승인이 남긴 이름이 원장의 `acknowledged_by`에 실제로 들어간 것**까지 본다 — 원장은 빈 이름을 **이미 거부하고**(`outbox.go:371-373`) 이름을 **이미 저장한다**(`:377`). 그래서 위험은 빈 actor가 아니라 **CLI가 고정 문자열을 넣어 그 검사를 통과하면서 audit trail만 죽는 것**이다(6라운드 P4) | CLI 표면이 없고 `Notifier.Acknowledge`의 **외부 프로덕션 호출자가 0**이다 |
| **R6** | **회귀 핀** | **승인에 타이핑 확인이 없다** | 4.4가 만든 명령이 **추가 입력을 요구하지 않는다** | **오늘 CLI가 없어 빨간 시점이 없다**(B-P3). 사용자 지시(2026-07-27)를 **새 표면이 안 어기게** 고정하는 핀이다 |
| **R7** | **RED — 시점** | **실행자가 있어도 엔진이 상한 안에 멈춘다** | ctx를 취소하고 `Runtime.Run`이 반환한다. **가짜 시계를 전혀 전진시키지 않고** 단언한다 — `≤ 주기`로 쓰면 **취소 불가능한 `Sleep(주기)`가 통과한다**(B-P4) | 오늘 실행자가 없다. **`wg`가 담는 goroutine이 하나 늘어** 못 죽는 경로도 하나 는다(A-T3) |
| **R8** | **RED 오늘** | **재시작이 진입 차단을 풀지 않는다** | 미전달 critical 행을 남기고 프로세스를 다시 세운 뒤 `gate.Blocks()`를 관측. **차단이 첫 진입 판정보다 먼저 서는 것**까지 본다 — 최종 상태만 보면 **주문 하나를 통과시킨 뒤 잠그는 구현이 통과한다**(6라운드 P3) | **오늘 풀린다.** `EntryGate`는 메모리 맵뿐이고 원장을 안 읽는다(`execgw/retry.go:498-505`). **a099를 안 기다린다**(B-P3이 시점을 앞당겼다) |
| **R9** | **RED — 시점** | **밀린 목록에 임차 보유자와 나이가 보인다** | 목록이 id·나이·시도 수·보유자를 돌려주고 **토큰 원문·알림 본문·payload·계좌 식별자를 안 돌려준다 — 넷 전부.** 토큰 하나만 보면 약하다: 원천 질의 `alertSelect`(`outbox.go:386-387`)가 **`body`와 `payload`를 이미 싣고 온다** — 구조체를 그대로 출력하는 CLI가 통과한다(6라운드 P4 · 불변식 8 · spec `:174`) | a099의 임차 열과 4.10 투영이 선행 |
| ~~R10~~ | — | ~~임차 기간이 살아 있는 설정에서 계산된다~~ | **삭제 — 7판, 6라운드 P10 판정.** 지키려던 성질은 **a099가 이미 진다**: 값의 충분함은 a099 **R20**(`DefaultAlertLease > DefaultAlertDeliveryBound()`), 주입과 기본값 채우기는 a099 **task 4.1b**와 `internal-journal--open` 번들의 **B7+** 분기다. 남는 조각인 *"운영자가 조절하는 임차"*는 **어느 결정·불변식도 요구하지 않는다** — 세우면 scope creep이다. **번호는 비워 두고 재사용하지 않는다** | 실측: 프로덕션 `journal.Options{…}`(`internal/app/engine/engine.go:638`)에 그런 노브가 없고 a099 C4의 주입은 **인스턴스 단위**다 |
| **R11** | **RED — 시점** | **정산 실패 뒤에 임차를 풀지 않는다** | publish 성공 + 정산 실패 뒤 **다음 주기가 실제로 돌았다는 대조군**과 함께 같은 행이 다시 안 나가는 것을 본다 — 대조군이 없으면 **죽은 루프도 통과한다**(B) | 2판의 「어느 쪽이든 끝나면 해제」가 이것을 깬다 |
| **R12** | **회귀 핀** | **감독 루프 집합이 안 는다** | `LoopNames()`가 **오늘과 같은 넷**을 낸다 — `reconcile`·`exit`·`filldetect`·`strategy-entry`. **정확 비교**로 본다 | **오늘도 넷이다.** 기존 정확 비교 핀은 `TestProductionRuntimeIncludesOneDormantStrategyEntryOuterLoop`(`cmd/tossctl/engine_strategy_entry_dormant_test.go:50`) |
| **R13** | **RED — 시점** | **정상 종료는 게이트를 안 잠근다** | **두 경우.** ① 런타임 취소 ② **감독 루프 하나를 실패시켜** 엔진이 내려감. 둘 다 `gate.Blocks()`가 비어야 한다. ①에서는 **critical 알림도 안 나가는 것**을 함께 본다 — 게이트만 보면 **취소 때 critical을 쏘는 구현이 통과한다**(6라운드 P7) | **오늘은 공허하게 통과한다 — born-GREEN**(B-P3). `OnStop`을 「반환하면 잠근다」로 짜면 ①이, 부모 `ctx`로 판정하면 ②가 깨진다(A-P2) |
| **R14** | **RED — 시점** | **보조 실행자를 다 기다린 뒤 원장이 닫힌다** | 실행자가 **원장 쓰기에 진입했다는 barrier 뒤** 취소하고, ① **쓰기가 풀리기 전에는 `Run`이 반환하지 않고** ② **풀린 뒤에는 고정 상한 안에 반환하는 것**을 본다. ①만 보면 **영원히 안 돌아오는 `wg.Wait()`가 통과한다**(6라운드 P2) | 답은 기존 `wg` 재사용(4.1의 ⛔). **`wg.Add(1)`만 빠지고 `defer wg.Done()`이 남으면 `panic: sync: negative WaitGroup counter`**, 둘 다 빠지면 `engine.go:213`의 deferred close와 **경합한다**(B-P5) |
| **R15** | **RED — 시점** | **실행자의 패닉이 엔진을 안 죽인다** | **subprocess/crash harness가 필요하다**(B-P6) — 같은 프로세스에서 패닉시키면 `recover` 없는 RED에서 **테스트 바이너리가 죽어** exit 루프와 게이트의 사후 상태를 못 본다 | `recover`가 없으면 손절 루프까지 죽는다(불변식 4) — R2가 패닉 경로에서 거짓이 된다 |
| **R16** | **RED 오늘** | **새 사유 코드가 열거 셋에 다 들어간다** | **네 검사 전부**: ① `AllReasonCodes()` 길이 29→30 ② 새 코드의 **명시적 membership** ③ golden 대조(`TestReasonCodeEnumIsStable`, `failclosed_test.go:33`) ④ `latchOrder` 포함 | 코드가 없다. **`reason.go`에만 선언하면 golden이 안 깨진다** — 실측: 상수 45 · 열거 29 · golden 29줄(B) |
| **R17** | **RED — 시점** | **전부 승인해도 「발송 주체 없음」 차단은 안 풀린다** | 실행자를 죽이고 밀린 행을 전부 승인한 뒤 `gate.Blocks()`에 그 사유가 남아 있는 것을 본다 | 오늘 사유가 하나뿐이라 `Acknowledge`(`notifier.go:506-512`)가 미전달 0에서 **그 하나를 푼다**. **다만 「실행자를 죽인다」가 오늘 불가능**하므로 RED 오늘이 아니다 — 6라운드 재판정 |
| **R18** | **RED — 시점** | **실행자의 죽음이 운영 모드를 승격하지 **않는다**** | 실행자를 죽이고 `EscalateOperatingMode`가 **한 번도 안 불린 것**과 모드가 **그대로인 것**을 본다. 게이트는 잠기고 모드는 안 움직인다 — **둘을 한 테스트에서** 본다 | **결정 11-1의 금지를 아무도 안 지고 있었다**(6라운드 P6). 금지는 테스트가 없으면 규율이 아니다 — `Block`과 승격을 **함께** 하는 구현이 R3·R16을 통과한다 |
| **R19** | **RED — 시점** | **배달 실행자와 동기 발송 경로가 같은 행을 동시에 안 보낸다** | 두 경로를 같은 행에 동시에 붙이고 **publish가 정확히 한 번** 일어나는 것을 본다. 진 쪽은 임차 거절로 물러난다 | Scenario(`spec.md:367`)에 **대응 R이 없었다**(6라운드 P9) — 아무도 안 지는 요구였다 |
| **R20** | **RED — 시점** | **만료된 임차를 루프가 실제로 집는다 — 조건의 재관측 없이** | 임차를 쥔 채 죽은 행 하나를 두고 **조건을 다시 일으키지 않은 채** 루프만 돌린다. `ClaimAlertByID`가 **`Stole=true`**로 주고 그 행이 배달된다. **가짜 시계를 임차 만료 너머로 전진시켜야만** 빨갛다 — 안 전진시키면 `HeldElsewhere`로 물러나 **공허하게 통과한다** | **task 0.2 ②.** a099 4라운드가 넘긴 요건인데 **등록부에 대응 행이 없었다**(2026-08-12). a099의 만료 술어는 *다음 claim 이 일어날 때만* 평가된다 — 집는 주체가 이 루프다 |
| **R21** | **RED — 시점** | **매 주기 held 로 남는 행은 경합이 아니라 잠긴 것이고, 그것이 보인다** | 같은 행이 **연속 K주기** `ClaimHeldElsewhere`로 물러나는 상황을 만들고, 그 사실이 **한 번은 기록되는 것**을 본다. **주기마다 한 줄씩 쏟아지지 않는 것**도 함께 본다 — 로그 폭풍은 관측이 아니다 | **task 0.2 ③** (*"조용히 넘기지 않는다"*). a099 시점에 `ClaimHeldElsewhere`는 **발행도 게이트도 에스컬레이션도 안 한다**(`owed=false`, D10) |
| **R22** | **회귀 핀** | **`PendingAlerts(ctx, N)`이 id 오름차순 앞의 N행을 준다** | PENDING 5행에 `limit=2` → 앞의 둘 | **빨간 시점이 없다 — 오늘도 참이다.** 다만 **한 번도 실행된 적이 없다**: 호출 지점 **열일곱이 전부 `0`**이고 커버리지 블록 `520.15-523.3`이 **count=0**이다 (두 측정 독립 일치) |
| **R23** | **RED — 시점** | **한 주기가 batch를 넘지 않고, 나머지는 다음 주기에 나온다** | PENDING을 `batch`보다 많이 두고 **한 주기 뒤** 처리된 수가 `batch`인 것과, **다음 주기 뒤** 나머지가 나오는 것을 본다 | **4.0b가 B1의 참 갈래를 처음 켠다.** 오늘 실행자가 없어 관측 불가 |

> **⛔ R20~R23은 7판 이후에 생긴 구멍이다 — 발명이 아니라 회수다** (2026-08-12).
>
> | # | 어디에 이미 요구가 있었나 | 왜 등록부에 없었나 |
> |---|---|---|
> | R20 · R21 | **task 0.2** — *"§3이 이것을 RED로 관측한다"* | 0.2가 a099 4라운드(2026-08-12)에 들어왔고 **§3 표는 7판(2026-08-11)이 마지막이었다.** 요구가 표보다 나중에 왔다 |
> | R22 · R23 | **task 4.0 `:287`의 `PendingAlerts(ctx, batch)`** · 4.0b · design D4·D5 | `pendingalerts` 번들이 *"a098은 배치를 도입하지 않으므로 이 갈래를 켜지 않는다"*로 **소유를 a092에 잘못 배정**하고 있었다. 그 번들을 다시 만들면서(§1.5) 드러났다 |
>
> **둘의 기전이 다르다.** 앞엣것은 **순서**(요구가 표보다 늦게 왔다), 뒤엣것은
> **오배정**(요구는 있었는데 남의 것으로 적혀 있었다). 뒤엣것이 더 나쁘다 —
> 순서는 다시 세면 보이지만, **오배정은 「누가 지는지 적혀 있으므로」 다 본 것처럼 읽힌다.**

**종류별 개수** — 합이 맞는지 세어서 적는다.

| 종류 | 수 | 어느 것 |
|---|---:|---|
| RED 오늘 | **4** | R1 · R5 · R8 · R16 |
| RED — 시점 | **16** | R2 · R3 · R4 · R4b · R7 · R9 · R11 · R13 · R14 · R15 · R17 · R18 · R19 · **R20** · **R21** · **R23** |
| 회귀 핀 | **3** | R6 · R12 · **R22** |
| **합** | **23** | 빈 번호 하나(R10) — 재사용하지 않는다 |

> **6판이 이 표를 또 바꿨다.** 5판은 18행에 5·11·2였다. 6라운드가 셋을 고쳤다 —
> **R17이 RED 오늘이 아니다**(「실행자를 죽인다」가 오늘 불가능하다),
> **R18·R19가 없었다**(결정 11-1의 금지와 `spec.md:367` Scenario를 아무도 안 지고 있었다).
> 위 수는 **적은 게 아니라 센 것**이다 — 표의 행을 파싱해서 얻었다.
>
> **7판이 또 바꿨다 — 이번엔 줄었다.** 3.1a의 셋을 판정하면서 **R10을 지웠다**(P10):
> 지키려던 성질을 a099가 이미 지고 있었고, 남는 조각은 요구한 결정이 없었다.
> **R5·R9는 관측을 조였다**(P4) — 둘 다 spec이 이미 SHALL로 들고 있는데
> **R이 그 절반만 보고 있었다.** P5가 요구한 R은 a098이 아니라 **a099 R26**으로 갔다 —
> 그 사건이 건드리는 함수를 **a099가 편집하기 때문**이다.
> **번호는 안 옮긴다** — R10 자리는 비운다. a099가 R5에 쓴 것과 같은 규칙이다.

- [x] 3.1a **6라운드가 남긴 셋을 판정했다** (7판, 2026-08-11). 셋 다 **측정이 지적을 바꿨다** —
      셋 중 어느 것도 지적이 쓴 형태로는 참이 아니었고, **셋 다 다른 형태로 참이었다.**

      | # | 지적이 주장한 것 | 실측 | 판정 |
      |---|---|---|---|
      | P4 | *"빈 actor를 쓰고 통과한다"* | **거짓.** `AcknowledgeAlert`가 빈 이름을 **이미 거부하고**(`outbox.go:371-373`) `acknowledged_by`에 **이미 저장한다**(`:377`). spec `:171-172`의 SHALL은 원장 층에서 이미 만족된다 | **R5를 조였다** — 위험은 빈 actor가 아니라 **새 CLI가 고정 문자열을 넣는 것**이다. 그리고 **민감정보 쪽은 참이었다**: `alertSelect`(`:386-387`)가 `body`·`payload`를 싣고 오는데 R9는 **토큰만** 보고 있었다 → **R9도 조였다**(넷 전부) |
      | P5 | *"Scenario를 지는 R이 없다"* | **절반 거짓.** 그 Scenario(`spec.md:186`, 인용이 `:184`로 밀려 있었다)는 주장 셋이다. ① 승인이 임차에 안 막힌다 → **a099 R22**가 진다 ③ 잔재는 재무장이 지운다 → **a099 R17**이 진다 | ② *"그 발송자의 정산은 거부된다"*만 **주인이 없었다.** 오늘은 `MarkAlertDelivered`의 `state = PENDING` 술어(`outbox.go:342-343`)가 공짜로 준다 — 그런데 **a099가 그 함수에 토큰 술어를 더한다**(tasks `:18`). 회귀 핀이고 **주인은 a099다** → **a099 R26 신설** |
      | P10 | *"a099 소관이면 옮긴다"* | **옮길 것이 없었다.** 값의 충분함은 a099 **R20**, 주입·기본값은 a099 **4.1b + B7+**가 **이미** 진다. 남는 조각은 *"운영자가 조절하는 임차"*뿐이고 **그것을 요구한 결정·불변식이 없다** | **R10 삭제 · 조각은 `not-applicable`** — 근거: 프로덕션 `journal.Options{…}`(`engine.go:638`)에 노브가 없고 a099 C4의 주입은 **테스트를 위한 인스턴스 단위**다. 노브를 세우는 것은 scope creep이다 |

      > **셋이 같은 것을 가르쳤다 — 리뷰의 지적은 「어디를 볼지」이지 「무엇이 참인지」가 아니다.**
      > P4는 위험한 자리를 맞게 짚고 **기전을 틀렸다**(빈 actor가 아니라 고정 문자열).
      > P5는 R이 없다고 했는데 **셋 중 둘은 옆 change가 지고 있었다.**
      > P10은 옮기라고 했는데 **이미 그쪽에 있었다.**
      > 지적을 그대로 반영했다면 셋 다 **틀린 수정**이 됐다.

      > **⛔ 이 판정 중에 내가 만든 결함 하나를 찾았다.** 7판이 `spec.md:97`에 두 줄을 더하면서
      > **그 아래 모든 Scenario가 +2 밀렸는데 인용을 안 고쳤다.** 실측으로 넷을 고쳤다 —
      > `spec.md:365`→`:367` 셋(R19 근거 · 개수 표 주석 · a099 review), `:184`→`:186` 하나.
      > `openspec/specs/…`·`a092/…` 접두가 붙은 인용은 **다른 파일이라 안 밀렸다** — 문맥으로 갈랐다.
      > **교훈: 내용을 고치는 편집은 줄 수를 바꾸고, 줄 수는 남의 문서에 박혀 있다.**

- [ ] 3.1 **R1~R23 + R4b — R10은 빈 번호 (스물셋)** 을 RED로 작성하고 **실패를 실제로 관측한다.**
      실패 출력을 여기 붙인다. **종류 칸이 요구하는 시점에서 관측한다.**

      | 종류 | 관측 규율 |
      |---|---|
      | RED 오늘 (넷) | 지금 HEAD에서 바로 빨갛다 |
      | RED — 시점 (**열여섯**) | **그 시점을 실제로 만들어서** 본다. 안 만들면 born-GREEN이고 **「관측했다」고 적지 않는다** |
      | 회귀 핀 (**셋**) | 빨간 시점이 없다 — **그것을 그대로 적는다** |

      > **⚠ 회귀 핀 셋 중 R22는 「통과한다」로 끝내면 안 된다** (기억: 통과는 증거가 아니다).
      > R22가 재는 갈래는 **한 번도 실행된 적이 없는 코드**다(커버리지 count=0).
      > 그러므로 R22를 쓴 뒤 **그 갈래를 실제로 깨뜨려서**(예: `LIMIT` 절을 빼는 뮤테이션)
      > **R22만 빨개지고 기존 열일곱 호출은 그대로 통과하는 것**을 본다.
      > 못 만들면 R22는 아직 증거가 아니라 문장이다.

      **진행 — R16 완료 (2026-08-12).** `internal/execgw/a098_sender_down_reason_test.go`

      | 단계 | 관측 |
      |---|---|
      | **RED** | `go test ./internal/execgw/ -run TestTheSenderDown` → **exit 1 · build failed** · `undefined: execgw.ReasonAlertSenderDown` (6자리) |
      | **GREEN** | 4.3.3의 네 자리를 채운 뒤 `go test ./internal/execgw/` → **exit 0 · 341건 통과** |

      **그리고 통과만으로 끝내지 않았다 — 뮤테이션 둘을 실제로 걸었다.**

      | 뮤테이션 | 기존 골든 테스트 | **새 테스트** |
      |---|---|---|
      | **A** `latchOrder`에서만 뺀다 (열거·골든은 그대로) | — | **FAIL** — `round 3: CheckEntry reported "guardian_decision_missing"` |
      | **B** 열거에서 빼고 **골든을 재생성한다** (= 상수만 선언한 상태) | **PASS (exit 0)** | **FAIL** — 길이 29≠30 · membership 없음 |

      > **B가 이 테스트의 존재 이유를 실측으로 보여 준다.** 상수를 `reason.go`에만
      > 선언하고 열거에 안 넣으면 `TestReasonCodeEnumIsStable`은 **통과한다** —
      > 열거와 골든이 **둘 다** 그 코드를 모르기 때문이다. `failclosed.go:283-288`이
      > 적는 그 일이 실제로 재현됐다.
      >
      > **A는 「빠뜨려도 차단은 걸린다」의 반대편을 잰다.** `latchOrder`에 없어도
      > 래치는 걸리고(`retry.go:545-547`의 폴백), **보고되는 사유만 map 순회 순서**가 된다.
      > 그래서 관측을 *"래치가 걸린다"*로 쓰면 못 잡는다 — **latchOrder에도 없는 코드와
      > 짝지어 200회 반복**해야 잡힌다.

      > **⛔ 이 뮤테이션 중에 나를 한 번 물었다 — 적어 둔다.**
      > 원복을 `git checkout <file>`로 했는데 **GREEN 편집이 아직 커밋 전이었다.**
      > 그래서 checkout 이 뮤테이션과 **함께 GREEN 편집까지** HEAD 로 되돌렸고,
      > `git diff --quiet`는 **초록을 냈다** — 그 검사가 재는 것은
      > *"HEAD 와 같은가"*이지 *"뮤테이션 전과 같은가"*가 아니기 때문이다.
      > **원복 검사가 확인해 준 것이 확인하려던 것이 아니었다.**
      > 세 파일을 세어서 잡았다(`reason.go` 2 · `failclosed.go` **0** · `retry.go` **0**).

      **진행 — R1 완료 (2026-08-12).**
      `internal/app/engine/a098_the_outbox_gets_emptied_test.go` · GREEN 은 4.0.

      | 단계 | 관측 |
      |---|---|
      | **RED** | **build failed** — `undefined: alertDeliverer` · `alertDeliveryInterval` · `alertDeliveryBatch` |
      | **GREEN** | `alertdelivery.go` 신설 뒤 **4건 통과**, engine 패키지 전체 **exit 0** |

      **상한을 「유한 시간」으로 안 썼다** (B-P4). 가짜 시계에서 **주기 × 2** 안이고,
      매 `Advance` 전에 `WaitForSleepers` 로 sleeper 를 기다린다 — 안 기다리고 전진시키면
      루프가 몇 주기를 지났는지 알 수 없어 상한이 상한이 아니게 된다.

      **뮤테이션 넷을 걸었고, 그중 둘이 나를 고쳤다.**

      | # | 뮤테이션 | 결과 |
      |---|---|---|
      | C | 주기 대기를 `time.Sleep` 으로 (유한·취소 불가) | FAIL — **다만 `WaitForSleepers` 에서 걸렸다.** 주장한 이유가 아니다 |
      | **D** | `Clock.Sleep(context.Background(), …)` — **sleeper 로 등록은 되고 취소만 무시** | **FAIL — 주장한 그 자리에서**: *"Run did not return within 2s of cancellation"* |
      | E | 정산(`MarkAlertDelivered`)을 통째로 건너뛴다 | `HoldsNoLease…` **FAIL** · `NeverPublishesTwice…` **통과** |
      | (원복) | 넷 다 역편집으로 원복, `grep -c 'MUTATION'` = **0** | 4건 재통과 |

      > **C 와 D 를 함께 적는 이유.** C 만 걸었으면 *"취소 가능성을 잰다"* 로 적었을 텐데,
      > C 가 잡은 것은 **가짜 시계에 등록조차 안 되는 구현**이다. B-P4 가 경고한 형태는
      > 그것이 아니라 **등록은 되고 취소만 무시하는 것**이고, 그것이 D 다.
      > **뮤테이션 하나로는 「무엇을 잡는지」가 안 갈린다.**
      >
      > **E 가 내 주석을 고쳤다.** `NeverPublishesTwice…` 의 첫 주석은
      > *"정산을 안 알아채면 다시 보낸다"* 였는데, 정산을 건너뛰어도 **통과했다.**
      > 두 번째 발송을 막은 것은 정산이 아니라 **살아 있는 임차**다 — 주기 2s · 임차 81s
      > 이므로 다음 주기의 claim 이 같은 claimant 여도 `HeldElsewhere` 로 물러난다
      > (a099 의 `alertClaimable` 은 청구자 자신을 특별대우하지 않는다).
      > 주석을 실측에 맞췄고, **「정산이 실제로 일어났다」는 `HoldsNoLease…` 가 진다.**

      **진행 — R8 완료 (2026-08-12).**
      `internal/app/engine/a098_restart_does_not_release_the_gate_test.go` · GREEN 은 4.6.

      | 테스트 | 종류 | 관측 |
      |---|---|---|
      | `TestARestartWithAnUndeliveredCriticalAlertKeepsEntryLatched` | **RED 오늘** | 조립 직후 `Blocks()` 에 래치가 **없다** → FAIL. 4.6 뒤 PASS |
      | `TestAnEmptyOutboxLeavesTheGateUnlatchedOnRestart` | **회귀 핀** | 오늘도 통과 — 빨간 시점이 없다 |
      | `TestTheRestoreNeverClearsALatchItDidNotSet` | **회귀 핀** | 아래 ⛔ |
      | `TestTheRestoredLatchDetailCarriesNoAlertContent` | **RED 오늘** | 복원이 없으니 래치도 없다 → FAIL. 불변식 8 |

      **셋 중 둘이 오늘 통과한다 — 그것을 그대로 적는다.** 회귀 핀 둘의 값은 4.6 **뒤**에
      있다: 「미전달 수에서 유도한다」 구현을 잡는 것이 그 둘의 일이다.

      > **⛔⛔ 그런데 그 둘 중 하나가 이빨이 없었다. 뮤테이션이 그것을 찾았다.**
      >
      > `TestTheRestoreNeverClearsALatchItDidNotSet` 의 첫 판은 `buildGateway` 로
      > **조립한 뒤에** `Block` 을 걸었다. 복원은 그보다 **먼저** 끝나 있으므로,
      > `Clear` 를 부르는 구현을 넣고 돌려도 **네 테스트가 전부 통과했다**
      > (`MUT-DERIVE-EXIT=0`). **관측하려는 순서가 코드의 순서와 반대였다.**
      >
      > 고친 판은 leaf 를 직접 부른다 — 게이트를 **미리 잠근 채** 복원을 돌린다.
      > 같은 뮤테이션을 다시 걸었더니 이번엔 **FAIL** 했다
      > (`the latch disappeared while the outbox was empty`).
      >
      > | | 첫 판 | **고친 판** |
      > |---|---|---|
      > | derive 뮤테이션 | **통과 (이빨 없음)** | **FAIL** |
      >
      > **이것이 「통과는 증거가 아니다」의 정확한 형태다** — 첫 판도 초록이었고,
      > 초록만 봤으면 회귀 핀이 하나 있다고 적었을 것이다. 있는 것은 **문장**이었다.

- [x] 3.2 루프 주기를 **측정해서** 정한다. a092의 값을 인용하지 않는다 (2026-08-12)

      **잰 것** — `internal/app/engine/a098_cycle_cost_bench_test.go`.
      벤치마크라서 `make test`를 안 느리게 하고, **다시 잴 수 있다**:

      ```text
      go test ./internal/app/engine/ -run '^$' -bench 'A098' -benchtime 200x
      ```

      | 무엇 | 실측 (i9-12900H · 200회) |
      |---|---:|
      | **빈 주기 읽기** — 밀린 행 0 | **0.065 ms** |
      | 배치 읽기 `limit=1` | 0.109 ms |
      | 배치 읽기 `limit=10` | **0.091 ms** |
      | 배치 읽기 `limit=50` | 0.212 ms |
      | 배치 읽기 `limit=100` | 0.449 ms |
      | 배치 읽기 `limit=0` (전부 1000행 — `Flush`가 하는 것) | **4.011 ms** |
      | **claim → settle 왕복** (원장 쓰기 둘) | **5.584 ms / 행** |

      **읽기와 쓰기가 두 자릿수 차이다.** 100행을 읽는 것(0.449 ms)이 **한 행을
      정산하는 것(5.584 ms)의 1/12**이다. 그러므로 **배치를 정하는 것은 읽기 비용이 아니라
      쓰기 비용**이고, 주기를 정하는 것은 **빈 주기 비용**이다.

      **주기 — 측정이 주는 것은 값이 아니라 띠(band)다. 그것을 정직하게 적는다.**

      | 경계 | 어디서 오는가 | 값 |
      |---|---|---:|
      | 하한 | 빈 주기 0.065 ms 가 정상 상태 duty cycle **1/10,000 이하**여야 한다 | **≥ 0.65 s** |
      | 상한 | 주기가 사후 회수 지연(임차 81 s)에 **5% 넘게** 더하면 안 된다 | **≤ 4.05 s** |

      **띠는 `0.65 s ~ 4.05 s`이고 고른 값은 `2 s`다** — 하한의 3배, 상한의 1/2,
      기하중심(1.62 s)에 가깝다. **duty cycle 목표와 5% 는 고른 것이지 잰 것이 아니다** —
      잰 것은 0.065 ms 와 81 s 이고, 그 둘에서 띠가 나온다.

      > **⚠ a092 와 같은 값이 나왔다 — 그런데 그것은 근거가 아니라 대조다.**
      > `a092/design.md:444`가 `const alertFlushInterval = 2 * time.Second`를 적는다.
      > **위 유도는 그것을 보기 전에 끝냈다.** 같은 값이 나온 것은 확인이지 출처가 아니고,
      > 3.2 의 요구(*"a092 의 값을 인용하지 않는다"*)는 **유도가 스스로 서는 것**이지
      > 다른 값이 나오는 것이 아니다.
      >
      > **§6.5 가 여기서 할 일이 생겼다** — a092 `design.md:470-486`이
      > *"`alertFlushInterval`은 더 이상 a092 가 정할 값이 아니다 · a098 이 정한다"*라고
      > 적고 `tools/check_values.py`의 `ADOPTED_MS`에 `alertFlushInterval: 2000.0`이
      > **아직 남아 있다.** 값이 같으므로 **고칠 것은 숫자가 아니라 소유**다.

      **배치 — 이쪽은 쓰기 비용이 정한다.**

      | 배치 | 정상 경로 한 주기 원장 비용 (읽기 + 배치×5.584 ms) | 주기 2 s 대비 |
      |---:|---:|---:|
      | 10 | **56 ms** | **2.8%** |
      | 50 | 279 ms | 14% |
      | 100 | 559 ms | 28% |

      **`batch = 10`을 고른다.** 정상 경로에서 한 주기가 자기 주기의 **2.8%** 만 쓴다.
      전송이 **전부** 실패하는 최악에서는 한 주기가
      `10 × (publish timeout 10 s + 원장)` ≈ **100 s** 까지 늘어난다 — 그래서
      **4.0 의 주기 루프는 배치 도중에도 취소에 반응해야 한다**(R7·R14).

      > **⛔ 이 측정이 증명하지 않는 것.** publish 자체를 안 쟀다 — 그것은 네트워크이고
      > `DefaultPublishTimeout`(10 s)이 이미 상한을 준다. 그리고 **엔진의 다른 writer 와의
      > 경합도 안 쟀다** — WAL 에서 읽기는 안 막히지만 claim·settle 은 쓰기다.
      > 그 경합은 R4·R4b 가 **체류로** 재고, 여기서 미리 주장하지 않는다.

## 4. GREEN — 최소 구현

- [x] 4.0 **배달 경로를 `internal/app/engine`에 신설한다** (2판 · design D1.2).
      **구현 완료 2026-08-12** — `internal/app/engine/alertdelivery.go`
      (`alertDeliverer`: `Run` · `cycle` · `deliverOne` · `release`).

      > ## ⛔⛔ 「신설했다」는 「배달된다」가 **아니다** — 프로덕션 구성자가 아직 0이다
      >
      > 이 실행자를 **만드는 코드가 어디에도 없다.** 조립부(`buildGateway`)도
      > `RuntimeOptions`도 아직 이것을 모른다 — 그것이 **4.1**이다.
      >
      > **즉 지금 상태는 a098 이 `Flush` 에 대해 적은 결함과 정확히 같은 모양이다**:
      > 함수는 있고 프로덕션 호출자가 0이라 런타임에서 도달하지 않는다(§1.4).
      > 4.1·4.1b 가 끝나기 전에는 **밀린 알림은 여전히 아무도 안 보낸다.**
      > 4.0 의 체크는 「경로가 존재한다」까지만 뜻한다.

      한 주기의 형태는 행마다 다음이고, **어떤 뮤텍스도 publish 위에서 안 쥔다**:

      ```text
      PendingAlerts(ctx, batch)
        → ClaimAlertByID(ctx, id, claimant)        # a099 C3. lease를 안 넘긴다
             HeldElsewhere → 건너뛴다 (오류가 아니다)
             Settled       → 건너뛴다
             Acquired      → Token을 들고 아래로
        → Publisher.Publish(ctx, msg)
        → 성공: MarkAlertDelivered(ctx, id, token)   # 정산과 함께 임차를 푼다
             그 결과가 LeaseLost/AlreadySettled면 → ⛔ 해제하지 않는다
        → 실패: MarkAlertAttemptFailed(ctx, id, token, cause)   # 임차를 유지한다
        → 이 주기에서 그 행을 포기할 때만: ReleaseAlertClaim(ctx, id, token)
      ```

      > **⛔ 2판의 「어느 쪽이든 끝나면 해제」를 지웠다.** a099 design D3·C5가 셋을
      > 가른다. 무조건 해제는 두 규칙을 동시에 깬다 —
      > **성공은 이미 `MarkAlertDelivered`가 풀었고**(두 번 푸는 것은 무의미하다),
      > **publish 성공 + 정산 실패는 안 푸는 것이 규범이다**(a099 spec).
      > 거기서 풀면 다음 주기가 같은 행을 다시 보내 **2026-08-08의 폭풍이 성공 경로로
      > 돌아온다.**
      >
      > **결과 타입을 문자열 오류로 다루지 않는다** — `ClaimResult.Disposition`과
      > `SettleOutcome`으로 분기한다 (a099 C3)

      **`Notifier.Flush`를 부르지 않는다.** `internal/obs`는 한 줄도 안 건드린다 —
      `obs.Publisher` 인터페이스와 a099의 journal API만 쓴다.
      `gateway.go:85`의 `publisher obs.Publisher`와 `in.journal`이 이미 그 자리에 있다
- [ ] 4.0b 한 주기 배치 크기를 §3이 잰 값으로 정한다 — **`batch = 10` · 주기 `2 s`** (3.2).
      **잠금을 쥔 채 여러 publish를 하지 않는다** — 그것이 안 D를 고른 이유다.

      - **이 값이 `PendingAlerts` B1의 참 갈래를 처음 켠다.** 오늘 호출 열일곱이 전부
        `0`이고 커버리지 count도 `0`이다 — **R22가 그 갈래를 고정하고 R23이 루프 쪽을 진다**
      - **주기 대기는 취소 가능해야 한다**(R7). 그리고 최악에서 한 주기가 100 s 까지
        늘 수 있으므로 **배치 도중에도** 취소에 반응해야 한다(3.2의 마지막 표)
- [ ] 4.1 배달 실행자를 **`RuntimeOptions.Auxiliary`에 등록한다 — `Loops`가 아니다**
      (사용자 결정 9-2 · design D8.3).

      > **⛔⛔ 3판은 `SupervisedLoop`로 등록하라고 적었다. 결정 9-2가 뒤집었다.**
      >
      > | | 3판 | **4판** |
      > |---|---|---|
      > | 등록 자리 | `RuntimeOptions.Loops` | **`RuntimeOptions.Auxiliary`** (신설) |
      > | 다른 루프를 안 내리는 법 | `Runtime.Run`에 **정지 정책 한 갈래**를 더한다 | **`stops` 채널에 아무것도 안 보낸다** |
      > | 승인된 정본 | *"루프 집합은 셋"*·*"루프가 반환하면 전체 정지"*를 **바꾼다** → `MODIFIED` 필요 | ~~안 바꾼다~~ → **6판이 뒤집었다: `MODIFIED` 하나를 쓴다** |
      >
      > **뒤엣것이 더 적은 편집으로 더 강한 성질을 준다.** 판정으로 피하는 것은
      > 판정이 틀리면 무너지고, 채널에 안 보내는 것은 **틀릴 판정이 없다.**
      > 그 성질은 6판에도 그대로다 — 바뀐 것은 **정본을 안 건드린다는 주장**뿐이다.
      >
      > **⛔ 6판 — 사용자 결정 10-1. 위 표의 마지막 칸이 거짓이었다.**
      > 4판은 정본이 못 박은 집합이 안 바뀐다는 데서 힘을 얻었는데, **그 집합이
      > 프로덕션과 이미 다르다** — 정본 셋(`openspec/specs/engine-safety/spec.md:170`)
      > 대 프로덕션 넷(`cmd/tossctl/engine.go:377-398`, 5라운드 A-P1·B-P2).
      > 그래서 빌리지 않고 **고친다** — `specs/engine-safety/spec.md`의
      > `## MODIFIED Requirements`. 그 작성은 **4.1b**다.

- [ ] 4.1b 승인된 「엔진 런타임 수명주기」 `MODIFIED`가 **네 가지를 다 담았는지** 확인한다
      (사용자 결정 10-1 · design D8.1). **문서 task이고 Go 편집이 아니다.**

      | 담아야 하는 것 | 왜 |
      |---|---|
      | 열거가 **넷** (`strategy-entry` 포함) | 착지한 코드가 이미 넷이다. 셋으로 두면 다음 편집이 셋을 인용한다 |
      | **보조 실행자** 개념 + 런타임이 지는 셋 (대기·`recover`·관측) | 셋 중 하나라도 빠지면 「감독 밖」 = 「아무도 안 본다」 |
      | 방어적 종료의 주어가 **감독 루프** | 주어가 *"루프"*면 보조 실행자가 그 대상으로 읽힌다 |
      | Scenario 「프로덕션 감독 루프 집합은 넷이다」가 **집합 동일성**을 요구 | 부분 일치는 다섯 번째가 늘어도 초록이다 — 저장소에 그 거짓 초록이 실재한다(§5.2c) |

      세 자리를 함께 편집한다 (예측은 `internal-app-engine--runtime.run`·`--newruntime` 번들):

      | 자리 | 편집 | 편집 후 AST 예측 |
      |---|---|---|
      | `RuntimeOptions` | `Auxiliary []AuxiliaryExecutor` 필드 | 구조체 — 분기 없음 |
      | `NewRuntime` | 이름 공백·중복·`Run` nil 거부. **중복 판정은 `seen`을 감독 루프와 공유한다** | 분기 12 → 는다. **B1과 B4~B12는 안 바뀐다** |
      | `Runtime.Run` | B3 뒤 `range` 하나. **새 `Wait` 호출은 없다** | **분기 4 → 5 · 이탈 3 그대로.** 이탈이 늘면 정지 정책을 건드린 것이다 |

      - **⛔ 보조 goroutine은 기존 `wg`에 넣는다 — 새 WaitGroup을 안 만든다** (design D8.3).
        `wg.Add(1)`/`defer wg.Done()`는 감독 루프와 **똑같이** 쓰고 **`stops`에만 안 보낸다.**
        그러면 `wg.Wait()`(`runtime.go:300`)가 **이미** 그들을 기다린다 —
        원장 close 계약(`runtime.go:258-260`)을 **다시 구현하지 않고 물려받는다.**

        > **4판 첫 초안은 여기에 `auxWG`를 새로 만들라고 적었다. 되돌렸다.**
        > 새 WaitGroup은 **새 `Wait` 호출 = 잊을 수 있는 자리**를 하나 더 만든다.
        > **위험이 사라졌다고 적지 않는다** — `wg.Add(1)`을 빼먹으면 여전히 깨지고
        > **R14가 그것을 잡는다.** 줄어든 것은 잊을 수 있는 자리의 수다.
        >
        > 채널 산술은 그대로다: `stops` 버퍼가 `len(Loops)`(`:275`)이고 **보내는 쪽은
        > 여전히 감독 루프뿐**이므로 `first := <-stops`(`:297`)에 보조 실행자가
        > 절대 안 나타나고 `drain(stops)`(`:302`)도 안 막힌다
      - **`recover`는 런타임의 보조 launcher 안에 한 번 쓴다** — 실행자가 아니라.
        실행자를 쓰는 쪽은 그것을 **뺄 수 없다**. **여기도 0이 아니라 한 자리이고 R15가 잡는다**
      - **`Health`는 안 붙인다.** `superviseHealth`(`:290`)가 `Loops`만 훑으므로
        보조 실행자에게는 올라갈 사다리가 없다.
        **대가는 design D8.4가 적고 「안 하는 것」 표로 넘긴다**

      > **⚠ `Run`은 ctx 취소에 반드시 반응한다 — 주기 대기는 취소 가능한 sleep이다**
      > (a092 7.4.3이 19판 4차에 넘겨준 P).
      >
      > `Runtime.Run`의 종료 순서는 `first := <-stops` → `cancel()` →
      > `close(supervised)` → **`wg.Wait()`(`internal/app/engine/runtime.go:300`)**이고,
      > 배달 루프의 goroutine이 그 `wg`에 들어간다.
      >
      > > **⚠ 이 박스의 첫 판(19판 4차)은 기전을 틀리게 적었다 — 19라운드 B-P6.**
      > > *"취소 불가능한 sleep으로 쓰면 `wg.Wait()`가 **영원히** 안 돌아온다"*고
      > > 썼는데 **틀렸다.** 유한한 `time.Sleep(d)`은 끝나고, 그다음 루프가 ctx를
      > > 보면 반환한다. **결론(종료 지연)은 맞고 근거가 틀렸다** — 18라운드
      > > A-P4·A-P6·B-P12와 같은 형태다. 정확한 두 갈래는 아래와 같다.
      >
      > | 배달 루프가 하는 일 | `Runtime.Run`의 결과 |
      > |---|---|
      > | ctx를 **한 번도 안 본다** | `wg.Wait()`가 **안 돌아온다.** 엔진이 종료하지 못한다 |
      > | ctx는 보지만 주기 대기가 **취소 불가능**하다 | 종료가 **최대 한 주기**(`alertFlushInterval`)만큼 늦는다 |
      > | 주기 대기가 **취소 가능**하다 | 즉시 반환 — 이것이 요구다 |
      >
      > **그래서 R7은 「유한 시간에 반환」으로 쓰면 안 된다.** 유한한 sleep도
      > 유한 시간이므로 그 계약은 나쁜 구현을 통과시킨다(B-P6). **상한을
      > 단언한다** — 가짜 시계로 `alertFlushInterval` 안에 반환할 것.
      >
      > 첫째 줄이 더 무겁다: 엔진이 못 죽는 것은 알림 문제가 아니라 **정지 문제**다.
      >
      > **R2와 정반대의 실패 모드이고, 둘이 같은 한 줄에서 만난다.**
      > R2는 *"루프가 반환해도 엔진이 산다"*를 지고, 이것은
      > *"루프가 반환하지 않으면 엔진이 못 죽는다"*다. 19판이 배달 루프를 a098로
      > 분리할 때 **근거 산출물(`internal-app-engine--runtime.run`의
      > 「Safety conclusion」)은 a092에 남았고**, 그 산출물이 적어 둔 두 결합 중
      > **하나만 여기로 건너왔다.**
      >
      > 정지하지 못하는 엔진은 사람이 손절 배선을 고치러 들어갈 수 없다.
- [ ] 4.2 **`OnStop`이 죽음을 관측한다** (design D8.3). 결정 9-2가 *"죽음을 관측할 주체를
      따로 만들어야 한다"*를 대가로 지목했고, 답은 **실행자 자신의 goroutine 안 `defer`**다 —
      감시 루프를 새로 만들지 않는다.

      | 반환 사유 | `OnStop`이 하는 일 |
      |---|---|
      | **취소** — `gracefulStop(loopCtx, err)`로 판정 | **아무것도 안 한다** — R13 |
      | 그 밖의 반환 | 4.3·4.3.2·4.3.3 (**4.3.1은 결정 11-1로 삭제됐다**) |
      | **패닉** | `recover`로 잡아 위와 같게 다룬다 — R15 |

      > **⛔ `gracefulStop`에 넣는 것은 부모 `ctx`가 아니라 `loopCtx`다** (5라운드 A-P2).
      > `gracefulStop`은 **넘겨받은 ctx의 `Err()`**를 보고(`runtime.go:341`),
      > 감독 루프 하나가 실패하면 **부모는 그대로인 채 `loopCtx`만 취소된다**(`:298`).
      > 부모를 넣으면 **아무 잘못도 없는 실행자의 정상 반환이 죽음으로 오인되어**
      > 게이트가 `ReasonAlertSenderDown`으로 잠긴다

      > **`recover`가 없으면 R2가 패닉 경로에서 거짓이다.** 배달 실행자의 패닉은
      > 프로세스를 죽이고, 그것은 **손절 루프까지 죽인다**(불변식 4).
      > 남는 잔재(집힌 채 버려진 임차)는 a099의 만료가 지운다.
- [ ] 4.3 정지 시 `EntryGate.Block`을 **직접** 부른다.
      `EscalateOperatingMode`에 기대지 않는다 (design D2의 인용)
- [x] **4.3.3 그 `Block`은 자기 사유 코드를 쓴다** (4판 신설 — 사용자 결정 8-1 · design D8.2).
      **오늘 없던 여섯 번째 차단 트리거이고, 그 사실을 숨기지 않는다.**
      **넷 다 채웠다 (2026-08-12) — 문자열은 `critical_alert_sender_down`.**

      | 자리 | 편집 | 왜 | 실측 |
      |---|---|---|---|
      | `internal/execgw/reason.go` | 상수 하나 선언 | 오늘 다섯 자리가 전부 `ReasonAlertUndelivered` 하나를 쓴다(`notifier.go:262`·`:379`·`:404`·`:482`·`:511`) | 상수 45 → **46** |
      | `internal/execgw/failclosed.go` `AllReasonCodes` | 리터럴에 원소 하나 | **빠뜨려도 컴파일이 통과한다** — `failclosed.go:283-288`의 전례 | 열거 29 → **30** · 분기 **0 그대로** |
      | `internal/execgw/retry.go` `latchOrder` | append | 안 넣어도 차단은 걸린다(`retry.go:545-547`의 폴백). **순서만 비결정적**이 된다 | 뮤테이션 A가 그 비결정성을 **실제로 냈다** |
      | `testdata/reason_codes.golden` | `TOSSOS_UPDATE_GOLDEN=1`로 재생성 | `TestWriteReasonCodeGolden`이 **유일한 정규 경로**다 | 29줄 → **30줄** |

      > **⛔ `latchOrder`에 「append」라고 적혀 있는데 맨 뒤에 안 넣었다 — 이유를 적는다.**
      > 그 목록의 마지막 원소 `ReasonOperatingModeBlocked`에는
      > *"the operating mode reads last **on purpose** as well as by convention"*
      > (`retry.go:596-602`)이 붙어 있다. 맨 뒤에 넣으면 **그 문장을 깬다.**
      >
      > **「append, never insert」 규칙이 지키는 것은 「기존 쌍의 상대 순서」다**
      > (`retry.go:581-583`: *"the relative precedence of the original three is itself
      > a contract"*). 운영 모드 **앞**에 넣으면 **어떤 기존 쌍도 안 움직인다** —
      > 새로 생기는 관계는 `sender-down > mode` 하나뿐이다. 그래서 규칙의 **글자**가
      > 아니라 **지키는 것**을 따랐고, 그 판단을 코드 주석에 남겼다.
      >
      > 자리의 근거: 다른 알림 사유들은 *"보내려다 실패했다"*라 더 구체적인 답이고,
      > 이것은 *"아무도 안 보내고 있다"*이다. 그래서 알림 사유들 **뒤**, 모드 **앞**이다.

      > **⚠ 이 task가 `internal/execgw`에 파일을 하나 더 만들었다 — 5.2의 표를 넘는다.**
      > `a098_sender_down_reason_test.go`(신규 테스트)이고, **5.2가 세는 네 파일은
      > 프로덕션 쪽**이다. 테스트가 없으면 R16이 없고 R16이 없으면 이 트리거는
      > 아무도 안 지는 요구가 된다. **5.2의 표에 다섯째 줄로 적었다** — 침묵한 초과 금지.

      > **같은 코드를 쓰면 운영자의 승인이 그것을 지운다.** `Acknowledge`는 미전달 수가
      > 0이면 `ReasonAlertUndelivered` 래치를 푼다(`notifier.go:507-512`).
      > 실행자가 죽은 채 운영자가 밀린 행을 전부 승인하면 **「보낼 주체가 없다」는 차단도
      > 함께 풀린다** — 보낼 주체는 여전히 없는데 진입만 열린다. R17이 그 자리다.
      >
      > **이 차단을 자동으로 푸는 경로는 안 만든다.** 죽은 실행자는 안 되살아나므로
      > 복구는 재시작이고, 새 프로세스에는 산 실행자가 있다.
      > 위 4.6의 *"재시작이 우회로가 되면 안 된다"*와 **대상이 다르다** — design D8.5.
- ~~**4.3.1 정지 시 운영 모드를 `ENTRY_BLOCKED`로 승격한다**~~ — **삭제된 task다.
  체크박스를 안 남긴다** (완료로 읽히면 안 하기로 한 일이 한 일이 된다).
  사용자 결정 11-1, 2026-08-11 · design D8.5a. 19판 4차가 B-P5로 만든 task다.
  아래가 지운 이유이고, 지우면서 무엇을 잃는지도 같이 적는다.

      | | 하면 | **안 하면 (6판)** |
      |---|---|---|
      | `internal/journal` diff | `AutomaticTriggers`·`TargetModeForTrigger` **두 자리** | **비어 있다** — §5.2의 선언이 참으로 남는다 |
      | 승인된 `risk-management` | **`MODIFIED` 필요** (`:88`의 여섯에 sender-down 없음) | **불필요** |
      | 「복구는 재시작」(4.3.3) | **거짓이 된다** — 모드는 durable·완화는 사람 승인(`:102-108`) | **참** |
      | 미전달 행 0인데 실행자만 죽었던 재시작 | **막힌 채 — 사람 승인 전까지** | **열린다** — 새 프로세스의 실행자는 살아 있다 |

      > **19판이 이 task를 만든 논거는 여전히 참이다.** `EntryGate.Block`은 `g.mu`와
      > 맵뿐이고 원장에 안 닿아(`internal/execgw/retry.go:498-505`) **재시작이 래치를
      > 지운다.** 운영 모드는 `tx.Commit()`(`operating_mode.go:468`)으로 남는다.
      >
      > **틀린 것은 논거가 아니라 결론이다.** 19판이 막으려던 것 —
      > *"미전달 critical 알림이 그대로인데 신규 진입이 다시 열린다"* — 은
      > **4.6의 기동 복원이 이미 막는다.** 실행자의 죽음이 남긴 것은 **원장의
      > 미전달 행**이고, 4.6이 그 행을 보고 잠근다. 19판은 **이미 막힌 구멍을
      > 두 번째 수단으로 다시 막고 있었고**, 그 수단의 대가가 위 표의 3·4행이다.
      >
      > **「직접 건다」와 「모드를 승격하지 않는다」는 다른 말이다** — 19판이 그것을
      > 옳게 가렸다. 6판은 앞엣것(4.3)을 그대로 두고 **뒤엣것을 고른다.**
      > design D2의 *"`EscalateOperatingMode`에 기대지 않는다"*는 유효하다.

      > **a092가 같은 조항을 SHALL로 들고 있다** (`a092/specs/engine-safety/spec.md:126-130`).
      > **a098은 그것을 안 진다** — 사용자 결정 12-2. 인계는 a099 §7.5이고,
      > a092가 자기 조항을 정리하기 전에는 **두 델타가 서로 반대를 적고 있는 상태**다.
      > 그 사실을 숨기지 않는다.
- [ ] **4.3.2 죽은 실행자를 자동으로 되살리지 않는다** (19판 4차 신설 — B-P5).
      a092 spec의 다섯째 조항이다. **자동 재시작은 배달 실행자를 둘로 만들 수 있고**,
      둘이면 같은 행을 두 번 집는다 — a096이 막은 이중 발송의 전제가 되살아난다.
- [ ] 4.4 **운영자 표면 — `tossctl` 하위 명령 둘** (사용자 결정 4, 2026-08-11 · design D7).

      | 명령 | 무엇 | 배선 |
      |---|---|---|
      | 목록 | 밀린 critical 알림 — id · 나이 · 시도 수 · **임차 보유자와 임차 나이** | `PendingAlerts` (a099 4.10의 투영) |
      | 승인 | 행 하나를 ACKNOWLEDGED로 | **엔진 프로세스 안의 `obs.Notifier.Acknowledge`** — 오늘의 경로 그대로 (design D7.1) |

      - **⛔ 표면은 `httpapi`가 아니라 엔진 소유 Unix 소켓이다 (design D7.1 · 4라운드 A-P9).**
        3판은 *"`httpapi`가 엔진과 같은 프로세스인지 실측하고, 같으면
        `httpAPIMutationRoutes`(`httpapi.go:170`)에 승인 라우트를 더한다"*고 적었다.
        **그 명령의 자기 설명이 이미 금지하고 있었다** (`httpapi.go:59-66`):
        *"The service has no order, LIVE, **gate**, kill-switch, engine start/stop … route
        and **never starts the engine**."* 「나중에 확인한다」가 아니라 **불가능**이었다.
        선례는 `internal/app/engine/position_policy_runtime_transport_unix.go`
        (`:46`·`:63`·`:67-68`·`:72`)와 `internal/positionpolicyrpc`다 —
        접근 제어는 **파일시스템 권한**이고, 엔진이 없으면 승인은 **명확히 실패한다**
      - **원장만 고치고 끝내면 안 된다.** `EntryGate`는 엔진 메모리 안의 맵이고
        (`execgw/retry.go:495-510`), CLI가 원장만 바꾸면 **운영자가 승인해도 진입은
        막힌 채**로 재시작까지 간다. 그것은 운영 표면이 아니다
      - **콘솔 UI는 안 건드린다.** 타이핑 확인·추가 승인 마찰을 넣지 않는다
        (사용자 지시 2026-07-27)
      - 운영자 이름은 CLI가 만든다 — `AcknowledgeAlert`가 **공백 아닌 이름을 요구한다**
        (`outbox.go:371`)
      - **토큰 원문은 출력하지 않는다** (불변식 8)
      - 이 명령이 `cmd/`를 건드리는 **유일한 이유**다. 5.2의 diff 확인이 그것을 반영한다

      > ## ⚠⚠ 19판까지 이 한 줄이 설계되지 않았다 (19라운드 B-P8) — 이제 채웠다
      >
      > `proposal.md:89`는 *"console이냐 CLI냐는 design D2가 고른다"*고 적고,
      > **D2는 그 선택을 하지 않는다** — 실행자 사망과 게이트 동작만 논한다.
      > `design.md:66`은 §6 RED로 미루는데 **그 §6이 없다.** 세 문서가
      > 서로를 가리키고 아무도 안 고른다.
      >
      > **정해지지 않은 것을 열거한다** — 패키지 · API 형태 · 명령 이름 ·
      > 권한 경계 · 운영자 신원 · 승인이 무엇에 배선되는가.
      >
      > **이것이 왜 R5보다 무거운가.** a092 R17-11이 `Acknowledge`를 **직접 부르는**
      > 회귀 핀으로 강등됐고(19라운드 A-P6), 그 결정의 근거가
      > **"진짜 결함은 a098 R5가 진다"**였다. R5가 표면 없이 남으면
      > **`ReasonAlertUndelivered` 래치를 푸는 경로가 어느 change에도 없다** —
      > 오늘 그 래치가 걸리면 프로세스 재시작이 유일한 해제 수단이고, a098이
      > 배달 루프를 세우면 **래치가 걸릴 확률이 오른다.**
      >
      > **사용자 지시 하나가 이 설계를 이미 한정한다** (2026-07-27):
      > **콘솔 UI에 타이핑 확인·추가 승인 마찰을 넣지 않는다.** R6이 그것을 지고,
      > 표면을 고를 때 그 제약이 먼저다.
      >
      > **표면을 고르기 전에는 4.4를 실행할 수 없었다.** 사용자 결정 4가 그 빈칸을
      > 채웠고, 위 표가 그 결정이다.
- [x] **4.6 기동 직후 진입 게이트를 복원한다 — `Block`만** (design D6 · a099 C6 ·
      사용자 결정 5-1). **구현 완료 2026-08-12.**

      | 무엇 | 값 |
      |---|---|
      | ~~자리~~ | ~~**`RuntimeOptions.Recover` 콜백** (`runtime.go:160` 필드, 호출 `:267`)~~ |
      | **자리 (구현)** | **`buildGateway`** — `tracker.Restore` 바로 뒤 (`gateway.go`). 신설 leaf `restoreAlertEntryLatch` |
      | 판정 | 미전달 critical 수가 0보다 크다 |
      | 행동 | **`Block`만 건다.** `Clear`는 **안 한다** |

      > **⛔⛔ 자리를 바꿨다 — 승인된 task 와 다르므로 근거를 실측으로 적는다.**
      >
      > | # | 사실 | 근거 |
      > |---|---|---|
      > | 1 | 이 함수는 **어떤 루프보다도 먼저** 끝난다 — 조립이고 루프는 `Runtime.Run` 이 띄운다 | `engine.go:490` · `runtime.go:277-283` |
      > | 2 | **같은 결함을 고친 선례가 이 함수 안에 있다** — `tracker.Restore` 가 RECONCILE 래치를 원장에서 재건한다. 주석: *"without this call a restart silently clears every block a disagreement raised"* | `gateway.go:216-227` |
      > | 3 | 원장과 게이트가 **둘 다 이 함수 안에** 있다. `Recover` 클로저(`cmd/tossctl/engine.go:373`)에는 게이트가 없다 | `gateway.go:214` |
      > | 4 | **복구 시퀀스가 이 래치를 안 지운다** — `recovery.Run` 의 `Clear` 는 `ReasonRecoveryIncomplete` **하나뿐** | `internal/reconcile/recovery.go:289` |
      >
      > **design 이 준 이유를 안 어긴다.** D6 가 `Recover` 를 고른 논거는
      > *"첫 tick 은 루프가 이미 도는 뒤라 그 사이에 진입이 열린다"* — 즉 **3판의
      > 「루프 첫 tick」을 기각하는 논거**이지 `Recover` 를 유일한 자리로 지목하는
      > 논거가 아니다. 조립은 그 논거를 **더 강하게** 만족한다(1).
      >
      > **4가 없었으면 이 자리를 못 골랐다.** 복구가 게이트를 통째로 비웠다면
      > 조립 시점 래치는 `Recover` 가 도는 사이에 사라졌다. 추측이 아니라 읽어서 확인했다.
      >
      > 산출물: `analysis/function-logic/internal-app-engine--buildgateway`
      > (**분기 4 → 5 · 이탈 6 → 7**, 예측대로. 호출은 19 를 예측했는데 **20** —
      > error-wrap 의 `fmt.Errorf` 를 안 셌다. 틀린 것을 적어 뒀다).

      > `EntryGate.Block`은 메모리 래치이고 **재시작이 지운다**
      > (`execgw/retry.go:498-505`). 원장에 미전달 critical 행이 남았는데 프로세스는
      > 그것을 모른 채 진입을 연다 — **재시작이 게이트를 푸는 우회로**다.
      >
      > **⛔ 3판은 이것을 「루프의 첫 tick」에 두고 「유도」라고 적었다. 둘 다 고쳤다.**
      > 첫 tick은 **루프가 이미 도는 뒤**라 그 사이에 진입이 열린다. `Recover`는
      > *"모든 루프보다 먼저, 완주해야 한다"*고 `runtime.go:155-160`이 적는다.
      > 그리고 「유도」는 **푸는 쪽까지 포함**해서 승인된 정본
      > `openspec/specs/engine-safety/spec.md:147-152`보다 덜 보수적이다 — 3라운드 A-P1.
- [ ] **4.7 임차 기간을 살아 있는 설정에서 계산해 주입한다** (design D6 · a099 C4).
      `bound`를 `Notifier.Attempts`·`RetryDelay`·`Ntfy.Timeout`·`BusyTimeout`에서
      계산하고 `Journal.Options.AlertLease`로 넘긴다.
      **a099는 기본값에 대한 단언까지만 진다**(a099 R20) — 살아 있는 주입이 여기다
- [ ] 4.8 R1~R23 + R4b — R10 제외 (**스물셋**)이 GREEN

## 5. VERIFY

- [ ] 5.1 `go test ./...` — **반드시 `-timeout 30m`을 붙인다** (`Makefile:35`와 같게).

      > **⛔ 5라운드 B-P1이 여기서 걸렸다.** `-timeout` 없이 부른
      > `go test ./internal/journal/... -count=1`이 **기본값 10분에 걸려 실패**했다.
      > 직렬 재측정은 **593.257초** — 기본값 600초에 **7초** 남았다(4라운드에는 305.9초였다).
      > **a099가 이 패키지에 v31 마이그레이션과 열 넷과 새 테스트를 더한다.**
      > `make`는 30분이라 안 깨지지만 **임시 실행은 이제 뒤집힌다.**
- [ ] 5.2 **`internal/obs`·`internal/journal`·`internal/risk`의 diff가 비어 있음을 확인**
      — D3의 Pre-Edit 선언이 지는 계약.

      | 패키지 | diff | 왜 |
      |---|---|---|
      | `internal/obs` | **비어 있어야 한다** | 결정 5-1이 다섯 게이트 자리를 그대로 두라고 했다 |
      | `internal/journal` | **비어 있어야 한다** | a099가 지는 표면이다 |
      | `internal/risk` | **비어 있어야 한다** | — |
      | `internal/execgw` | **⛔ 비어 있지 않다** — 파일 셋 (`reason.go`·`failclosed.go`·`retry.go`) + golden 하나 | 4.3.3 (사용자 결정 8-1). **3판의 "execgw 전부 안 건드린다"는 철회했다** |
      | `internal/app/engine` | 비어 있지 않다 | 4.0·4.1·4.2·**4.6** (`gateway.go` — `buildGateway` 한 갈래 + 신설 leaf) |
      | `cmd/tossctl` | 비어 있지 않다 | 4.4의 운영자 표면 (design D7) |

      **`internal/execgw`의 프로덕션 diff는 위 네 파일을 넘으면 안 된다.** 특히
      `checkAccountEntryLocked`(`retry.go:537`)와 `EntryGate.Block`/`Clear`
      (`retry.go:497`·`:510`)의 본문은 **0줄**이어야 한다 — `latchOrder`는 그 함수가
      **읽는 변수**이고 변수의 append는 함수 편집이 아니다.

      | 다섯째 줄 — 테스트 | 왜 표에 적는가 |
      |---|---|
      | `internal/execgw/a098_sender_down_reason_test.go` (신규) | **위 네 파일은 프로덕션 쪽이다.** R16이 이 패키지의 심볼 넷을 재므로 테스트도 여기 산다. 표에 안 적으면 diff 확인이 *"네 파일을 넘었다"*로 실패하고, **그때 고르는 답이 「테스트를 지운다」가 될 수 있다** |

      > **⛔ 실측으로 확인할 것 — 이 다섯 파일 밖의 `internal/execgw` diff는 0이어야 한다.**
      > 4.3.3 시점 실측: `reason.go`(+상수 1) · `failclosed.go`(+리터럴 1) ·
      > `retry.go`(+`latchOrder` 1) · `reason_codes.golden`(+1줄) · 새 테스트 1.
      > `checkAccountEntryLocked`·`Block`·`Clear` **본문 0줄** — `git diff`로 확인한다
- [ ] 5.2b **`AllReasonCodes()`의 길이가 정확히 하나 늘었음을 잰다** (29 → 30).
      golden 대조만으로는 부족하다 — 상수를 `reason.go`에만 선언하고 열거에 안 넣으면
      **golden이 안 깨진다**(`internal-execgw--allreasoncodes` 번들이 그 함정을 적는다).

      > **⚠ 이 측정이 증명하지 않는 것을 적어 둔다** (5라운드 A-T2).
      > `reason.go`는 상수를 **45개** 선언하는데 `AllReasonCodes()`는 **29개**만 등록한다 —
      > **16개가 이미 빠져 있다.** 즉 이 함수의 doc(*"every reason code this build can emit"*)은
      > 지금도 거짓이다. **5.2b는 「새 코드가 등록됐다」만 증명하고 「열거가 완결됐다」는
      > 증명하지 않는다.** 남은 16개는 a098의 범위가 아니다 —
      > **「안 하는 것」 표에 이름을 붙여 둔다**
- [ ] 5.2c **`LoopNames()`가 오늘과 같은 넷을 낸다** (R12의 GREEN) —
      `reconcile`·`exit`·`filldetect`·`strategy-entry`.
      기존 핀 `TestProductionRuntimeIncludesOneDormantStrategyEntryOuterLoop`
      (`cmd/tossctl/engine_strategy_entry_dormant_test.go:50`)이 **안 깨져야 한다.**
      배달 실행자의 이름이 거기 나타나면 **결정 9-2가 지켜지지 않은 것**이다.

      > **⚠ 이 넷이 `MODIFIED`가 적는 넷과 같아야 한다** (사용자 결정 10-1 · 4.1b).
      > 5판까지 이 확인은 *"정본은 셋인데 코드가 넷이다"*를 **그대로 두는** 확인이었다.
      > 6판부터는 **정본도 넷**이므로, 이 자리에서 둘이 갈리면 그것은 drift가 아니라
      > **이 change의 결함**이다.
      >
      > **저장소의 거짓 초록 하나를 여기서 다시 이름 붙인다** — 5라운드 B-T3.
      > `TestTheLoopSetIsTheSpecifiedThree`(`cmd/tossctl/engine_test.go:347-361`)은
      > **소스 문자열 포함만** 검사해 **루프가 넷이어도 통과한다.** 그 테스트를
      > 5.2c의 근거로 **쓰지 않는다** — 근거는 위의 정확 비교 핀이다
- [ ] 5.3 exit 사이클 체류 무변화 실측 (R4의 GREEN 값)

## 6. Gate

- [ ] 6.1 `make sdd-sync`
- [ ] 6.2 `make sdd-check`
- [ ] 6.3 `make gate CHANGE=a098-nobody-sends-what-the-outbox-keeps`
- [ ] 6.4 독립 리뷰
- [ ] 6.5 **a092의 D0.10 표와 non-goal이 a098을 가리키는지 재확인** —
      두 change가 같은 일을 청구하면 그것이 16라운드 B-8이 잡은 형태다

      **19판 3차가 이 확인에 세 가지를 더했다.**

      1. **a092 §6.11의 시나리오 지도가 a098을 다섯 행에서 가리킨다.** 그중
         「배달 실행자가 죽는다」는 **a092에 대응 RED가 없다** — a098의 R2·R3이
         그 Scenario의 유일한 증거다. 같은 일을 둘이 청구하는 것보다
         **어느 쪽도 안 지는 것**이 나쁘고, 이 행이 그 위험이 있는 자리다.

         > **⛔ 6판 — 그 매핑이 이제 부분적이다** (사용자 결정 11-1·12-2).
         > a092의 「배달 실행자가 죽는다」는 **다섯 조항**인데 a098이 지는 것은
         > **넷**이다. 남은 하나(*"운영 모드를 `ENTRY_BLOCKED`로 승격한다"*)는
         > **a098이 안 진다.** §6.5는 그 하나가 a092 쪽에서 **누구 것인지**를
         > 확인해야 하고, 답이 「아무도」면 그것을 **그대로 적는다.**
         > 표제만 맞춰 22 = 22를 통과시키는 것이 19라운드 B-P5가 잡은 형태다.
      2. **`alertFlushInterval`을 아직 a092가 채택값으로 선언한다**
         (`a092/tools/check_values.py`의 `ADOPTED_MS`). a098 3.2가 그 주기를
         재서 정하므로, **재고 나면 a092에 알려 10.4.5를 닫는다.**
         그 전까지 두 change가 같은 상수를 정하고 있다.

         > **✅ 쟀다 (2026-08-12, 3.2). 그리고 값이 같다 — `2 s`.**
         > 그래서 이 항목이 **더 쉬워진 게 아니라 더 어려워졌다.** 값이 달랐다면
         > 누가 이겼는지가 diff 로 보인다. 같으면 **아무것도 안 고쳐도 둘 다 맞아 보인다** —
         > 그런데 소유는 여전히 하나여야 한다(a092 `design.md:470-486`이
         > *"a098 이 정한다"*라고 이미 적었다).
         >
         > **그러므로 §6.5 가 확인할 것은 숫자가 아니라 선언이다**:
         > `a092/tools/check_values.py`의 `ADOPTED_MS` 에서 `alertFlushInterval: 2000.0`
         > 항목이 **빠졌는지**. 값 비교로는 이 항목을 닫을 수 없다.
      3. a092 §6.11의 대칭성은 기계로 확인했다(22 = 22, 누락 0). a098이
         Scenario를 더하면 **그 대칭이 깨진다** — a092 10.4.4가 검사로 만든다.

## 안 하는 것 — 이름 붙여 둔다

| 무엇 | 어디로 |
|---|---|
| 잠금이 덮는 구간, 배치 공정성, 백로그 상한, 등급 이관 | **a092** |
| `SetModeProjector`·`RestoreOperatingModeProjection` 배선 | 미배정 후속 (a092 proposal이 이미 적음) |
| 발송 상수 도입 (`alertPublishTimeout` 등) | **a092** |
| ntfy topic 실측 POST | **사람 승인 사항** — 사용자 휴대폰으로 실제 알림이 간다 |
| **배달 실행자의 「살아 있는데 계속 실패」 열화 사다리** | **미배정 후속** (4판 신설 — design D8.4) |
| **`AllReasonCodes()`에 미등록인 기존 ReasonCode 16개** | **미배정 후속** (5판 신설 — 5라운드 A-T2). `reason.go` 45개 선언 vs 열거 29개. a098은 **자기 코드 하나만** 등록한다 |
| **`TestTheLoopSetIsTheSpecifiedThree`의 거짓 초록** | **미배정 후속** (5판 신설 — 5라운드 B-T3). `cmd/tossctl/engine_test.go:347-361`은 **소스 문자열 포함만** 검사해 **루프가 넷이어도 통과한다** |
| **`Notifier.Flush`의 정산·반납 실패 경로 여덟이 미측정** | **a092** (신설 2026-08-12, §1.5의 커버리지 측정이 찾았다). a099가 더한 B10·B12·B13·B14·B16·B17·B18·B19가 **`Flush`를 통해서는 한 번도 실행되지 않는다** — a099의 테스트는 같은 규범을 `deliver` 쪽에서 잰다. 즉 *"두 발송 경로가 같은 규범을 진다"*의 **`Flush` 쪽 절반이 미측정**이다. a098은 이 함수를 안 건드리므로 안 지고, **조용히 넘기지도 않는다** |
| ~~**승인된 정본의 `strategy-entry` drift**~~ | **더 이상 「안 하는 것」이 아니다** — 사용자 결정 10-1이 **a098에 배정했다.** 4.1b의 `MODIFIED`가 넷으로 고친다 |
| **a092 spec `:24-25`의 *"루프를 하나 더한다 — 알림 배달 루프"*** | **a092로 되돌린다** — 사용자 결정 9-2가 그것을 부정한다(보조 실행자이지 감독 루프가 아니다). a099 §7.5 |
| **a092 spec `:126-130`의 *"운영 모드를 `ENTRY_BLOCKED`로 승격한다"*** | **a092가 진다** (6판 신설 — 사용자 결정 12-2). a098은 결정 11-1로 그 조항을 **안 진다.** a099 §7.5 |
| **a092 MODIFIED의 *"그 실행자는 엔진의 루프 감독 아래에 있어야 하며(SHALL)"*** | **a092가 진다** (6판 신설 — 12-2의 값 단위 확대). 결정 9-2와 **정면으로 반대**이고, 「미배정」이 아니라 **「반대」**다 — a099 §7.5가 그 구별을 적는다 |

> **마지막 줄을 정직하게 적는다 — 결정 9-2가 포기한 것이다.**
> 감독 루프는 사이클이 연속 5회 실패하면 `ENTRY_BLOCKED`로 자동 강화된다
> (`superviseHealth` → `CheckHealth`). 보조 실행자는 그 사다리에 **올라갈 자리가 없다** —
> `Health`가 `SupervisedLoop`의 필드이고 `superviseHealth`(`runtime.go:290`)가 `Loops`만 훑는다.
>
> **그래서 「배달 실행자가 매 주기 publish에 실패하면서 계속 도는 상태」를 a098은
> 자동으로 안 잡는다.** 그때 진입을 막는 것은 오늘의 다섯 자리 중 `deliver`의 실패
> 경로이고, 그것은 **동기 알림이 한 번이라도 시도될 때만** 돈다.
> 지금 메우려면 `Health`를 보조 실행자에도 다는 설계가 되고, 그것은
> **감독 집합 밖에 두라는 결정 9-2와 반대 방향**이다.
