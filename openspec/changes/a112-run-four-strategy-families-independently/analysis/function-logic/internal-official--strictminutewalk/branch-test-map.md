# Branch Test Map: `strictMinuteWalk`

- Source: `internal/official/strict_minute_candles.go`, SHA-256 `441bed46f81bc928cab03d512b3ff1305c0c663cb1b58027986e2e91b739977d`; branch IDs follow `ast.json` (15 branches, generated 2026-08-17 after GREEN).
- New function (lot L1b, not in the frozen base 016da624). RED per review.md: build-failure RED on the new symbols before GREEN; ruling 29's depth-bound test (17 refused, 16 accepted) closed the adversary's D5 challenge RED-first, and the `default` arm was given its unreachability comment in the same round.
- Tests: `internal/official/strict_minute_candles_test.go`.

| Branch | Scenario anchor | Required test disposition | RED observed | GREEN observed |
|---|---|---|---|---|
| B1 | if at 485:2 — an empty body: the first token is `io.EOF` | `TestStrictMinuteCandlesRefusesMalformedBodies` (subtest `empty body`) | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-17) |
| B2 | if at 489:2 — scalar leaves accepted | `TestStrictMinuteCandlesSendsTheCanonicalQueryAndReturnsThePage` | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-17) |
| B3 | if at 493:2 — 17 nested levels refused, 16 accepted | `TestStrictMinuteCandlesRefusesMalformedBodies` (subtest `seventeen levels of nesting`), `TestStrictMinuteCandlesAcceptsNestingUpToTheBound` | build/behaviour RED captured by the implementer 2026-08-17 (review.md L1b implementation report) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-17) |
| B4 | switch at 496:2 — objects and arrays dispatched | `TestStrictMinuteCandlesSendsTheCanonicalQueryAndReturnsThePage` | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-17) |
| B5 | case at 497:2 — an object level tracks its own key set | `TestStrictMinuteCandlesSendsTheCanonicalQueryAndReturnsThePage` | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-17) |
| B6 | for at 499:3 — envelope members walked, including an ignored `traceId` sibling | `TestStrictMinuteCandlesIgnoresUnknownEnvelopeKeys` | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-17) |
| B7 | if at 501:4 — the key token cannot be read | untested: needs a syntactically malformed object; the fixtures are well-formed apart from the empty body (recorded gap; fails closed) | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-17) |
| B8 | if at 505:4 — a non-string object key | not-applicable: `encoding/json` yields only string keys inside an object; `More()` excludes the closing delimiter | not-applicable | not-applicable |
| B9 | if at 508:4 — the same key twice at envelope, `result` and candle depth, and inside an ignored sibling | `TestStrictMinuteCandlesRefusesMalformedBodies` (subtests `duplicate key in the envelope`, `duplicate key in the result`, `duplicate key in a candle`, `duplicate key inside an ignored sibling`) | build/behaviour RED captured by the implementer 2026-08-17 (review.md L1b implementation report) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-17) |
| B10 | if at 512:4 — a nested refusal reaches the top | `TestStrictMinuteCandlesRefusesMalformedBodies` (subtests `duplicate key in the result`, `duplicate key in a candle`, `duplicate key inside an ignored sibling`, `seventeen levels of nesting`) | build/behaviour RED captured by the implementer 2026-08-17 (review.md L1b implementation report) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-17) |
| B11 | case at 516:2 — the `candles` array | `TestStrictMinuteCandlesSendsTheCanonicalQueryAndReturnsThePage` | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-17) |
| B12 | for at 517:3 — two candle elements walked | `TestStrictMinuteCandlesSendsTheCanonicalQueryAndReturnsThePage` | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-17) |
| B13 | if at 518:4 — a refusal inside an array element propagates | `TestStrictMinuteCandlesRefusesMalformedBodies` (subtest `duplicate key in a candle`) | build/behaviour RED captured by the implementer 2026-08-17 (review.md L1b implementation report) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-17) |
| B14 | case at 522:2 — any other delimiter | not-applicable: unreachable defence declared in the source comment at 523–524 (`encoding/json` emits only `{ [ ] }`; closers are consumed by the `More()`/`Token()` pairing) | not-applicable | not-applicable |
| B15 | if at 527:2 — the closing token cannot be read | untested: needs a truncated body; the fixtures are well-formed apart from the empty one (recorded gap; fails closed) | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-17) |

Verification (this bundle, 2026-08-17): `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0, 404 tests passed across the two packages.
