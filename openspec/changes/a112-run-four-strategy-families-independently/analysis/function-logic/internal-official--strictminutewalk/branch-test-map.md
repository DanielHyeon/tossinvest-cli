# Branch Test Map: `strictMinuteWalk`

- Source: `internal/official/strict_minute_candles.go`, SHA-256 `d32181a939f298db306f492b488468b5925ac0ba97dad3f82cb1cb3286254ced`; branch IDs follow `ast.json` (15 branches, regenerated 2026-08-18 against the decision-30 sources).
- New function (lot L1b, not in the frozen base 016da624). RED per review.md: build-failure RED on the new symbols before GREEN; ruling 29's depth-bound test (17 refused, 16 accepted) closed the adversary's D5 challenge RED-first, and the `default` arm was given its unreachability comment in the same round.
- **Decision-30 correction (2026-08-18).** The broker's `timestamp` is the bar's close instant, not its open (US probe 03:29 KST, review.md). This function's behaviour is unchanged; only its documentation and its source line numbers moved. Branch count unchanged.
- Tests: `internal/official/strict_minute_candles_test.go`.

| Branch | Scenario anchor | Required test disposition | RED observed | GREEN observed |
|---|---|---|---|---|
| B1 | if at 497:2 — an empty body: the first token is `io.EOF` | `TestStrictMinuteCandlesRefusesMalformedBodies` (subtest `empty body`) | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B2 | if at 501:2 — scalar leaves accepted | `TestStrictMinuteCandlesSendsTheCanonicalQueryAndReturnsThePage` | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B3 | if at 505:2 — 17 nested levels refused, 16 accepted | `TestStrictMinuteCandlesRefusesMalformedBodies` (subtest `seventeen levels of nesting`), `TestStrictMinuteCandlesAcceptsNestingUpToTheBound` | build/behaviour RED captured by the implementer 2026-08-17 (review.md L1b implementation report) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B4 | switch at 508:2 — objects and arrays dispatched | `TestStrictMinuteCandlesSendsTheCanonicalQueryAndReturnsThePage` | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B5 | case at 509:2 — an object level tracks its own key set | `TestStrictMinuteCandlesSendsTheCanonicalQueryAndReturnsThePage` | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B6 | for at 511:3 — envelope members walked, including an ignored `traceId` sibling | `TestStrictMinuteCandlesIgnoresUnknownEnvelopeKeys` | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B7 | if at 513:4 — the key token cannot be read | untested: needs a syntactically malformed object; the fixtures are well-formed apart from the empty body (recorded gap; fails closed) | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B8 | if at 517:4 — a non-string object key | not-applicable: `encoding/json` yields only string keys inside an object; `More()` excludes the closing delimiter | not-applicable | not-applicable |
| B9 | if at 520:4 — the same key twice at envelope, `result` and candle depth, and inside an ignored sibling | `TestStrictMinuteCandlesRefusesMalformedBodies` (subtests `duplicate key in the envelope`, `duplicate key in the result`, `duplicate key in a candle`, `duplicate key inside an ignored sibling`) | build/behaviour RED captured by the implementer 2026-08-17 (review.md L1b implementation report) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B10 | if at 524:4 — a nested refusal reaches the top | `TestStrictMinuteCandlesRefusesMalformedBodies` (subtests `duplicate key in the result`, `duplicate key in a candle`, `duplicate key inside an ignored sibling`, `seventeen levels of nesting`) | build/behaviour RED captured by the implementer 2026-08-17 (review.md L1b implementation report) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B11 | case at 528:2 — the `candles` array | `TestStrictMinuteCandlesSendsTheCanonicalQueryAndReturnsThePage` | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B12 | for at 529:3 — two candle elements walked | `TestStrictMinuteCandlesSendsTheCanonicalQueryAndReturnsThePage` | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B13 | if at 530:4 — a refusal inside an array element propagates | `TestStrictMinuteCandlesRefusesMalformedBodies` (subtest `duplicate key in a candle`) | build/behaviour RED captured by the implementer 2026-08-17 (review.md L1b implementation report) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B14 | case at 534:2 — any other delimiter | not-applicable: unreachable defence declared in the source comment at 535–536 (`encoding/json` emits only `{ [ ] }`; closers are consumed by the `More()`/`Token()` pairing) | not-applicable | not-applicable |
| B15 | if at 539:2 — the closing token cannot be read | untested: needs a truncated body; the fixtures are well-formed apart from the empty one (recorded gap; fails closed) | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |

Verification (this bundle, 2026-08-18): `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0, 408 tests passed across the two packages.
