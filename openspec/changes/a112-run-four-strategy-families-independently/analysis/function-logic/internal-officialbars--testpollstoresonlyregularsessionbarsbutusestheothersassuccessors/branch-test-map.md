# Branch Test Map: `TestPollStoresOnlyRegularSessionBarsButUsesTheOthersAsSuccessors`

- Source: `internal/officialbars/producer_test.go`, SHA-256 `6c6f064d3ecceb46ed4df8546cb5d1f16c40e2831dbfa99c223c0c99f2b5eda4`; branch IDs follow `ast.json` (6 branches, regenerated 2026-08-18 against the decision-30 sources, revision `current`).
- Disposition: **Test helper / test function caught by the gate because it existed at the frozen base `d9d9f71f` and the decision-30 correction edited it. Enumerated for gate completeness: these carry no production branch.** The rows below therefore read "what the guard proves" — this *is* the test; the production branches behind it are `PollClosedBars` B35/B36 and `minuteGaps`, enumerated in their own bundles.
- The edit: **two inline comments only**. Every argument, assertion and expected value is unchanged from the frozen base, because the test already spoke in open instants through `usBar`; the comments now say "16:00 ET**에 여는** 봉" and "15:59**에 여는** 봉의 successor" instead of leaving "the 16:00 bar" ambiguous between broker label and open instant.
- Tests: `internal/officialbars/producer_test.go` (this file).

| Branch | Scenario anchor | Required test disposition | RED observed | GREEN observed |
|---|---|---|---|---|
| B1 | if at 422:2 — `PollClosedBars` returns an error on a page mixing in-window and out-of-window bars | proves an out-of-window bar is **not** a contract violation: the page is accepted and the poll completes | n/a (test helper; the decision-30 RED was the 13 producer tests that failed one minute off, review.md 2026-08-18) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0, 408 tests passed, 0 skipped (2026-08-18) |
| B2 | if at 425:2 — `Observed != 5` or `Admitted != 2` | proves the two counters diverge exactly as design §5 requires: five bars seen, two admitted | n/a (test helper; the decision-30 RED was the 13 producer tests that failed one minute off, review.md 2026-08-18) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0, 408 tests passed, 0 skipped (2026-08-18) |
| B3 | if at 429:2 — the sealed series does not hold exactly 2 bars | proves the store agrees with `Admitted`, so the count is not just the producer reporting on itself | n/a (test helper; the decision-30 RED was the 13 producer tests that failed one minute off, review.md 2026-08-18) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0, 408 tests passed, 0 skipped (2026-08-18) |
| B4 | if at 432:2 — the stored `OpenAtMS` values are not `usOpen` (09:30) and `usClose−1m` (15:59) | proves **which** minutes were kept, by value, in open-instant space. This is the guard that would have caught a decision-30 one-minute shift with no new test written — it passes unchanged, which is evidence the conversion landed in `adoptPage` and nowhere else | n/a (test helper; the decision-30 RED was the 13 producer tests that failed one minute off, review.md 2026-08-18) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0, 408 tests passed, 0 skipped (2026-08-18) |
| B5 | if at 437:2 — the 15:59 bar's `SuccessorOpenAtMS` is not the bar opening at 16:00 | proves an **out-of-window** bar still serves as the successor that makes an in-window bar admissible — the invariant separating "not stored" from "not seen" (decision 6 × design §5) | n/a (test helper; the decision-30 RED was the 13 producer tests that failed one minute off, review.md 2026-08-18) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0, 408 tests passed, 0 skipped (2026-08-18) |
| B6 | if at 440:2 — the gap report is not exactly one 388-minute gap from `usOpen+1m` to `usClose−2m` | proves `minuteGaps` counts on converted opens, clamps to the regular window, and reports without suppressing either stored bar | n/a (test helper; the decision-30 RED was the 13 producer tests that failed one minute off, review.md 2026-08-18) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0, 408 tests passed, 0 skipped (2026-08-18) |

Complementary coverage: this test asserts the US window boundary in **open** space; the label-space form of the same boundary (the bar labelled 16:00 ET opening at 15:59) is asserted by `TestPollTreatsTheClosingLabelAsTheLastRegularBar`, and its KRX twin by `TestPollTreatsTheKoreanClosingLabelAsTheLastRegularBar`.

Verification (this bundle, 2026-08-18): `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 — `ok internal/official 4.685s`, `ok internal/officialbars 1.548s`, 408 `=== RUN` / 408 PASS, 0 SKIP.
