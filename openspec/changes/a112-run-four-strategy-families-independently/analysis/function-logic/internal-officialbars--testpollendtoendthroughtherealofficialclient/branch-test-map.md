# Branch Test Map: `TestPollEndToEndThroughTheRealOfficialClient`

- Source: `internal/officialbars/producer_test.go`, SHA-256 `6c6f064d3ecceb46ed4df8546cb5d1f16c40e2831dbfa99c223c0c99f2b5eda4`; branch IDs follow `ast.json` (13 branches, regenerated 2026-08-18 against the decision-30 sources).
- Disposition: **Test function edited by the decision-30 correction (broker label = bar close). Enumerated for gate completeness: the assertions are themselves the evidence; there is no production branch behind these rows.** The rows below therefore read "what the guard proves", not "which test covers it" — this *is* the test.
- The edit: line 1410 now builds the page-2 cursor with `cursorBefore(t, instants[1])` instead of `brokerTimestamp(t, instants[1].Add(-time.Minute))`. Both produce the same wire value; the helper names it in the decision-30 vocabulary (the measured cursor is the oldest bar's **label** minus one minute, which equals that bar's open instant).
- Tests: `internal/officialbars/producer_test.go` (this file).

| Branch | Scenario anchor | Required test disposition | RED observed | GREEN observed |
|---|---|---|---|---|
| B1 | if at 1395:2 — `MarketUS.TradingDay(now)` returns an error | assertion guard on the harness: the eastern zone must resolve before the fixture means anything; not-applicable as a *taken* path (`time/tzdata` is embedded, so the lookup cannot fail) | n/a (test function; the decision-30 RED was the 13 producer tests that failed one minute off, review.md 2026-08-18) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0, 408 tests passed (2026-08-18) |
| B2 | if at 1401:2 — the wall clock puts the fixture bars outside the 00:10–23:50 ET window → announced `t.Skipf` | proves the test declines rather than asserts falsely near the eastern day boundary (≈22 minutes a day: 00:00–00:14 and 23:52–24:00 ET); the announced residual in review.md 2026-08-17 | n/a (test function; the decision-30 RED was the 13 producer tests that failed one minute off, review.md 2026-08-18) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0, 408 tests passed (2026-08-18) |
| B3 | if at 1413:3 — the handler sees `/oauth2/token` | proves the poll travels the ordinary client path including the token exchange, not a bypass | n/a (test function; the decision-30 RED was the 13 producer tests that failed one minute off, review.md 2026-08-18) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0, 408 tests passed (2026-08-18) |
| B4 | switch at 1417:3 — dispatch on the `before` query parameter | proves the server can answer only the bounds the producer actually formats, so the request literal is under test | n/a (test function; the decision-30 RED was the 13 producer tests that failed one minute off, review.md 2026-08-18) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0, 408 tests passed (2026-08-18) |
| B5 | case at 1418:3 — `before == firstBefore` → page 1 (2 candles, `nextBefore` = cursor) | proves the page-1 bound is `PollAt` truncated to the second, rendered in `Asia/Seoul` with the measured layout | n/a (test function; the decision-30 RED was the 13 producer tests that failed one minute off, review.md 2026-08-18) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0, 408 tests passed (2026-08-18) |
| B6 | case at 1421:3 — `before == cursor` → page 2 (1 candle, `nextBefore: null`) | proves the producer echoes the cursor byte for byte and stops on the terminal page | n/a (test function; the decision-30 RED was the 13 producer tests that failed one minute off, review.md 2026-08-18) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0, 408 tests passed (2026-08-18) |
| B7 | case at 1423:3 — any other `before` → `t.Errorf` and HTTP 400 | proves no unexpected third request is made and no drifted bound is silently answered | n/a (test function; the decision-30 RED was the 13 producer tests that failed one minute off, review.md 2026-08-18) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0, 408 tests passed (2026-08-18) |
| B8 | if at 1436:2 — `strategyevidence.Open` fails | harness guard on the real store; nothing after it would be evidence | n/a (test function; the decision-30 RED was the 13 producer tests that failed one minute off, review.md 2026-08-18) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0, 408 tests passed (2026-08-18) |
| B9 | if at 1445:2 — `PollClosedBars` returns an error | proves request literal, token, body contract, decision-30 conversion, envelope construction and append all complete without a refusal | n/a (test function; the decision-30 RED was the 13 producer tests that failed one minute off, review.md 2026-08-18) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0, 408 tests passed (2026-08-18) |
| B10 | if at 1448:2 — `Pages != 2`, `Observed != 3`, `Admitted != 2` or `!Terminal` | proves the crawl shape through the real client: two pages, three bars observed, decision 6 withholding the newest, terminal reached | n/a (test function; the decision-30 RED was the 13 producer tests that failed one minute off, review.md 2026-08-18) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0, 408 tests passed (2026-08-18) |
| B11 | if at 1451:2 — `SessionID != "US:"+day` | proves the session identity is the market-local trading day of the poll instant, not a UTC date | n/a (test function; the decision-30 RED was the 13 producer tests that failed one minute off, review.md 2026-08-18) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0, 408 tests passed (2026-08-18) |
| B12 | range at 1454:2 — every captured envelope walked | proves the provenance assertion applies to each appended row, not only the first | n/a (test function; the decision-30 RED was the 13 producer tests that failed one minute off, review.md 2026-08-18) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0, 408 tests passed (2026-08-18) |
| B13 | if at 1458:3 — `SourceObservedAtMS <= PollAt` (strictly, so `==` also fails) | proves `source_observed_at_ms` comes from the transport's `BodyReadComplete` and not from `PollAt` — the mistake an `==` comparison would have let through | n/a (test function; the decision-30 RED was the 13 producer tests that failed one minute off, review.md 2026-08-18) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0, 408 tests passed (2026-08-18) |

Verification (this bundle, 2026-08-18): `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0, 408 tests passed across the two packages (`ok internal/official 4.608s`, `ok internal/officialbars 1.577s`, no skips in this run — the B2 window did not apply at the wall-clock instant of the run).
