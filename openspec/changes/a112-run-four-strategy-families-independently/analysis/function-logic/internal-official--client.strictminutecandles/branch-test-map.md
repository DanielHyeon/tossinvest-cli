# Branch Test Map: `Client.StrictMinuteCandles`

- Source: `internal/official/strict_minute_candles.go`, SHA-256 `d32181a939f298db306f492b488468b5925ac0ba97dad3f82cb1cb3286254ced`; branch IDs follow `ast.json` (11 branches, regenerated 2026-08-18 against the decision-30 sources).
- New function (lot L1b, not in the frozen base 016da624). RED per review.md: build-failure RED on the new symbols, then behaviour RED against a first cut without the `result`-key-count rule, with an absent `nextBefore` treated as terminal and without the candle key-count rule (`cursor absent`, `unknown result key`, `unknown candle key` failed); ruling 28 (last attempt must be 2xx) and ruling 26 (minute alignment) landed RED-first in the fix round.
- **Decision-30 correction (2026-08-18).** The broker's `timestamp` is the bar's close instant, not its open (US probe 03:29 KST, review.md). This function's behaviour is unchanged; only its documentation and its source line numbers moved. Branch count unchanged.
- Tests: `internal/official/strict_minute_candles_test.go` (the `StrictMinutePage{` guard lives there too).

| Branch | Scenario anchor | Required test disposition | RED observed | GREEN observed |
|---|---|---|---|---|
| B1 | if at 132:2 — a nil `*Client` | `TestStrictMinuteCandlesRefusesANilClient` | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B2 | if at 136:2 — `us` and `JP` refused before any request | `TestStrictMinuteCandlesRefusesBadArgumentsBeforeAnyRequest` (subtests `lower case market`, `unknown market`; zero server hits) | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B3 | if at 139:2 — six symbol-grammar violations across both markets | `TestStrictMinuteCandlesRefusesBadArgumentsBeforeAnyRequest` (subtests `kr symbol with letters`, `kr symbol too short`, `us symbol lower case`, `us symbol too long`, `us symbol leading digit`, `empty symbol`) | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B4 | if at 142:2 — count 0, 201, −1 | `TestStrictMinuteCandlesRefusesBadArgumentsBeforeAnyRequest` (subtests `count zero`, `count above the page cap`, `count negative`) | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B5 | if at 147:2 — with and without a `before` | `TestStrictMinuteCandlesSendsTheCanonicalQueryAndReturnsThePage`, `TestStrictMinuteCandlesAcceptsTheKoreanMarket` | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B6 | if at 149:3 — `Z` offset, no offset, four fractional digits, named zone, 30 February | `TestStrictMinuteCandlesRefusesBadArgumentsBeforeAnyRequest` (subtests `before with a zulu offset`, `before without an offset`, `before with four fractional digits`, `before with a named zone`, `before that does not exist`) | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B7 | if at 159:2 — the canonical query string with and without `before` | `TestStrictMinuteCandlesSendsTheCanonicalQueryAndReturnsThePage` (`adjusted=false&before=…%2B09%3A00&count=200&interval=1m&symbol=AAPL`), `TestStrictMinuteCandlesAcceptsTheKoreanMarket` | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B8 | if at 168:3 — an outer observer already on the ctx sees both attempts | `TestStrictMinuteCandlesUsesTheLastSuccessfulAttemptAndChainsTheOuterObserver` | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B9 | if at 172:2 — 429 and 500 propagate `ErrRateLimited`/`ErrServer`, not a contract refusal | `TestStrictMinuteCandlesPropagatesTransportClassification` (subtests `rate limited`, `server error`) | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B10 | if at 177:2 — no attempt usable under ruling 28 | not-applicable: unreachable through today's `send` (D6, declared in the source comments at 241 and 246); untaken side pinned by `TestStrictMinuteCandlesUsesTheLastSuccessfulAttemptAndChainsTheOuterObserver` (401 then 200 uses the second attempt's digest and `ReadAt`) | not-applicable | not-applicable |
| B11 | if at 181:2 — 45 malformed bodies, each asserting its `StrictReason*` | `TestStrictMinuteCandlesRefusesMalformedBodies` | build/behaviour RED captured by the implementer 2026-08-17 (review.md L1b implementation report) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |

Whole-function properties (calls and page fields, not branches): body digest and `ReadAt` provenance `TestStrictMinuteCandlesSendsTheCanonicalQueryAndReturnsThePage`; advisory budget `TestStrictMinuteCandlesReportsTheRateBudgetOfTheSameResponse`; ignored envelope siblings `TestStrictMinuteCandlesIgnoresUnknownEnvelopeKeys`; static guards `TestStrictMinutePageLiteralLivesOnlyInItsOwnFile`, `TestStrictMinuteCandlesFilesCarryNoSeamIdentifier`.

Verification (this bundle, 2026-08-18): `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0, 408 tests passed across the two packages.
