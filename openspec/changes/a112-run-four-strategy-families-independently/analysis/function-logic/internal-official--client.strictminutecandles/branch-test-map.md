# Branch Test Map: `Client.StrictMinuteCandles`

- Source: `internal/official/strict_minute_candles.go`, SHA-256 `441bed46f81bc928cab03d512b3ff1305c0c663cb1b58027986e2e91b739977d`; branch IDs follow `ast.json` (11 branches, generated 2026-08-17 after GREEN).
- New function (lot L1b, not in the frozen base 016da624). RED per review.md: build-failure RED on the new symbols, then behaviour RED against a first cut without the `result`-key-count rule, with an absent `nextBefore` treated as terminal and without the candle key-count rule (`cursor absent`, `unknown result key`, `unknown candle key` failed); ruling 28 (last attempt must be 2xx) and ruling 26 (minute alignment) landed RED-first in the fix round.
- Tests: `internal/official/strict_minute_candles_test.go` (the `StrictMinutePage{` guard lives there too).

| Branch | Scenario anchor | Required test disposition | RED observed | GREEN observed |
|---|---|---|---|---|
| B1 | if at 121:2 — a nil `*Client` | `TestStrictMinuteCandlesRefusesANilClient` | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-17) |
| B2 | if at 125:2 — `us` and `JP` refused before any request | `TestStrictMinuteCandlesRefusesBadArgumentsBeforeAnyRequest` (subtests `lower case market`, `unknown market`; zero server hits) | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-17) |
| B3 | if at 128:2 — six symbol-grammar violations across both markets | `TestStrictMinuteCandlesRefusesBadArgumentsBeforeAnyRequest` (subtests `kr symbol with letters`, `kr symbol too short`, `us symbol lower case`, `us symbol too long`, `us symbol leading digit`, `empty symbol`) | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-17) |
| B4 | if at 131:2 — count 0, 201, −1 | `TestStrictMinuteCandlesRefusesBadArgumentsBeforeAnyRequest` (subtests `count zero`, `count above the page cap`, `count negative`) | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-17) |
| B5 | if at 136:2 — with and without a `before` | `TestStrictMinuteCandlesSendsTheCanonicalQueryAndReturnsThePage`, `TestStrictMinuteCandlesAcceptsTheKoreanMarket` | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-17) |
| B6 | if at 138:3 — `Z` offset, no offset, four fractional digits, named zone, 30 February | `TestStrictMinuteCandlesRefusesBadArgumentsBeforeAnyRequest` (subtests `before with a zulu offset`, `before without an offset`, `before with four fractional digits`, `before with a named zone`, `before that does not exist`) | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-17) |
| B7 | if at 148:2 — the canonical query string with and without `before` | `TestStrictMinuteCandlesSendsTheCanonicalQueryAndReturnsThePage` (`adjusted=false&before=…%2B09%3A00&count=200&interval=1m&symbol=AAPL`), `TestStrictMinuteCandlesAcceptsTheKoreanMarket` | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-17) |
| B8 | if at 157:3 — an outer observer already on the ctx sees both attempts | `TestStrictMinuteCandlesUsesTheLastSuccessfulAttemptAndChainsTheOuterObserver` | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-17) |
| B9 | if at 161:2 — 429 and 500 propagate `ErrRateLimited`/`ErrServer`, not a contract refusal | `TestStrictMinuteCandlesPropagatesTransportClassification` (subtests `rate limited`, `server error`) | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-17) |
| B10 | if at 166:2 — no attempt usable under ruling 28 | not-applicable: unreachable through today's `send` (D6, declared in the source comments at 230 and 235); untaken side pinned by `TestStrictMinuteCandlesUsesTheLastSuccessfulAttemptAndChainsTheOuterObserver` (401 then 200 uses the second attempt's digest and `ReadAt`) | not-applicable | not-applicable |
| B11 | if at 170:2 — 45 malformed bodies, each asserting its `StrictReason*` | `TestStrictMinuteCandlesRefusesMalformedBodies` | build/behaviour RED captured by the implementer 2026-08-17 (review.md L1b implementation report) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-17) |

Whole-function properties (calls and page fields, not branches): body digest and `ReadAt` provenance `TestStrictMinuteCandlesSendsTheCanonicalQueryAndReturnsThePage`; advisory budget `TestStrictMinuteCandlesReportsTheRateBudgetOfTheSameResponse`; ignored envelope siblings `TestStrictMinuteCandlesIgnoresUnknownEnvelopeKeys`; static guards `TestStrictMinutePageLiteralLivesOnlyInItsOwnFile`, `TestStrictMinuteCandlesFilesCarryNoSeamIdentifier`.

Verification (this bundle, 2026-08-17): `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0, 404 tests passed across the two packages.
