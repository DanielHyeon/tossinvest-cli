# Branch Test Map: `PollClosedBars`

- Source: `internal/officialbars/producer.go`, SHA-256 `8d45ca93b090cfe9e10a93e5a658991ed3376820b56dfa05e49b809171c16772`; branch IDs follow `ast.json` (42 branches, regenerated 2026-08-18 against the decision-30 sources).
- New function (lot L1b, not in the frozen base 016da624). RED per review.md: build-failure RED on the new symbols, then behaviour RED against a first cut that looped from index 0 with `successor = open+1m` and no overlap check (10 of 32 failed); the ruling 24–29 fix round was RED-first again (calendar window at open+6h, same-`PollAt` retry conflicts, page identity, ascending merged list, sub-second truncation, calendar window on its own day).
- **Decision-30 correction (2026-08-18).** The 03:29 KST US probe proved the broker's `timestamp` is the bar's **close**; `adoptPage` now converts once (`openAt = label − 60 s`) and this function's branch count is unchanged. The decision-30 RED is recorded in review.md: 13 producer tests failed exactly one minute off before the subtraction landed. Several existing tests were re-based to express open instants through the `usBar`/`krBar` helpers (which now synthesise `Timestamp = open + 1m`), and the new `usBarLabelled`/`krBarLabelled` helpers speak in broker labels — the boundary rows below (B36) use those.
- Tests: `internal/officialbars/producer_test.go`, `internal/officialbars/guard_test.go`.

| Branch | Scenario anchor | Required test disposition | RED observed | GREEN observed |
|---|---|---|---|---|
| B1 | if at 171:2 — a nil reader or store | untested: no test passes a nil dependency (reachable from a caller; recorded gap) | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B2 | if at 175:2 — market `jp` | `TestPollRefusesInvalidInputBeforeAnyRequest` (subtest `unknown market`; zero reader calls) | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B3 | if at 179:2 — empty symbol, symbol carrying a space | `TestPollRefusesInvalidInputBeforeAnyRequest` (subtests `empty symbol`, `symbol with a space`) | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B4 | if at 182:2 — a US calendar handed to a KR poll | `TestPollRefusesInvalidInputBeforeAnyRequest` (subtest `calendar market mismatch`) | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B5 | if at 186:2 — `PollAt` left zero | `TestPollRefusesInvalidInputBeforeAnyRequest` (subtest `poll instant missing`) | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B6 | if at 195:2 — ruling 24 calendar gate: clock-skewed snapshot refused, stale/late snapshot accepted at open+6h and open+6h29m | `TestPollRefusesInvalidInputBeforeAnyRequest` (subtest `calendar fetched after the poll`) and `TestPollAcceptsAStaleOrLateCalendarThroughTheWholeSession` | build/behaviour RED captured by the implementer 2026-08-17 (review.md L1b implementation report) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B7 | if at 198:2 — `Today.Regular` nil | `TestPollRefusesInvalidInputBeforeAnyRequest` (subtest `no regular session`) | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B8 | range at 204:2 — both edges of the regular window walked | `TestPollRefusesACalendarWindowThatIsNotOnItsOwnDay` | build/behaviour RED captured by the implementer 2026-08-17 (review.md L1b implementation report) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B9 | if at 209:3 — hand-built snapshot whose window sits on the previous day | `TestPollRefusesACalendarWindowThatIsNotOnItsOwnDay` (refuses `CALENDAR_INVALID`, zero reader calls) | build/behaviour RED captured by the implementer 2026-08-17 (review.md L1b implementation report) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B10 | if at 216:2 — `TradingDay(PollAt)` error | not-applicable: only fails when the market zone cannot load; `time/tzdata` is embedded | not-applicable | not-applicable |
| B11 | if at 219:2 — poll 48 h off the calendar's day; and the 01:00 KST overnight poll that must still match | `TestPollRefusesInvalidInputBeforeAnyRequest` (subtest `poll instant on another trading day`), `TestPollLabelsOvernightUSBarsWithTheirEasternDate` | build/behaviour RED captured by the implementer 2026-08-17 (review.md L1b implementation report) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B12 | if at 224:2 — `MaxPages` 9 and −1 | `TestPollRefusesInvalidInputBeforeAnyRequest` (subtests `max pages above the bound`, `max pages negative`) | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B13 | if at 230:2 — `MaxPages` 0 takes the default 4 | `TestPollDefaultsToFourPages`; untaken side `TestPollStopConditions` (subtest `max pages`) | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B14 | if at 234:2 — `Asia/Seoul` unavailable | not-applicable: unreachable defence declared in the source comment at 235 (`time/tzdata` compiled in) | not-applicable | not-applicable |
| B15 | if at 243:2 — default lower bound versus an explicit one | `TestPollStopConditions` (subtest `lower bound`), `TestPollStopsAtAnExplicitLowerBound` | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B16 | for at 254:2 — two measured-shape pages crawled | `TestPollCrawlsPagesInTheMeasuredShape` | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B17 | if at 259:3 — the reader fails on page 2; `Pages` must survive | `TestPollReturnsAReaderErrorWithoutAppending`, `TestPollKeepsThePageCountWhenTheReaderFails` | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B18 | if at 262:3 — a KR/005930 page answered a US/AAPL poll | `TestPollRefusesAPageForAnotherMarketOrSymbol` | build/behaviour RED captured by the implementer 2026-08-17 (review.md L1b implementation report) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B19 | if at 268:3 — `adoptPage` refusal propagated (unreadable label, or a converted open instant off the minute) | not-applicable: the reader has already parsed every candle label and pinned it to a minute (ruling 26), and label − 60 s stays minute-aligned; no interface-guard test for this arm | not-applicable | not-applicable |
| B20 | if at 272:3 — a second non-empty page arrives after a non-empty first | `TestPollCrawlsPagesInTheMeasuredShape`, `TestPollInterfaceGuardDropsTheEqualInstantOverlapBar` | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B21 | if at 274:4 — page 2 starts newer than page 1's oldest bar, or the shared minute differs | not-applicable through `official.Client` (ruling 27); interface guards `TestPollInterfaceGuardRefusesAPageThatStartsNewerThanThePreviousPageEnded`, `TestPollInterfaceGuardRefusesAnOverlapThatIsNotByteIdentical` | build/behaviour RED captured by the implementer 2026-08-17 (review.md L1b implementation report) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B22 | if at 280:4 — the equal-instant overlap bar is dropped exactly once | `TestPollInterfaceGuardDropsTheEqualInstantOverlapBar` (Observed 5, Admitted 4) | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B23 | if at 285:3 — terminal page stops the crawl | `TestPollNeverAdmitsTheNewestObservedBar`, `TestPollCrawlsPagesInTheMeasuredShape` | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B24 | if at 289:3 — an empty page that still carries a cursor stops the crawl | `TestPollStopConditions` (subtest `empty page`; Pages 2, no flag set) | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B25 | if at 292:3 — the page's oldest bar falls below the lower bound | `TestPollStopConditions` (subtest `lower bound`), `TestPollStopsAtAnExplicitLowerBound` | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B26 | if at 297:3 — the cursor is not a timestamp | not-applicable: unreachable defence declared in the source comment at 298 (the reader parsed it under the same grammar) | not-applicable | not-applicable |
| B27 | if at 303:3 — a cursor that does not move backwards, incl. one equal to the truncated bound; cursor and bound are both broker labels (close instants) and are deliberately not converted | `TestPollRefusesACursorThatDoesNotMoveBackwards`, `TestPollTruncatesTheSubSecondPollInstant` | build/behaviour RED captured by the implementer 2026-08-17 (review.md L1b implementation report) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B28 | if at 307:3 — `MaxPages` reached ⇒ `Truncated`, no further request | `TestPollStopConditions` (subtest `max pages`), `TestPollDefaultsToFourPages` | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B29 | if at 311:3 — `Remaining 0` budget ⇒ `RateGated`, 1 reader call | `TestPollStopConditions` (subtest `rate gated`) | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B30 | for at 325:2 — the merged list is walked pairwise | `TestPollRefusesAMergedListThatIsNotStrictlyDescending` | build/behaviour RED captured by the implementer 2026-08-17 (review.md L1b implementation report) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B31 | if at 326:3 — an ascending merged list, and the same minute twice in a row | not-applicable through `official.Client` (comment at 320–324); interface guards `TestPollRefusesAMergedListThatIsNotStrictlyDescending`, `TestPollInterfaceGuardRefusesAnAdjacentDuplicateInstant` (zero bars stored) | build/behaviour RED captured by the implementer 2026-08-17 (review.md L1b implementation report) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B32 | if at 333:2 — `NewestObserved` published when bars exist, as a converted open instant | `TestPollNeverAdmitsTheNewestObservedBar`, `TestPollConvertsTheBrokerCloseLabelIntoAnOpenInstant` (`NewestObserved == label − 1 min`); the zero-bar side (a first page with no candles) is untested — recorded gap | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B33 | if at 345:2 — the de-duplication read itself fails | untested: the doubles fail `Append`, never `SealBarSeries` (recorded gap) | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B34 | range at 350:2 — stored rows indexed by open minute (D2) | `TestPollRepolledWithIdenticalContentAppendsNothing`, `TestPollDeduplicatesEveryStoredRowRegardlessOfIngestionInstant` | build/behaviour RED captured by the implementer 2026-08-17 (review.md L1b implementation report) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B35 | for at 357:2 — a 3-bar page yields 2 envelopes; the newest is admitted only on the next poll | `TestPollNeverAdmitsTheNewestObservedBar`, `TestPollAdmitsThePreviousNewestBarOnceASuccessorExists`, `TestPollBindsEachSuccessorToTheNextNewerObservedBar` | build/behaviour RED captured by the implementer 2026-08-17 (review.md L1b implementation report) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B36 | if at 359:3 — the window test runs on converted opens: the bar **labelled** 16:00 ET (opening 15:59) is the last regular bar and the bar **labelled** 09:30 ET (opening 09:29) is pre-market; bars outside the window are still counted and used as successors | `TestPollTreatsTheClosingLabelAsTheLastRegularBar` (5 labels → 3 stored, opens 09:30/15:58/15:59), `TestPollTreatsTheKoreanClosingLabelAsTheLastRegularBar` (KRX 09:00–15:30 KST, same shape), `TestPollStoresOnlyRegularSessionBarsButUsesTheOthersAsSuccessors` (open-instant helpers; the bar opening 15:59 takes the 16:00 after-hours bar as its successor) | 13 producer tests failed one minute off before the conversion landed (review.md, decision-30 correction 2026-08-18) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B37 | if at 368:3 — a row stamped after `PollAt` is still seen (ruling 25 horizon) | `TestPollDeduplicatesEveryStoredRowRegardlessOfIngestionInstant`, `TestPollRetryAfterAStoreErrorDoesNotConflict` | build/behaviour RED captured by the implementer 2026-08-17 (review.md L1b implementation report) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B38 | if at 369:4 — identical raw ⇒ `Unchanged`, zero rows; changed close ⇒ revision 2 with `SupersedesRevisionIdentity` r1 | `TestPollRepolledWithIdenticalContentAppendsNothing`, `TestPollWritesACorrectionAsARevisionAndLeavesTheEarlierReplayAlone` | build/behaviour RED captured by the implementer 2026-08-17 (review.md L1b implementation report) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B39 | if at 383:3 — a constructor-refused bar is recorded and the others still land | `TestPollRecordsAConstructorRefusalAndKeepsTheOtherBars` | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B40 | if at 387:3 — `Append` returns an error | `TestPollReturnsStoreErrorWithTheCountsSoFar`, `TestPollCountsAConflictWhenAForeignWriterHoldsTheSameRevision` | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B41 | if at 388:4 — a foreign same-revision row ⇒ `Conflicts == 1` and the poll continues; any other error ⇒ `STORE_ERROR` with the counts so far | `TestPollCountsAConflictWhenAForeignWriterHoldsTheSameRevision`, `TestPollReturnsStoreErrorWithTheCountsSoFar` | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B42 | if at 395:3 — an admitted correction increments `Corrections` | `TestPollWritesACorrectionAsARevisionAndLeavesTheEarlierReplayAlone` | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |

Whole-function properties (calls and result fields, not branches): decision-30 clock conversion `TestPollConvertsTheBrokerCloseLabelIntoAnOpenInstant`; gaps counted on converted opens `TestPollReportsGapsOnConvertedOpenInstants`; determinism `TestPollIsDeterministic`; provenance `TestPollTakesObservedAtFromTheResponseNotThePollInstant`; end-to-end through the real client `TestPollEndToEndThroughTheRealOfficialClient`; gap clamping `TestPollGapsAreClampedToTheRegularWindow`; KR binding `TestPollUsesTheKoreanCalendarAndSessionPrefix`; package guards `TestProductionImportsStayInsideTheAllowlist`, `TestProductionWritesEvidenceOnlyThroughTheCombinedConstructor`, `TestPackageCarriesNoMeasurementSeamIdentifier`.

Verification (this bundle, 2026-08-18): `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0, 408 tests passed across the two packages.
