# Branch Test Map: `adoptPage`

- Source: `internal/officialbars/producer.go`, SHA-256 `8d45ca93b090cfe9e10a93e5a658991ed3376820b56dfa05e49b809171c16772`; branch IDs follow `ast.json` (3 branches, regenerated 2026-08-18 against the decision-30 sources).
- New function (lot L1b, not in the frozen base 016da624). RED per review.md: build-failure RED on the new symbols before GREEN; the provenance rule this function carries (`ObservedAt` from the response, not from `PollAt`) was pinned in the ruling 29 fix round.
- **Decision-30 correction (2026-08-18).** The 03:29 KST US probe proved the broker's `timestamp` is the bar's close, so this function gained the `−60 s` conversion and a third branch (B3, the whole-minute assertion on the converted side). The decision-30 RED was behavioural and already recorded in review.md: with the conversion absent, 13 producer tests failed exactly one minute off before the subtraction was added. Existing tests were re-based through the `usBar`/`krBar` helpers, which now synthesise `Timestamp = open + 1m`; the new `usBarLabelled`/`krBarLabelled` helpers speak raw broker labels.
- Tests: `internal/officialbars/producer_test.go`.

| Branch | Scenario anchor | Required test disposition | RED observed | GREEN observed |
|---|---|---|---|---|
| B1 | range at 417:2 — a page's candles converted from close labels to open instants, in order, with the response's `ReadAt` and `BodyDigest` | `TestPollConvertsTheBrokerCloseLabelIntoAnOpenInstant` (labels written by hand; every stored `OpenAtMS`, every `SuccessorOpenAtMS` and `NewestObserved` are label − 1 min), `TestPollTakesObservedAtFromTheResponseNotThePollInstant`, `TestPollCrawlsPagesInTheMeasuredShape`, `TestPollNeverAdmitsTheNewestObservedBar` | 13 producer tests failed one minute off before the conversion landed (review.md, decision-30 correction 2026-08-18) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B2 | if at 419:3 — a candle timestamp `time.Parse` cannot read | not-applicable: the reader refuses such a candle before the producer sees it (grammar, parse and ruling 26 minute alignment); defence for a foreign `CandleReader`, undriven by any interface-guard test | not-applicable | not-applicable |
| B3 | if at 426:3 — the converted open instant is not on a whole minute | not-applicable for the taken side: ruling 26 already refuses any label with non-zero seconds or a fraction, and label − 60 s stays minute-aligned, so no fixture can reach it behind `official.Client` (recorded gap; fails closed). Untaken side driven on every converted bar by `TestPollConvertsTheBrokerCloseLabelIntoAnOpenInstant`, `TestPollTreatsTheClosingLabelAsTheLastRegularBar`, `TestPollTreatsTheKoreanClosingLabelAsTheLastRegularBar` | not-applicable (taken side); the untaken side rode the same 13-test decision-30 RED | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |

Verification (this bundle, 2026-08-18): `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0, 408 tests passed across the two packages.
