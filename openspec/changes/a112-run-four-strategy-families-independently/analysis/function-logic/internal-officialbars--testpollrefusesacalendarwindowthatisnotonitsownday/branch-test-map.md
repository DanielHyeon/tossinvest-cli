# Branch Test Map: `TestPollRefusesACalendarWindowThatIsNotOnItsOwnDay`

- Source: `internal/officialbars/producer_test.go` **at the frozen base `d9d9f71f`** (revision `base`), SHA-256 `1d36e945513968599fdb553b18c1b8160436965aeb68ca345be054d25f12afde`; branch IDs follow `ast.json` (3 branches, regenerated 2026-08-18). Coordinates below are base coordinates — the same 25 lines sit at 1492–1516 in the working tree.
- Disposition: **Test helper / test function caught by the gate because it existed at the frozen base `d9d9f71f` and the decision-30 correction edited it. Enumerated for gate completeness: these carry no production branch.**
- Measured qualification: the function's 25 lines are **byte-identical** between `d9d9f71f` and the working tree (base 1473–1497 → current 1492–1516, +19). What the correction appended is the block of four new decision-30 tests immediately below it, reported by `git diff --unified=0 d9d9f71f` as `@@ -1497,0 +1517,153 @@` — an insertion at base line 1497, which `check_analysis` counts as touching `start..end+1` on the base side while the new-side lines fall past the current range. No assertion, fixture or expected value in it changed, and it never constructs a bar, so no broker label crosses it.
- Tests: `internal/officialbars/producer_test.go` (this file). The production branches it drives are `PollClosedBars` B8/B9, enumerated in `internal-officialbars--pollclosedbars`.

| Branch | Scenario anchor | Required test disposition | RED observed | GREEN observed |
|---|---|---|---|---|
| B1 | if at base 1488:2 — a hand-built snapshot whose `Today.Regular` sits one day back is **not** refused (`err == nil`) → `t.Fatal` | proves the poll refuses at all; without it the two rows below could pass vacuously on an accepted poll that merely stored nothing | n/a (test helper; the decision-30 RED was the 13 producer tests that failed one minute off, review.md 2026-08-18) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0, 408 tests passed, 0 skipped (2026-08-18) |
| B2 | if at base 1491:2 — the typed reason is not `RefusalCalendarInvalid` → `t.Fatalf` | proves the refusal comes from the window-edge day check (`PollClosedBars` B9) and not from a neighbouring rule — `CALENDAR_DAY_MISMATCH` (B11, the poll instant's day), `NO_REGULAR_SESSION` (B7), or a reader/store error. `refusalOf` unwraps to `*PollRefusal`, so the reason is read from the type, never from a message substring | n/a (test helper; the decision-30 RED was the 13 producer tests that failed one minute off, review.md 2026-08-18) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0, 408 tests passed, 0 skipped (2026-08-18) |
| B3 | if at base 1494:2 — the `fakeReader` recorded any call → `t.Fatalf` | proves fail-closed ordering: an invalid calendar spends zero quota on the shared OpenAPI credential. This is the assertion `internal-officialbars--pollclosedbars`' safety conclusion cites for "an invalid calendar sends no request at all" | n/a (test helper; the decision-30 RED was the 13 producer tests that failed one minute off, review.md 2026-08-18) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0, 408 tests passed, 0 skipped (2026-08-18) |

Note on the GREEN column: the command runs the **working tree**, not the base. That is honest here precisely because the base and current bodies are byte-identical — the run exercises the same 25 lines the base AST describes.

Verification (this bundle, 2026-08-18): `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 — `ok internal/official 4.685s`, `ok internal/officialbars 1.548s`, 408 `=== RUN` / 408 PASS, 0 SKIP.
