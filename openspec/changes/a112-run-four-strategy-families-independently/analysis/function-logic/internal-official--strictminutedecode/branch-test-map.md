# Branch Test Map: `strictMinuteDecode`

- Source: `internal/official/strict_minute_candles.go`, SHA-256 `d32181a939f298db306f492b488468b5925ac0ba97dad3f82cb1cb3286254ced`; branch IDs follow `ast.json` (10 branches, regenerated 2026-08-18 against the decision-30 sources).
- New function (lot L1b, not in the frozen base 016da624). RED per review.md: the first cut had no `result`-key-count rule and treated an absent `nextBefore` as terminal — `unknown result key` and `cursor absent` failed before GREEN; the UTF-8 rule was re-pinned RED-first in the ruling 29 fix round after the reviewers showed it was masked by a cursor-only fixture.
- **Decision-30 correction (2026-08-18).** The broker's `timestamp` is the bar's close instant, not its open (US probe 03:29 KST, review.md). This function's behaviour is unchanged; only its documentation and its source line numbers moved. Branch count unchanged.
- Tests: `internal/official/strict_minute_candles_test.go`.

| Branch | Scenario anchor | Required test disposition | RED observed | GREEN observed |
|---|---|---|---|---|
| B1 | if at 255:2 — a body just above 2 MiB | `TestStrictMinuteCandlesRefusesABodyAboveTwoMebibytes` | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B2 | if at 261:2 — a broken byte inside `openPrice`, and one inside the cursor | `TestStrictMinuteCandlesRefusesMalformedBodies` (subtest `invalid utf-8 inside a price`), `TestStrictMinuteCandlesRefusesInvalidUTF8` | build/behaviour RED captured by the implementer 2026-08-17 (review.md L1b implementation report) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B3 | if at 264:2 — duplicate keys at three depths plus an ignored sibling, trailing value, empty body, 17-deep nesting | `TestStrictMinuteCandlesRefusesMalformedBodies` (subtests `duplicate key in the envelope`, `duplicate key in the result`, `duplicate key in a candle`, `duplicate key inside an ignored sibling`, `trailing json value`, `empty body`, `seventeen levels of nesting`) | build/behaviour RED captured by the implementer 2026-08-17 (review.md L1b implementation report) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B4 | if at 268:2 — a well-formed array as the whole body | `TestStrictMinuteCandlesRefusesMalformedBodies` (subtest `body is not an object`) | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B5 | if at 273:2 — the envelope carries `data` instead of `result` | `TestStrictMinuteCandlesRefusesMalformedBodies` (subtest `result absent`) | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B6 | if at 277:2 — `result` is an array | `TestStrictMinuteCandlesRefusesMalformedBodies` (subtest `result is not an object`) | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B7 | if at 282:2 — `result` carries only `nextBefore` | `TestStrictMinuteCandlesRefusesMalformedBodies` (subtest `candles absent`) | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B8 | if at 285:2 — a third key `hasMore`, and a `result` carrying `candles` alone | `TestStrictMinuteCandlesRefusesMalformedBodies` (subtests `unknown result key`, `cursor absent`) | build/behaviour RED captured by the implementer 2026-08-17 (review.md L1b implementation report) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B9 | if at 291:2 — every candle-level refusal reaches the caller unchanged | `TestStrictMinuteCandlesRefusesMalformedBodies` (candle subtests), `TestStrictMinuteCandlesRefusesMoreCandlesThanRequested`, `TestStrictMinuteCandlesRefusesInstantsNewerThanBefore` | build/behaviour RED captured by the implementer 2026-08-17 (review.md L1b implementation report) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |
| B10 | if at 295:2 — every cursor-level refusal reaches the caller unchanged | `TestStrictMinuteCandlesRefusesMalformedBodies` (cursor subtests) | build/behaviour RED captured by the implementer 2026-08-17 (review.md L1b implementation report) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-18) |

Accepting side (all ten branches fall through): `TestStrictMinuteCandlesSendsTheCanonicalQueryAndReturnsThePage`, `TestStrictMinuteCandlesAcceptsTheKoreanMarket`, `TestStrictMinuteCandlesIgnoresUnknownEnvelopeKeys`, `TestStrictMinuteCandlesAcceptsNestingUpToTheBound`, `TestStrictMinuteCandlesAcceptsAnEmptyPageThatStillCarriesACursor`.

Verification (this bundle, 2026-08-18): `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0, 408 tests passed across the two packages.
