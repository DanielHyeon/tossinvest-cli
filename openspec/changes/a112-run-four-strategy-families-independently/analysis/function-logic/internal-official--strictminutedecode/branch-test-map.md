# Branch Test Map: `strictMinuteDecode`

- Source: `internal/official/strict_minute_candles.go`, SHA-256 `441bed46f81bc928cab03d512b3ff1305c0c663cb1b58027986e2e91b739977d`; branch IDs follow `ast.json` (10 branches, generated 2026-08-17 after GREEN).
- New function (lot L1b, not in the frozen base 016da624). RED per review.md: the first cut had no `result`-key-count rule and treated an absent `nextBefore` as terminal — `unknown result key` and `cursor absent` failed before GREEN; the UTF-8 rule was re-pinned RED-first in the ruling 29 fix round after the reviewers showed it was masked by a cursor-only fixture.
- Tests: `internal/official/strict_minute_candles_test.go`.

| Branch | Scenario anchor | Required test disposition | RED observed | GREEN observed |
|---|---|---|---|---|
| B1 | if at 244:2 — a body just above 2 MiB | `TestStrictMinuteCandlesRefusesABodyAboveTwoMebibytes` | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-17) |
| B2 | if at 250:2 — a broken byte inside `openPrice`, and one inside the cursor | `TestStrictMinuteCandlesRefusesMalformedBodies` (subtest `invalid utf-8 inside a price`), `TestStrictMinuteCandlesRefusesInvalidUTF8` | build/behaviour RED captured by the implementer 2026-08-17 (review.md L1b implementation report) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-17) |
| B3 | if at 253:2 — duplicate keys at three depths plus an ignored sibling, trailing value, empty body, 17-deep nesting | `TestStrictMinuteCandlesRefusesMalformedBodies` (subtests `duplicate key in the envelope`, `duplicate key in the result`, `duplicate key in a candle`, `duplicate key inside an ignored sibling`, `trailing json value`, `empty body`, `seventeen levels of nesting`) | build/behaviour RED captured by the implementer 2026-08-17 (review.md L1b implementation report) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-17) |
| B4 | if at 257:2 — a well-formed array as the whole body | `TestStrictMinuteCandlesRefusesMalformedBodies` (subtest `body is not an object`) | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-17) |
| B5 | if at 262:2 — the envelope carries `data` instead of `result` | `TestStrictMinuteCandlesRefusesMalformedBodies` (subtest `result absent`) | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-17) |
| B6 | if at 266:2 — `result` is an array | `TestStrictMinuteCandlesRefusesMalformedBodies` (subtest `result is not an object`) | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-17) |
| B7 | if at 271:2 — `result` carries only `nextBefore` | `TestStrictMinuteCandlesRefusesMalformedBodies` (subtest `candles absent`) | n/a (new code; pinned by mutation, see review.md) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-17) |
| B8 | if at 274:2 — a third key `hasMore`, and a `result` carrying `candles` alone | `TestStrictMinuteCandlesRefusesMalformedBodies` (subtests `unknown result key`, `cursor absent`) | build/behaviour RED captured by the implementer 2026-08-17 (review.md L1b implementation report) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-17) |
| B9 | if at 280:2 — every candle-level refusal reaches the caller unchanged | `TestStrictMinuteCandlesRefusesMalformedBodies` (candle subtests), `TestStrictMinuteCandlesRefusesMoreCandlesThanRequested`, `TestStrictMinuteCandlesRefusesInstantsNewerThanBefore` | build/behaviour RED captured by the implementer 2026-08-17 (review.md L1b implementation report) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-17) |
| B10 | if at 284:2 — every cursor-level refusal reaches the caller unchanged | `TestStrictMinuteCandlesRefusesMalformedBodies` (cursor subtests) | build/behaviour RED captured by the implementer 2026-08-17 (review.md L1b implementation report) | `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0 (2026-08-17) |

Accepting side (all ten branches fall through): `TestStrictMinuteCandlesSendsTheCanonicalQueryAndReturnsThePage`, `TestStrictMinuteCandlesAcceptsTheKoreanMarket`, `TestStrictMinuteCandlesIgnoresUnknownEnvelopeKeys`, `TestStrictMinuteCandlesAcceptsNestingUpToTheBound`, `TestStrictMinuteCandlesAcceptsAnEmptyPageThatStillCarriesACursor`.

Verification (this bundle, 2026-08-17): `go test ./internal/official ./internal/officialbars -count=1 -race` exit 0, 404 tests passed across the two packages.
