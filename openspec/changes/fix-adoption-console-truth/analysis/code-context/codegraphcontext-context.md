# CodeGraphContext supporting context

- `codegraphcontext update . --quiet` and `codegraphcontext report .` completed.
- The advisory report produced no additional task-specific symbol or cross-file
  candidate.
- Relevant candidates remain the hard-evidence set:
  `runConsole`, `engineJournalDir`, `openEngineJournal`,
  `handleSettingsSave`, `StopPctSlider`, `StopPctPercent`,
  `OpenReadOnly.checkSchema`, and `LivePositionExits`.
- This absence is advisory only and grants no edit authority.

