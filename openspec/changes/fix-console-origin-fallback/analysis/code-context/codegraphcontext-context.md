# CodeGraphContext supporting context

- `codegraphcontext stats .` completed: 5,469 files, 9,434 functions, 30
  classes, 145 imported modules.
- The generated repository report surfaced generic complexity/dead-code
  candidates but no `sameOrigin`, `mutating`, or remote-console-specific
  relationship.
- Therefore CodeGraphContext contributes no additional implementation authority
  for this narrow fix. It is advisory and the generated root report was removed
  after its relevant absence was recorded here.

The current HEAD, CodeGraph definitions/callers/impact, the Go AST, and focused
HTTP tests remain the hard evidence.
