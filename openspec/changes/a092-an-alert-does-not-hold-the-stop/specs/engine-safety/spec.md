# engine-safety — a092 delta

> **MODIFIED 하나만 쓴다.** 어떤 이벤트가 critical인지, 전달 실패의 결과가 무엇인지,
> 루프 집합이 무엇인지는 **전부 그대로다.**
>
> **17판에서 a092가 더하는 것이 바뀌었다.** 16판까지는 *"전송 예산은 조립하는 자리에
> 적혀 있어야 한다"*였다. 17판은 **"엔진 루프는 알림 전송을 동기로 기다리지 않는다"**이다.
>
> 바꾼 이유는 이 델타 자신이 세 번 적어 두었다. 16판의 :28은 *"상한이 필요하다면 전송이
> 루프 밖으로 나가야 한다"*, :30은 *"누적을 없애려면 전송이 루프 밖으로 나가야 한다"*,
> :34는 *"이 배수를 없애려면 전송이 루프 밖으로 나가야 하며, 그 구조는 이 요구가 정의하지
> 않는다"*로 끝났다. 세 문장이 같은 곳을 가리키는데 열여섯 판이 그것을 범위 밖에 두었고,
> 그래서 남은 요구는 **편성의 명시**뿐이어서 16라운드가 *"세 필드를 채우는 것은 명시된
> 안전 목표를 달성하지 않는다"*(A-1)로 막았다. 17판은 그 구조를 **정의한다**.
>
> 3판까지 이 델타는 세 번 틀렸다 (기록).
> 2판은 예산 기준을 "가장 짧은 주기의 루프"라고 썼는데 그것은 exit 관측이 아니다 —
> 감독자가 1초 주기다(`DefaultHealthInterval`, `runtime.go:84`).
> 3판은 (a) 조립 SHALL의 **범위를 한정하지 않아** CLI 시험 발송(`cmd/tossctl/notificationsettings.go:151`)이
> 자기 SHALL을 위반하게 만들었고, (b) 감독자 면제의 근거 두 개 중 **하나가 거짓**이었으며
> (`runtime.go:415`에는 기한이 없다), (c) "동기 체류의 **합**"을 약속해 **자기 테스트와
> 모순**됐다.
>
> **20판이 이 머리 주석의 마지막 문장을 고쳤다** (review §21 ①, 사용자 결정 9-2·10-1·12-2).
> 19판까지 여기에는 *"「엔진 런타임 수명주기」는 루프를 하나 더한다 — 알림 배달 루프.
> base spec 루프 열거의 `strategy-entry` drift는 여전히 미배정 후속이 다룬다"*가 있었다.
> **두 절 다 a098 착지(`3851824b`)로 거짓이 됐다.** 정본 「엔진 런타임 수명주기」는 감독 루프
> 집합을 넷(reconcile·exit·체결 감지·`strategy-entry`)으로 이미 고쳤고 — drift는 미배정이
> 아니다 — 알림 배달을 **감독 집합 밖의 보조 실행자**로 둔다(결정 9-2).
> **이 델타는 루프를 더하지 않고 「엔진 런타임 수명주기」를 건드리지 않는다.** 아래의
> 「배달 실행자」는 전부 그 보조 실행자(a098 `internal/app/engine/alertdelivery.go`)다.

## MODIFIED Requirements

### Requirement: 등급화된 알림
알림은 등급화되어야 한다(SHALL): critical(IN_DOUBT·UNRESOLVED_IN_DOUBT 발생, 자격증명 만료 임박, 영구 불일치, UNKNOWN_BROKER_STATE)은 로컬 durable outbox에 기록 후 전송하고, 전달 실패가 지속되면 신규 진입을 차단한다(SHALL). 일반(체결·상태 전이)은 best-effort. 죽은 프로세스는 스스로 통지할 수 없으므로 heartbeat(예상 주기 초과 시 ntfy 측에서 경보) 방식을 사용한다(SHALL). 알림 이벤트 타입은 확장 가능한 enum으로 정의하고 Phase 2 이벤트(kill switch·운영 모드)는 예약만 한다.

**엔진 루프는 알림의 원격 전송을 동기로 기다려서는 안 된다(SHALL NOT — 손절 판정이 사는 루프이고 §0.3이 걸리는 자리다).** 루프 안에서 완료되어야 하는 것은 **그 등급이 갖는 내구 기록까지**이며(SHALL — critical은 outbox 행 하나, 일반 등급은 내구 기록이 없으므로 구조화 로그 줄 하나다. 기록은 전송보다 먼저라는 기존 요구는 완화되지 않고 강화된다), 원격 전송·시도 간 대기·시도별 실패 기록·전달 실패에 따른 진입 게이트 래치·운영 모드 승격은 루프 **밖**의 배달 경로가 수행해야 한다(SHALL).

이 요구의 범위는 **엔진 루프가 기다리는 경로**이며, 사람이 명령을 입력해 기다리는 대화형 시험 발송은 루프를 붙잡지 않으므로 여기에 들지 않는다(SHALL NOT — 범위를 넓히면 이 요구는 사람의 대기 시간까지 규정하게 되고, 그것은 §0.3이 지키려는 것이 아니다).

> **18판이 이 두 문장을 문단으로 갈랐다.** 앞 문단에 등급별 내구 기록을 적자 체류 낱말 둘이 한 문단에 모였고, 같은 문단의 범위 배제 문장이 `check_values.py`의 부정 표현 규칙에 걸렸다 — **한 문단이 열거와 배제를 동시에 하고 있었다.** 검사기를 고치지 않고 문단을 갈랐다. 검사기가 맞았다.
>
> 이 주석은 그 낱말들과 부정 표현을 **재현하지 않는다.** 재현하면 이 주석 자체가 같은 규칙에 걸린다 — 이 change에서 아홉 번째 되는 형태이고, 실제로 한 번 걸린 뒤에 이렇게 고쳤다.

**이 요구는 등급으로 좁혀지지 않는다(SHALL NOT).** critical만 루프 밖으로 옮기고 일반 등급의 전송을 루프 안에 남기는 구성은 이 요구를 만족하지 않는다 — 일반 등급의 동기 발행도 같은 네트워크 왕복을 같은 루프에서 기다린다. 일반 등급은 durable 기록을 갖지 않으므로 그 이관은 유실을 허용하는 형태여도 된다(SHALL — 버리는 것이 등급 강등이 아닌 이유는 이 등급의 정의가 best-effort이기 때문이다). 다만 **무엇을 버렸는지는 기록되어야 한다(SHALL — 기록 없는 유실은 전송된 것과 구별되지 않는다).**

**배달 경로는 정본 「엔진 런타임 수명주기」의 보조 실행자여야 한다(SHALL) — 감독 루프여서는 안 된다(SHALL NOT — 사용자 결정 9-2).** 감독 밖의 실행자가 「아무도 안 본다」가 되지 않게 하는 것은 루프 감독이 아니라 런타임이 보조 실행자에 대해 지는 세 의무(반환을 기다린 뒤 원장을 닫는다 · 패닉이 프로세스를 죽이지 않게 한다 · 비정상 정지를 관측하고 기록한다)이며, 이 델타는 그 셋을 바꾸지 않는다. 같은 행이 두 번 나가지 않게 하는 것은 실행자의 수가 아니라 **원장의 임차**다(정본 「발송 권한은 원장이 준다」) — 그러므로 이 델타는 배달 경로가 하나라는 사실을 배제의 근거로 쓰지 않는다. 배달 실행자는 자기 사이클의 작업량에 상한을 가져야 한다(SHALL — 밀린 행 전부를 한 사이클에서 처리하는 구성은 사이클 자체가 무계다).

> **20판이 이 문단을 고쳤다 (review §21 ②).** 19판은 *"그 실행자는 엔진의 루프 감독 아래에 있어야 하며(SHALL)"*와 *"감독되는 단일 실행자"*를 적었다. 결정 9-2는 배달 실행자를 **감독 밖**에 두고, a099는 배제를 **원장**으로 옮겼다. 19판의 근거(*"감독 밖의 goroutine은 죽어도 아무도 모른다"*)는 참이었고, 정본은 그것을 감독이 아니라 보조 실행자 의무 ③으로 막는다.

**배달 실행자의 정지가 다른 루프를 내려서는 안 된다(SHALL NOT).** 이 요구의 정본은 a098이 착지시킨 「배달 실행자의 정지가 다른 루프를 내려서는 안 된다」와 「발송 주체의 부재는 발송 실패와 다른 차단 사유다」이며, 이 델타는 그 둘을 바꾸지 않는다. 배달 실행자가 반환했다는 이유로 exit 관측 루프를 내리는 구성은 **알림 실패를 손절 부재로 바꾸며**(SHALL NOT — 엔진이 멈추면 브로커측 보호가 없는 구성에서 손절은 존재하지 않는다), 그것은 이 요구가 막으려는 것보다 무겁다. 대신 그 정지는 진입 게이트를 **발송 주체 부재의 자기 사유 코드**(`ReasonAlertSenderDown`, 값 `critical_alert_sender_down`)로 래치해야 하고(SHALL — 사용자 결정 8-1. 전달 실패의 사유 `critical_alert_undelivered`와 한 코드로 합치면 밀린 행을 전부 승인하는 순간 「보낼 주체가 없다」까지 풀린다), **운영 모드 승격으로 이어지지 않는다**(SHALL NOT — 사용자 결정 11-1. 모드는 원장에 남고 완화에 사람 승인이 필요하므로 「복구는 재시작」이 거짓이 된다. 재시작을 건너 남아야 하는 것은 미전달 행이고, 그것은 정본 「재시작이 진입 차단을 푸는 우회로가 되어서는 안 된다」의 기동 복원이 다시 잠근다). 죽은 실행자는 **자동으로 되살아나지 않는다**(SHALL NOT — 재시작하지 않는다는 런타임의 기존 계약은 이 요구가 바꾸지 않는다).

> **20판이 이 문단을 고쳤다 (review §21 ③).** 19판은 *"그 정지는 진입 게이트 래치와 운영 모드 승격으로 이어져야 하고(SHALL)"*를 적었다. 결정 11-1이 승격을 뺐고 결정 8-1이 사유 코드를 갈랐다. 이 요구의 둘째 문단(*엔진 루프는 알림의 원격 전송을…*)이 적는 **전달 실패**에 따른 승격(`ModeTriggerCriticalAlertUndelivered`)은 그대로다 — 그것은 시도가 실패한 경우이고, 이 문단은 **시도할 주체가 없는** 경우다. 둘을 가르는 것이 결정 8-1이다.

**배달 실행자의 행 선택은 굶주림을 만들어서는 안 된다(SHALL NOT).** 실패한 행은 미전달로 남고 선택은 오래된 것 먼저이므로, 시도를 다 쓴 오래된 행이 사이클마다 상한을 채우면 **새 critical 알림은 첫 시도조차 하지 못한다.** 그러므로 한 사이클의 선택은 아직 시도 한도에 이르지 않은 행을 **먼저** 골라야 하며(SHALL), 한도를 넘긴 행은 그 사이클의 잔여 자리에만 들어간다(SHALL). 한도를 넘긴 행을 **버려서는 안 된다**(SHALL NOT — 내구성 완화이고, 미전달의 결과인 진입 차단도 함께 사라진다). 빠지는 것은 우선순위뿐이다.

**진입 게이트 래치가 걸리기까지의 최악 시간은 change에 적혀야 한다(SHALL).** 그 값은 첫 시도 전의 **큐 대기**를 포함해야 하며(SHALL — 선행 배치의 서비스 시간이 그 앞에 통째로 들어올 수 있다), 큐를 뺀 값을 상한이라고 불러서는 안 된다(SHALL NOT — 그렇게 적힌 수는 다음 편집이 인용하는 수가 된다).

**배달 경로는 원격 전송 위에서 어떤 잠금도 잡고 있어서는 안 된다(SHALL NOT — 그렇게 하면 결함은 사라지지 않고 자리만 옮긴다).** 기록 경로가 배달 실행자와 잠금을 공유하는 것 자체는 금지되지 않는다(SHALL — 로컬 원장 작업만 덮는 잠금은 기록 경로를 원격 왕복만큼 붙잡지 않는다). 금지되는 것은 **그 잠금이 원격 전송을 덮는 것**이다. 따라서 배달 실행자가 기록 경로와 잠금을 공유한다면, 그 잠금은 대상 행을 고를 때와 결과를 정산할 때만 잡고 전송하는 동안에는 놓아야 한다(SHALL). 기록 경로와 잠금을 전혀 공유하지 않고 원장 임차로 배제하는 구성은 이 요구를 만족한다(SHALL — 정본 「배달 실행자는 잠금을 쥔 채 전송하지 않는다」가 배제를 원장에 두고, a098의 배달 실행자가 그 형태다).

> **18판이 이 문장을 교정했다.** 17판은 *"배달 잠금을 기록 경로에서 잡아서는 안 된다"*로 썼고, 그것은 금지 대상을 잘못 지목했다. 잠금을 없애면 `Acknowledge`의 세고-푸는 구간이 무방비가 되고 — 잠금은 그것을 잡는 쪽만 배제하므로 — **미전달 critical 알림이 있는데 진입 게이트가 열린다.** 막으려던 것(원격 왕복이 루프에 남는 것)은 이 문장에서 그대로 막히고, 잘못 금지했던 것(로컬 배제)은 풀린다.

**전송하는 동안 잠금을 놓는다는 것은 그 사이에 남이 그 행을 정착시킬 수 있다는 뜻이고, 그것은 허용되어야 한다(SHALL — 18라운드 A-P1).** 배달 실행자가 결과를 정산할 때 대상 행이 더 이상 `PENDING`이 아니면, 그것은 오류가 아니라 **운영자가 먼저 정산했다는 사실**로 다루어야 하며(SHALL), 배달 실행자는 그 행을 덮지 않고(SHALL NOT — 승인을 미전달로 되돌리는 것은 사람이 이미 본 사건을 다시 세우는 것이다) 남은 행들의 처리를 계속해야 한다(SHALL — 한 행의 선점이 배치를 중단시켜서는 안 된다). **선점이 일어났다는 사실은 기록되어야 한다(SHALL — 조용히 갈라지는 분기는 다음 사람이 경합을 결함으로 오해하는 자리다).**

**운영자의 승인은 진입 게이트를 풀 수 있다(SHALL) — 발송이 아직 진행 중이어도 그렇다.** 푸는 조건은 정본 「운영자가 밀린 알림을 읽고 승인하는 경로가 존재해야 한다」의 둘(승인했고 미전달 수가 0)이며 이 문단은 그것을 넓히지 않는다(SHALL NOT — 발송 중인 행은 승인 전까지 미전달로 세어진다, 정본 「발송 중인 행은 여전히 미전달이다」). 래치의 사유는 *알림이 전달되지 않았다*이고, 승인은 **사람이 그것을 봤다는 선언**이므로 사유가 소멸한다. 전송의 성공 여부는 그 사유를 되살리지 않는다(SHALL NOT — 사람이 이미 아는 사건을 전송 실패가 다시 차단으로 만들면 승인이 의미를 잃는다).

> **19판이 더한 문단이다 (사용자 결정 2026-08-10, 결정 2 = 안 1).** 18판은 발송 중 승인이 **일어날 수 없다**고 보고 세 보존 테스트가 전부 그대로 초록이라고 적었다. 잠금을 놓는 설계에서 그 전제는 성립하지 않는다 — 18라운드 A-P1이 그것을 잡았다. 이 델타는 경합을 부인하는 대신 **승자를 정한다**: 승인이 이긴다.

**기록 경로가 잠금 아래에서 하는 일은 로컬 원장 연산이어야 한다(SHALL).** 잠금 아래에 원격 호출·무기한 대기·사람의 입력을 넣는 구현은 이 요구를 만족하지 않는다(SHALL NOT). 다만 잠금 아래의 로컬 연산에도 **기한은 없다**(원장이 멈추면 관측도 멈춘다) — 이 요구가 만드는 것은 기한이 아니라 **그 항이 네트워크가 아니라는 것**이다.

**미전달 수를 읽고 진입 게이트를 푸는 판단은 나눌 수 없는 하나여야 한다(SHALL).** 그 사이에 새 PENDING 행이 생기면 그 판단은 틀린 것이고, 새 PENDING 행은 재무장뿐 아니라 **새 event key의 최초 기록으로도** 생긴다. 배제의 수단은 이 요구가 정하지 않되, 무엇이 그 구간을 지키는지는 change에 적혀야 한다(SHALL — 적히지 않은 조건은 다음 편집이 지우는 조건이다).

**전송 시도 횟수는 다시 계약이다(SHALL).** 시도를 줄이는 유일한 근거는 그 시도가 손절 루프를 붙잡는다는 것이었고, 루프 밖으로 나가면 그 근거가 없다. 그러므로 이 change는 시도 횟수를 **줄이지 않는다**(SHALL NOT — 줄이는 구성은 upstream이 이미 landed한 재시도 계약을 이 change가 거짓으로 만드는 구성이다). 시도 사이의 대기를 배달 루프의 주기로 대신하는 구현은 허용되며(SHALL), 그 경우 **한 사이클은 한 행을 한 번만 시도해야 한다**(SHALL — 한 사이클 안에서 재시도하면 사이클이 다시 무계가 된다).

**전송 수단이 구성되지 않은 배선에서도 시도는 실패로 세어져야 한다(SHALL).** 전송기가 없는 구성에서 배달 실행자가 아무 시도도 기록하지 않으면, critical 알림이 쌓여도 시도 소진이 영원히 일어나지 않아 진입이 차단되지 않는다. 전송기의 부재는 전송의 실패이며(SHALL), 그 실패는 다른 실패와 같은 결과를 가져야 한다(SHALL).

**진입 게이트 래치가 늦어지는 것은 이 재설계의 대가이며 측정되어 적혀야 한다(SHALL — 적히지 않은 대가는 없는 것처럼 읽힌다).** 전달 실패에 따른 신규 진입 차단은 전송이 루프 안에 있을 때보다 늦게 걸린다. 그 지연은 `다음 배달 사이클까지의 시간 + 시도 횟수 × 1회 상한 + (시도 횟수 − 1) × 사이클 주기`이며, change는 그 값을 적어야 한다(SHALL). **이 지연이 손절을 늦추지 않는다는 것은 게이트가 진입만 막기 때문이며, 그 사실이 인용의 조건이다(SHALL — 게이트가 청산을 막게 되는 편집이 들어오면 이 대가는 다시 계산되어야 한다).**

**durable 기록의 실패는 여전히 동기로 다루어진다(SHALL).** outbox 기록 자체가 실패하면 행이 없고, 행이 없으면 배달 실행자가 찾을 것도 없다. 그 경우의 진입 게이트 래치와 운영 모드 승격은 기록을 시도한 그 자리에서 일어나야 하며(SHALL — 루프 밖으로 미룰 대상이 없다), 그 비용은 실패한 로컬 쓰기 한 번이다.

**이 요구가 만드는 것은 여전히 보장이 아니다(SHALL NOT — 실제 체류가 주기를 넘지 않는다고 이 요구를 인용해서는 안 된다).** 루프에 남는 항은 구조화 로그 한 줄과 outbox 트랜잭션이고 **둘 다 기한이 없다.** 원장이 멈추면 관측도 멈춘다. 17판이 바꾸는 것은 기한 없는 항의 **크기**이지 기한의 유무가 아니다 — 네트워크 왕복 × 시도 + 시도 간 대기였던 것이 로컬 쓰기 하나가 된다. 그러므로 루프에 남는 몫은 이름을 갖고 편성되어야 하며(SHALL — 이름 없는 여유는 다음 편집이 말없이 먹는다), 그 몫이 관측 주기보다 작아야 한다(SHALL NOT — 같거나 크게 두는 편성은 이 요구를 위반한다).

**루프에 남는 몫의 근거가 그 경로를 직접 잰 것이 아니면 그렇게 적어야 한다(SHALL).** 축소된 경로의 실측이 없는 동안 상위집합의 실측을 대입하는 것은 보수적이므로 허용되며(SHALL), 그 대입이라는 사실과 실측 의무는 change에 남아야 한다(SHALL — 대입을 실측으로 적으면 다음 편집이 그 위에 짓는다).

기준 루프가 exit 관측인 것은 주기가 가장 짧아서가 아니다(SHALL 근거 — 감독자 루프는 1초로 더 짧다). 감독자가 면제되는 근거는 **열화 승격을 루프 이름별로 래치하여 주기마다 전송하지 않는다**는 것 하나다(SHALL). 감독자가 자기 알림에 이미 기한을 갖고 있다는 것은 근거가 되지 못한다(SHALL NOT — 감독자의 승격 경로는 알림 뒤에 기한 없는 두 번째 전송 대기를 갖는다). 더 짧은 주기로 도는 루프가 같은 전송 경로를 동기로 기다린다면 그 루프도 이 요구의 범위 안이다(SHALL).

**1회 전송 상한은 transport 구현이 소유한다(SHALL).** 조립부가 채운 값은 그 값을 읽는 transport에 대해서만 실효를 가지며, 그것을 읽지 않는 `Publisher` 구현에는 상한이 없다(SHALL NOT — 조립부에 적혔다는 사실이 모든 구현에 대한 보장은 아니다). **그 상한을 잘못 고르는 대가는 17판에서 내려간다(SHALL 근거 — 상한이 손절 경로 밖에 있으므로, 낮게 고른 상한은 손절을 늦추지 않고 배달 사이클 하나를 버린다).** 다만 실측 의무 자체는 남는다(SHALL — 실측 없이 고른 값은 여전히 실측 없이 고른 값이다).

예산 축소가 durability를 줄여서는 안 된다(SHALL NOT): outbox 기록은 전송 시도보다 먼저이며 예산과 무관하게 완료된다(SHALL). 전송 수단이 실제로는 메시지를 받았는데 응답이 상한 안에 오지 않아 실패로 기록되는 경우가 생긴다(SHALL — 그 경우 운영자는 중복을 받고 게이트는 래치되며, 이 손실은 인정되고 기록되어야 한다).

#### Scenario: critical 알림 전달 실패 지속
- **WHEN** critical 이벤트의 전송이 재시도 한도까지 실패하면
- **THEN** 신규 진입이 차단되고 outbox에 미전달 상태로 보존되며, 전달 복구 후 수동 확인으로 해제한다

#### Scenario: 엔진 루프가 전송을 기다리지 않는다
- **WHEN** exit 관측 사이클이 critical 알림을 올리고 전송 수단이 응답하지 않으면
- **THEN** 그 사이클은 **그 호출이 쓰는 구조화 로그 줄 하나와 outbox 트랜잭션 하나**를 마치고 즉시 다음 포지션으로 넘어가며, 전송·시도별 실패 기록·게이트 래치·승격 트랜잭션은 그 사이클 밖에서 일어난다 — 사이클이 붙잡히는 시간에 원격 왕복이 포함되지 않는다

#### Scenario: 기록 경로가 배달 잠금을 기다리지 않는다
- **WHEN** 배달 실행자가 밀린 행을 발행하는 중에 exit 관측 사이클이 알림을 올리면
- **THEN** 관측 사이클은 발행이 끝나기를 기다리지 않고 자기 기록을 마친다 — 기다리는 구성은 결함을 옮긴 것이지 없앤 것이 아니다

#### Scenario: 같은 행이 두 번 나가지 않는다
- **WHEN** 관측이 어떤 조건의 알림을 올리는 순간 배달 실행자가 같은 행을 발행하고 있으면
- **THEN** 그 행은 한 번만 발행되고, 발행되지 않은 내용이 전달됨으로 기록되지 않는다

#### Scenario: 전송 수단이 없는 배선
- **WHEN** `Publisher`가 구성되지 않은 엔진에서 critical 알림이 올라오면
- **THEN** 각 배달 사이클이 그것을 실패한 시도로 기록하고, 시도가 소진되면 진입이 차단된다 — 전송기의 부재가 진입 차단의 면제가 되어서는 안 된다

#### Scenario: 진입 차단이 늦게 걸린다
- **WHEN** 전송이 계속 실패해 시도가 소진되면
- **THEN** 진입 차단은 전송이 루프 안에 있던 구성보다 늦게 걸리며, 그 지연은 change에 측정된 값으로 적혀 있고, 그동안 청산 경로는 아무 영향도 받지 않는다

#### Scenario: outbox 기록 자체가 실패한다
- **WHEN** critical 알림의 durable 기록이 실패하면
- **THEN** 진입 게이트 래치와 운영 모드 승격이 그 자리에서 동기로 일어난다 — 배달 실행자가 찾을 행이 없으므로 미룰 대상이 없다

#### Scenario: 일반 등급도 루프를 붙잡지 않는다
- **WHEN** exit 관측 사이클이 일반 등급 알림을 올리고 전송 수단이 응답하지 않으면
- **THEN** 그 사이클은 기다리지 않고 넘어가며, 이관 버퍼가 가득 차 있으면 그 알림은 버려지고 버렸다는 사실이 기록된다

#### Scenario: 사이클 총 체류는 여전히 약속하지 않는다
- **WHEN** 한 관측 사이클이 억제 없는 경로에서 알림을 여러 개 올리면
- **THEN** 사이클의 총 동기 체류는 알림 수에 비례해 늘어나며 이 요구는 그것을 유계로 만들지 않는다 — 비례하는 항이 원격 왕복에서 로컬 쓰기로 바뀔 뿐이다

#### Scenario: 상한을 읽지 않는 transport
- **WHEN** 조립부가 1회 상한을 채웠는데 배선된 `Publisher`가 그 값을 읽지 않으면
- **THEN** 그 구성에는 1회 상한이 없으며 배달 사이클의 작업량 상한이 그 구현에 대해 성립하지 않는다

#### Scenario: 예산을 줄여도 기록은 그대로다
- **WHEN** 전송 예산이 줄어든 구성에서 critical 알림이 올라오고 전송이 전부 실패하면
- **THEN** outbox 행은 이전과 동일하게 기록되어 있고 진입 차단도 이전과 동일하게 걸린다

#### Scenario: 배달 실행자가 죽는다
- **WHEN** 배달 실행자의 `Run`이 취소가 아닌 사유로 반환하거나 패닉하면
- **THEN** 런타임이 **그 사실을 관측해 구조화 로그로 남기고**(보조 실행자 의무 ③ — 루프 감독이 아니다) 진입 게이트를 `ReasonAlertSenderDown`(`critical_alert_sender_down`)으로 래치한다 — 전달 실패의 사유 `critical_alert_undelivered`와 **다른 코드다**
- **AND** 운영 모드는 **승격되지 않는다**
- **AND** **다른 루프는 계속 돈다** — exit 관측 루프를 함께 내리는 구성은 알림 실패를 손절 부재로 바꾼다
- **AND** 죽은 실행자는 자동으로 되살아나지 않는다

#### Scenario: 다시 올릴 주기가 있어도 래치가 그것을 막는다
- **WHEN** 알림 산출 지점이 자기 래치를 세운 **뒤에** 알림을 올리는 구조이면
- **THEN** 다음 관측은 래치에서 early return하여 알림 호출에 닿지 않으므로, 그 경로의 재발송은 관측이 아니라 **배달 실행자**가 담당한다 — 17판에서 outbox 행이 PENDING으로 남는 것은 재발송의 근거가 되며(16판까지는 되지 않았다), 그 차이가 이 재설계가 만드는 것이다

#### Scenario: 재강화는 새 게이트에 투영되지 않는다
- **WHEN** 원장의 운영 모드가 이미 강화된 상태에서 같은 트리거로 다시 강화하면
- **THEN** 변화 없음으로 조기 반환되어 모드 투영과 통지가 실행되지 않으므로, 재시작 후의 새 게이트는 이 재강화로부터 아무것도 받지 못한다 — 기록의 영속성을 강제의 영속성으로 인용해서는 안 된다(SHALL NOT)
