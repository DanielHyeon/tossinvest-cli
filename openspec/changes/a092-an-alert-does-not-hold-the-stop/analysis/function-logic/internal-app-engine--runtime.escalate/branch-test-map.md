# Branch Test Map: `Runtime.escalate`

a092는 이 함수를 편집하지 않는다. 표는 **인용한 분기가 실재함**을 AST로 고정한다.

| Branch | 위치 | Scenario | Test | RED observed | GREEN observed |
|---|---|---|---|---|---|
| B1 | `:412` | 승격기나 계정이 없으면 알림만 내고 끝난다 — `:415`에 도달하지 않는다 | 기존 커버리지 | n/a | n/a |
| B2 | `:416` | 승격이 실패해도 루프는 계속 돈다(로그 한 줄) | 기존 커버리지 | n/a | n/a |

## 분기가 아닌, 이 표가 고정해야 하는 사실

| 자리 | 사실 | 왜 중요한가 |
|---|---|---|
| `:396` | `r.alert` → `Notify` #1, `alertCtx`에 30초 기한 | ¶22 (a)절이 참인 유일한 절반 |
| `:415` | `EscalateOperatingMode(ctx, …)` → `Notify` #2, **기한 없음** | ¶22 (a)절이 거짓인 절반 |
| B1이 유일한 이탈 | `:396`과 `:415` 사이에 다른 이탈이 없다 | 프로덕션은 B1을 통과하므로 두 전송이 **연달아** 일어난다 |
