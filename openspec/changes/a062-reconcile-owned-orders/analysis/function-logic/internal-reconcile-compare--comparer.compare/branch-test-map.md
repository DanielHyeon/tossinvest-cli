# Branch Test Map: `Comparer.Compare`

Source: `internal/reconcile/compare.go`

| Branch | Condition | Verification |
| --- | --- | --- |
| B1 | if at `internal/reconcile/compare.go:362` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B2 | range at `internal/reconcile/compare.go:368` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B3 | range at `internal/reconcile/compare.go:374` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B4 | if at `internal/reconcile/compare.go:375` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B5 | range at `internal/reconcile/compare.go:380` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B6 | if at `internal/reconcile/compare.go:382` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B7 | range at `internal/reconcile/compare.go:389` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B8 | if at `internal/reconcile/compare.go:394` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B9 | if at `internal/reconcile/compare.go:397` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B10 | switch at `internal/reconcile/compare.go:403` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B11 | case at `internal/reconcile/compare.go:404` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B12 | case at `internal/reconcile/compare.go:406` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B13 | case at `internal/reconcile/compare.go:412` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B14 | case at `internal/reconcile/compare.go:416` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B15 | range at `internal/reconcile/compare.go:426` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B16 | range at `internal/reconcile/compare.go:427` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B17 | if at `internal/reconcile/compare.go:429` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B18 | range at `internal/reconcile/compare.go:442` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B19 | if at `internal/reconcile/compare.go:443` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B20 | if at `internal/reconcile/compare.go:447` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B21 | range at `internal/reconcile/compare.go:454` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B22 | if at `internal/reconcile/compare.go:455` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B23 | range at `internal/reconcile/compare.go:463` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B24 | if at `internal/reconcile/compare.go:464` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B25 | if at `internal/reconcile/compare.go:469` | Focused canonical/temporal ownership regressions plus full and race suites. |

Coverage includes external exclusion, identifier reuse, partial-scope rejection, v19 migration, confirmed temporal ownership, reservation recovery, and operator-release refusal/success.
