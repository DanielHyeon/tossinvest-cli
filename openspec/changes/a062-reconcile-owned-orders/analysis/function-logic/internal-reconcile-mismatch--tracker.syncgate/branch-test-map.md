# Branch Test Map: `Tracker.syncGate`

Source: `internal/reconcile/mismatch.go`

| Branch | Condition | Verification |
| --- | --- | --- |
| B1 | if at `internal/reconcile/mismatch.go:879` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B2 | range at `internal/reconcile/mismatch.go:883` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B3 | if at `internal/reconcile/mismatch.go:884` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B4 | range at `internal/reconcile/mismatch.go:894` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B5 | if at `internal/reconcile/mismatch.go:895` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B6 | range at `internal/reconcile/mismatch.go:901` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B7 | if at `internal/reconcile/mismatch.go:902` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B8 | if at `internal/reconcile/mismatch.go:908` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B9 | range at `internal/reconcile/mismatch.go:913` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B10 | if at `internal/reconcile/mismatch.go:917` | Focused canonical/temporal ownership regressions plus full and race suites. |
| B11 | else at `internal/reconcile/mismatch.go:919` | Focused canonical/temporal ownership regressions plus full and race suites. |

Coverage includes external exclusion, identifier reuse, partial-scope rejection, v19 migration, confirmed temporal ownership, reservation recovery, and operator-release refusal/success.
