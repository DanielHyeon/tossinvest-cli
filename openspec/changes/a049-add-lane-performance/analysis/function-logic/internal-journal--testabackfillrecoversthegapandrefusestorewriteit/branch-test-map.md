# Branch Test Map: `TestABackfillRecoversTheGapAndRefusesToRewriteIt`

| Branch | Scenario | Test | RED observed | GREEN observed |
|---|---|---|---|---|
| B1 | `if` at line 235: `if err := j.SetApplyHooks(ApplyHooks{Project: ProjectPosition, Exit: ApplyExitFill}); err != nil {`; invariant: missing/corrupt/alternate path is explicit | `TestABackfillRecoversTheGapAndRefusesToRewriteIt` (this regression test) | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B2 | `if` at line 242: `if err != nil {`; invariant: missing/corrupt/alternate path is explicit | `TestABackfillRecoversTheGapAndRefusesToRewriteIt` (this regression test) | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B3 | `if` at line 245: `if filled.InitialQuantity != "10" {`; invariant: missing/corrupt/alternate path is explicit | `TestABackfillRecoversTheGapAndRefusesToRewriteIt` (this regression test) | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B4 | `if` at line 248: `if filled.CostTotal == nil {`; invariant: missing/corrupt/alternate path is explicit | `TestABackfillRecoversTheGapAndRefusesToRewriteIt` (this regression test) | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B5 | `if` at line 252: `if ratOf(*filled.CostTotal).Cmp(wantBackfillCost) != 0 {`; invariant: missing/corrupt/alternate path is explicit | `TestABackfillRecoversTheGapAndRefusesToRewriteIt` (this regression test) | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B6 | `if` at line 256: `if stored.CostTotal == nil \|\| *stored.CostTotal != *filled.CostTotal {`; invariant: missing/corrupt/alternate path is explicit | `TestABackfillRecoversTheGapAndRefusesToRewriteIt` (this regression test) | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B7 | `if` at line 259: `if _, err := j.BackfillTradeOutcome(ctx, id, costs.DefaultModel()); !errors.Is(err, ErrTradeOutcomeExists) {`; invariant: missing/corrupt/alternate path is explicit | `TestABackfillRecoversTheGapAndRefusesToRewriteIt` (this regression test) | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
