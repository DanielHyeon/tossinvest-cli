# Branch Test Map: `TestDashboardRejectsCorruptPersistedDecimalsInsteadOfUsingZero`

| Branch | Scenario | Test | RED observed | GREEN observed |
|---|---|---|---|---|
| B1 | `range` at line 121: `for _, test := range []struct {`; invariant: missing/corrupt/alternate path is explicit | `TestDashboardRejectsCorruptPersistedDecimalsInsteadOfUsingZero` (this regression test) | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B2 | `if` at line 126: `if _, err := store.db.Exec(\`UPDATE performance_trades SET realized_pnl_after_costs='broken'\`); err != nil {`; invariant: missing/corrupt/alternate path is explicit | `TestDashboardRejectsCorruptPersistedDecimalsInsteadOfUsingZero` (this regression test) | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B3 | `if` at line 131: `if _, err := store.db.Exec(\`UPDATE performance_trades SET realized_r='broken'\`); err != nil {`; invariant: missing/corrupt/alternate path is explicit | `TestDashboardRejectsCorruptPersistedDecimalsInsteadOfUsingZero` (this regression test) | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B4 | `if` at line 136: `if _, err := store.db.Exec(\`UPDATE metric_observations SET value='broken'`; invariant: missing/corrupt/alternate path is explicit | `TestDashboardRejectsCorruptPersistedDecimalsInsteadOfUsingZero` (this regression test) | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B5 | `if` at line 147: `if _, err := store.Collect(context.Background(), trade, []Observation{{`; invariant: missing/corrupt/alternate path is explicit | `TestDashboardRejectsCorruptPersistedDecimalsInsteadOfUsingZero` (this regression test) | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B6 | `if` at line 154: `if _, err := store.Dashboard(context.Background(), DefaultQuery(now)); err == nil \|\|`; invariant: missing/corrupt/alternate path is explicit | `TestDashboardRejectsCorruptPersistedDecimalsInsteadOfUsingZero` (this regression test) | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
