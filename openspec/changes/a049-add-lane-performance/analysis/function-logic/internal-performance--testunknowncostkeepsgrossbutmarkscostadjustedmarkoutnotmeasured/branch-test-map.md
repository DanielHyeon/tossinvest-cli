# Branch Test Map: `TestUnknownCostKeepsGrossButMarksCostAdjustedMarkoutNotMeasured`

| Branch | Scenario | Test | RED observed | GREEN observed |
|---|---|---|---|---|
| B1 | `if` at line 152: `if err := trade.validate(); err != nil {`; invariant: missing/corrupt/alternate path is explicit | `TestUnknownCostKeepsGrossButMarksCostAdjustedMarkoutNotMeasured` (this regression test) | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B2 | `if` at line 160: `if unknown.Status != StatusNotMeasured \|\| unknown.GrossPct != "5" \|\| unknown.CostAdjustedPct != "" \|\|`; invariant: missing/corrupt/alternate path is explicit | `TestUnknownCostKeepsGrossButMarksCostAdjustedMarkoutNotMeasured` (this regression test) | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B3 | `if` at line 167: `if zero.Status != StatusComplete \|\| zero.GrossPct != "5" \|\| zero.CostAdjustedPct != "5" {`; invariant: missing/corrupt/alternate path is explicit | `TestUnknownCostKeepsGrossButMarksCostAdjustedMarkoutNotMeasured` (this regression test) | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B4 | `range` at line 171: `for _, invalid := range []string{"broken", "-0.01"} {`; invariant: missing/corrupt/alternate path is explicit | `TestUnknownCostKeepsGrossButMarksCostAdjustedMarkoutNotMeasured` (this regression test) | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
| B5 | `if` at line 173: `if err := trade.validate(); err == nil {`; invariant: missing/corrupt/alternate path is explicit | `TestUnknownCostKeepsGrossButMarksCostAdjustedMarkoutNotMeasured` (this regression test) | not separately captured for this evidence refresh | package-targeted regression PASS before integration; rerun by gate |
