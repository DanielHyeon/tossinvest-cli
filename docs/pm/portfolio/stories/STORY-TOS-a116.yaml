{
  "id": "STORY-TOS-a116",
  "title": "Codex saves its own session context",
  "feature": "FEAT-TOS-001",
  "intent": "active",
  "openspec": {
    "change_id": "a116-install-codex-session-save-hook",
    "path": "openspec/changes/archive/2026-08-29-a116-install-codex-session-save-hook"
  },
  "found_by": {
    "date": "2026-08-17",
    "how": "The operator requested the Claude PostToolUse session-save behavior for Codex while preserving the existing Claude script and keeping each agent's generated context in a separate store.",
    "record": "Interactive operator request"
  },
  "acceptance": [
    "Codex PostToolUse for Bash and apply_patch invokes a Codex-only session saver without replacing the existing SDD observation hook",
    "Codex context is written only below .codex-context and never reads from or writes to the Claude .ai-context store",
    "The existing Claude settings and save-session.sh remain byte-for-byte outside this change",
    "Concurrent or malformed hook events cannot corrupt the latest summary, and generated Codex context remains untracked",
    "make gate CHANGE=a116-install-codex-session-save-hook passes"
  ],
  "measured": null,
  "deviations": [],
  "limits": [
    "Codex transcript parsing is defensive because the official hook documentation does not guarantee a stable transcript wire format.",
    "The operator must trust the new project hook definition through /hooks before Codex executes it."
  ]
}
